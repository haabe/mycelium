# Mycelium CI dependencies
#
# These packages are installed in CI ONLY (GitHub Actions workflow), not in
# runtime hooks. Runtime hooks use Python stdlib plus PyYAML where a canvas is read.
#
# EXACT PINS since 2026-09-10 (security review DL-1262, finding 4): a floating lower
# bound resolves to whatever PyPI holds on the day CI runs, which is the supply-chain
# shape pip-audit exists to catch and cannot when the set moves. Bump deliberately.
# pip-audit and zizmor are pinned here too, so the auditors are audited.
PyYAML==6.0.3
jsonschema==4.26.0
referencing==0.37.0
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.0
shellcheck-py==0.11.0.1
pip-audit==2.10.1
zizmor==1.30.1
