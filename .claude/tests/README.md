# Tests — Framework Structural Tests

- **[validate-template.sh](validate-template.sh)** — Validates that the Mycelium template structure is intact: required directories exist, required files are present, YAML files parse correctly. Run after `npx degit haabe/mycelium` to verify a clean clone.

For functional testing of the framework (does the interview skill work? do gates catch violations?), see [`../evals/`](../evals/).
