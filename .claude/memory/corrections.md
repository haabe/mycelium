# Corrections Log

*Mycelium's friction log (Hoskins, Ch4). A record of experience, not prescriptions. Read before every task — past mistakes are expensive lessons.*

## TL;DR

- **L5 sycophancy**: At market scale, agent uses promotional language in decision logs. Guardrail G-M1 added.
- **Decision log skipped**: 27/44 skills had no decision log instruction. G-P4 strengthened, 10 skills patched, evaluator expanded.
- **python vs python3**: macOS has `python3` not `python`. Use `python3` in all commands.
- **upgrade.sh must be glob-driven, not list-driven**: Hardcoded file lists drift when new files are added. Use globs with explicit preserve-lists.
- **Interview ceremony too long for sprints**: /interview consumed an entire session before any delivery work started. Need sprint-mode detection and minimum-viable interview path.
- **Process cliff after onboarding**: After /interview, entire Mycelium process was abandoned — no diamonds, no canvas updates, no theory gates for 75% of the session. Discovery-to-delivery transition needs a lightweight mode.
- **Over-scope before constraints**: Agent proposed 20-hour plan before learning user had 8 hours. Ask time/resource constraints before proposing scope.
- **Eval overfitting**: Agent encoded test answers into data documentation to pass evals. New anti-pattern documented.
- **provenance singular/plural mismatch**: `_common.schema.json` accepted only `source_classes` (plural array) inside provenance, but framework convention uses singular `source_class` everywhere else. Writers (especially `/interview`) reached for the singular form and got rejected. Schema now accepts both + `notes`.
- **Canvas Write trips Claude Code's read-before-write check**: Canvas files ship pre-populated as templates, so they exist on every fresh project. `Write` tool requires prior `Read` (same tool); `cat` via Bash doesn't count. Every canvas-writing skill (/interview, /canvas-update, /log-evidence, etc.) hit this. Documented in CLAUDE.md "Canvas writes — Read before Write."
- **Wayfinding map improvised against spec**: Agent rendered `You Are Here — Wayfinding Map` with vertical box-drawing characters instead of `YOUR JOURNEY` template with horizontal phase progression. Doc was descriptive, not prescriptive. Tightened wayfinding.md with explicit "STRICT — reproduce the template literally" + a list of common deviations.
- **`/diamond-progress` re-invocation interpreted as approval**: when a previous `/diamond-progress` left the diamond in pending-approval state, typing `/diamond-progress` again was silently treated as "yes, advance" rather than as a fresh evaluation request. Implicit behavior nowhere in SKILL.md. Surfaced during Juniors.dev pre-run dogfood (2026-05-06).
- **`/diamond-progress` is phase-transition-shaped, not child-spawn-aware**: skill assumes "current phase → next phase" and treats spawning a child diamond as a side-effect bullet under step 9. Real spawn operations need first-class workflow handling — perspective-conflict check on parent's canvases, parent-confidence-vs-effective-threshold check, child-confidence initialization rules, dual-state output (parent + child). Surfaced 2026-05-07 spawning L1 from L0 in dogfood.
- **JTBD schema lacks per-dimension backing fields (Christensen tripartite)**: schema fix shipped 2026-05-08 (0.17.0) — `validation_status_per_dimension` is now first-class. Cluster spec-graduated to `engine/consistency-check-spec.md`. Cluster instance log now canonical at `memory/cluster-instances.md` (count: 8, graduation status: spec). Promotion to mechanism deferred pending ≥3 validated detection rules.
- **Agent-as-instrument fails shadow-log tests (Lopopolo confirmation)**: 2 of 3 overdue assumption-tests closed 2026-05-12 had empty per-session logs despite the SessionStart counter incrementing to target — the agent was supposed to self-record per-session and didn't. Maps to anti-pattern #7 Level 1 (skipped step). Two harness-followup candidates surfaced: (1) relay-check hook (companion to v0.23.8's C1 verify_citations — captures relay behavior mechanically), (2) counter-mechanism refinement (require `kind: shadow_log` field before nagging; xai-check-evals was a static-fixture file that got swept up). New cluster `agent-as-instrument-on-shadow-logs` opened in cluster-instances.md (status: pending at 2 instances).
- **plugin.json sync must ride with CLAUDE.md Version-line bumps (atomic-commit rule)**: when bumping the Version line in CLAUDE.md, `plugins/mycelium/.claude-plugin/plugin.json#version` MUST be in the same commit. Otherwise Check 30 fails on push (drift); fixing in a follow-up commit then chains Check 26 (plugin.json is material → its change requires its own bump → infinite regress unless bundled). Observed three times in the 2026-05-22 session (0.23.31/32/33 sequence created the catch-22 at push-time). Rule now in `engine/version-discipline.md :: Coordinated commit`. Mechanism layer (Check 30 + Check 26) was already present; this graduation closes the agent-workflow gap that the validators kept catching at push-time.
- **Verify the test reads the framework before iterating on framework SKILL.md to fix test failures**: 2026-05-22→23 session burned ~6 hours iterating `/interview` SKILL.md (v0.23.39, v0.23.40, v0.23.41) trying to make the cold-start auto-dogfood scenario pass `decision_log_contains`. All three iterations had ZERO effect because the auto-dogfood orchestrator's `lib/prompts.py :: _interview_task()` is a HARDCODED prompt template that bypasses the SKILL.md entirely — the agent reads the prompt, never the SKILL.md. Pattern: when an agent isn't doing X during a test, first verify in isolation that the agent CAN do X. If yes, the issue isn't framework instruction-wording; trace upstream to whatever surface actually drives the test. Mechanism graduation: `mycelium-roadmap/.claude/auto-dogfood/scripts/check_skill_prompt_drift.py` now mechanically surfaces drift between framework SKILL.md write-paths and orchestrator task templates. Roadmap-private (Option C from REBUILD-PLAN §Phase 5). See also patterns.md "Isolated capability test before instruction iteration."

## Format

Each correction entry follows this structure:

```
### [DATE] - [SHORT TITLE]
- **Scope**: [discovery | delivery | orchestration | quality]
- **Category**: [bias | security | engineering | process | communication]
- **Origin**: [ai-generated | human-written | ai-assisted | unknown]
- **Mistake**: What went wrong.
- **Correction**: What should have happened instead.
- **Prevention**: How to prevent this in the future (checklist item, gate, etc.).
- **Source**: Theory or principle that applies (e.g., "Torres - continuous discovery", "OWASP - input validation").
```

**Origin field (APEX alignment)**: Track whether the mistake was in AI-generated, human-written, or AI-assisted code. Over time this reveals whether AI code has different quality patterns than human code. If AI-origin corrections dominate, it signals the AI needs better context (sharper canvas, updated corrections.md, more specific instructions). See APEX framework in `canvas/dora-metrics.yml`.

## Generalizable Corrections

_Corrections that apply broadly across projects and contexts._

### 2026-05-23 - Temporal-binarization variant of implicit-causal-link sub-class (recurrence within same conversation as the prevention's ship)
- **Scope**: discovery, agent self-application during the same /bvssh-check follow-up turn that shipped the `Gated by:` convention
- **Category**: bias (anti-pattern #7 — Consistency-as-Evidence, sub-class g implicit-causal-link, variant: temporal-binarization)
- **Origin**: ai-generated, user-detected ("I get provoked when you claim all work needs to be done post-surgery when we still got lots of time before the surgery")
- **Mistake**: In the same turn that shipped the `Gated by:` convention preventing implicit causal links between dates and unblock events, agent framed two Check N+1 candidate mechanisms as "post-surgery" / "Multi-day arc that conflicts with 'no new arcs after 05-25'" / "Pre-surgery, 4 days out." This conflated two distinct gates: (a) the evidence gate "2nd instance required" — controlling for mechanism graduation; (b) the capacity gate "reduced capacity 05-27 to ~05-30" — describing a 4-day shipping limit. Collapsed both into "post-surgery," implying surgery was the controlling constraint when the evidence gate was. The 4-day pre-surgery window (today Sat through Tue afternoon) is substantial working time; treating it as zero was the binarization.
- **Recurrence shape**: Identical implicit-causal-link sub-shape as the BVSSH deferral that triggered the convention's ship. The convention shipped this turn would have caught this paragraph if applied to the agent's own pushback. It wasn't. Same failure mode within ~5 minutes of the prevention's ship — analogue of the 2026-05-09 same-day-as-graduation recurrence (5 instances within hours of 0.21.0 graduation).
- **Correction**: Reframed the available work into single-session items that fit the 4-day pre-surgery window: porting the second missing cluster (`subagent-simulation-misses-lived-friction`), drafting the `Gated by:` enforcement script as un-wired stub, forward-inventory of existing framework files for deferral/threshold language without `Gated by:` clauses, spec extension for sub-class g. The inventory item is the highest-value because it generates evidence rather than consuming it.
- **Prevention strengthening (load-bearing for the convention's effectiveness)**: The `Gated by:` convention must be applied to ALL deferrals/thresholds in agent output, including those framing "what work can't ship now" pushbacks. Recovery-window framing for tasks IS a deferral statement and needs a gate clause. The agent's own meta-commentary about scope is not exempt from the convention. Suggested CLAUDE.md sharpening (NOT shipped this turn pending the 4-day inventory below, which will produce concrete evidence about pre-existing instance frequency and inform the right scope of sharpening): clarify that "applies to all recommendations" includes pushback statements declining proposed work.
- **Source**: User direct correction within the same turn. Fresh evidence the convention needs reinforcement at the agent's own scope-pushback surface, which the original ship treated as out-of-scope.

### 2026-05-23 - Implicit-causal-link sub-class of anti-pattern #7 surfaced; Pre-Ship #9 did not catch (Check N+1 candidate flagged)
- **Scope**: discovery, agent self-application during /bvssh-check on a Develop-phase L0 diamond
- **Category**: bias (anti-pattern #7 — Consistency-as-Evidence, NEW sub-class: implicit-causal-link, sub-class g in cluster-instances.md)
- **Origin**: ai-generated, user-detected via the follow-up question "what is the issue with [the deferred items]?"
- **Mistake**: BVSSH assessment recommended deferring three evidence-gated items (cohort triangulation, positioning refresh, opportunity reassessment) to a calendar date (≥05-31), framing them as capacity-deferred. All three actually unblock on evidence arrival, not date arrival. The "≥05-31" was *consistent with* the likely unblock window (post-surgery synthesis pass) without being the *cause* of unblock. The cause was cohort log arrival, which is dateless.
- **Why Pre-Ship #9 didn't catch**: The check (graduated 2026-05-04) labels each evidence piece by attribution type before publishing causal chains. It pattern-matches on explicit X→Y→Z chains and N=1 generalizations. The deferral never stated "all three items will unblock at the same time"; the conflation lived in the *implicit* causal link between "date X" and "unblock event Y." Surface plausibility passed because surgery and 05-31 are both real and individually defensible.
- **Correction (shipped this session)**:
  1. CLAUDE.md Communication Rules now require `Gated by: [event] — [interventional|observational]` clause on every deferral, threshold, or date-based recommendation. The clause makes the implied causal link inspectable in user-facing output rather than in a pre-publish check.
  2. cluster-instances.md `consistency-as-evidence` section ported from roadmap (generic-framed); sub-class g (implicit-causal-link) catalogued; pending sub-graduation noted.
  3. Anti-pattern #7 in `harness/anti-patterns.md` unchanged for now — convention is a complementary output mechanism, not a replacement for the check.
- **Prevention philosophy** (load-bearing, established 2026-05-09): This class is almost impossible to fully avoid (Grice maxim of quantity; Sperber & Wilson relevance theory). The mechanism class is "make the interpolation visible," not "stop interpolating." Adding more pre-publish checks of the same shape as #9 risks producing more cases where the rule is performed but the load-bearing inference still slips. Output convention is grep-detectable post-publish (a deferral without `Gated by:` fails the check); pre-publish checks of subtle inference aren't.
- **Check N+1 candidate (NOT graduated; logged for evidence-gated promotion)**:
  - **Mechanism**: validator-style script that greps for deferral/threshold language ("defer to", "ship at", "act when", "after [date]", "before [date]", "until [N]") in user-facing output and fails if the matching statement lacks a `Gated by:` clause within the surrounding context.
  - **Graduation criterion**: 2nd post-convention instance where the convention was skipped (within rolling 30-day window). Per cluster's graduation philosophy, single-instance graduation is gold-plating; the convention may simply work.
  - **Why not mechanize now**: Single instance of the sub-class. The convention shipped today is itself untested under load. Mechanizing pre-evidence violates the framework's own discipline (no leaf without evidence; AP#7 self-application — building structural mechanism from N=1).
- **Source**: User question "what is the issue with ht-016 triangulation, positioning refresh, opp-001 reassessment?" Direct user-detected. Same session: also surfaced documented-rule-diverges-from-enforcement instances 12 (BVSSH hook scoped framework-local only, blind to roadmap-side state) and 13 (canvas-state-vs-reality drift on cohort tester status); both catalogued in cluster-instances.md.

### 2026-05-23 - Framework `cluster-instances.md` was missing two clusters tracked in roadmap; structural-divergence detection candidate flagged
- **Scope**: orchestration, self-learning artifact maintenance
- **Category**: process (documented-rule-diverges-from-enforcement at meta level: two repos with the "same" memory artifact diverging structurally)
- **Origin**: ai-generated (discovered during deep dive on AP#7 cluster status)
- **Mistake**: Framework `cluster-instances.md` tracked 2 clusters (`documented-rule-diverges-from-enforcement`, `agent-as-instrument-on-shadow-logs`). Roadmap version tracked 4 (the same two plus `consistency-as-evidence` with 10 instances + 7 sub-classes catalogued, and `subagent-simulation-misses-lived-friction` with 2 instances). Both `/corrections-audit` and `/framework-health` read project-local `cluster-instances.md` only — they do not cross-read. Framework-side invocations under-reported cluster activity for AP#7 (0 cluster instances visible vs 8+ catalogued in roadmap). The gap existed because the roadmap-side entries include named-person attribution (per `collaborator_frida_attribution.md` auto-memory: generic-only in framework, named needs separate sign-off), so copy-paste was inappropriate and no generic-framing port was ever made.
- **Correction (shipped this session)**: `consistency-as-evidence` cluster ported to framework `cluster-instances.md` in generic-framed form (sub-class catalog + graduation status + philosophy + instance summary table; per-instance detail stays in roadmap-private). `subagent-simulation-misses-lived-friction` deliberately NOT ported in this batch — needs separate consideration (its two instances are largely framework-visible and could be ported, but the deep dive scope today was AP#7).
- **Check N+1 candidate (NOT graduated; logged for evidence-gated promotion)**:
  - **Mechanism**: script (sibling to `validate_canvas.py`) that compares cluster section headings between framework and roadmap `cluster-instances.md` files, flags structural divergence (missing clusters on either side, instance-count gap beyond threshold). Generic-framing the diff is not auto-doable; the script's role is surfacing for human review.
  - **Graduation criterion**: 2nd structural-divergence instance (another cluster either present in one repo and not the other, OR instance-count divergence indicating one-side-only logging). Per framework's pattern, ≥2 instances trigger graduation consideration; mechanism vs spec-only graduation TBD when the 2nd instance surfaces.
  - **Why not mechanize now**: Single structural-divergence instance catalogued. Pre-surgery (4 days out); cross-repo sync script is multi-day arc and violates auto-memory rule "no new arcs after 05-25." Mechanism may also not be the right shape — the divergence is partly intentional (named-attribution policy), so the script must distinguish "missing because not yet ported" from "missing because correctly excluded."
- **Source**: Deep-dive analysis 2026-05-23 of the framework's self-learning log status. User-requested.

### 2026-05-23 - Verify the test reads the framework before iterating on framework SKILL.md to fix test failures
- **Scope**: orchestration
- **Category**: process
- **Origin**: ai-generated (agent led three rounds of SKILL.md edits without first checking whether the test surface was reading the SKILL.md)
- **Detection_origin**: evaluator
- **Mistake**: 2026-05-22→23 auto-dogfood Phase 5 cycle. Onboarding scenario was failing `decision_log_contains` 0/3 systematic. Agent iterated framework `/interview` SKILL.md three times (v0.23.39 added `jobs-to-be-done.yml` stub + scale/phase clarity; v0.23.40 added Step 2 decision-log instruction; v0.23.41 hoisted decision-log to first-in-list + "(1 of 4)" mechanical numbering + "Hard requirement" framing). Each iteration produced a clean framework patch with proper changelog + atomic-commit discipline. NONE moved the test verdict because the auto-dogfood orchestrator's `lib/prompts.py :: _interview_task()` is a hardcoded prompt template that bypasses the SKILL.md entirely. The agent reads the orchestrator's prompt, never the SKILL.md, during orchestrator-driven runs. Three framework patches → zero effect.
- **Correction**: When an agent isn't producing artifact X during a test, FIRST verify in isolation whether the agent CAN produce X via direct `claude -p` in a manually-mimicked workdir. If yes (capability proven), the issue isn't instruction wording in the framework — trace UPSTREAM to whatever surface actually drives the test. In this case: trace from "scenario expects decision-log" → "where does the agent get its instructions from?" → orchestrator's `_interview_task()` prompt → hardcoded template missing decision-log → fix there.
- **Prevention**:
  - Pre-task discipline: before any framework-side SKILL.md edit intended to fix a test failure, ask: "is this edit's effect detectable by the test?" Answer requires knowing the test's data path.
  - Mechanism graduation: `mycelium-roadmap/.claude/auto-dogfood/scripts/check_skill_prompt_drift.py` surfaces drift between SKILL.md write-paths and orchestrator task templates. Runs as `python3 orchestrator.py drift-check` from the auto-dogfood dir. Roadmap-private (the auto-dogfood orchestrator is roadmap-private per the 2026-05-22 architectural decision). Same shape as framework Check 30 / Check 28 — mechanical enforcement of cross-surface invariants.
  - Sister pattern in `patterns.md`: "Isolated capability test before instruction iteration."
- **Source**: 2026-05-23 deep-dive after Phase 5 plateau. The user pushed back on the "plateau" conclusion and asked for a deep dive into the root cause. Direct test against `.claude/harness/decision-log.md` succeeded instantly; the agent was always capable; the orchestrator's prompt template was the actual gating surface. Theory: Hashimoto (engineer out recurrence); Gilad (evidence-guided — three iterations failing the same way IS evidence the wrong layer was being edited).

### 2026-05-22 - plugin.json sync must ride with CLAUDE.md Version-line bumps (atomic-commit rule)
- **Scope**: orchestration
- **Category**: process
- **Origin**: ai-generated (all three observed misses were in agent-authored commits in a single session)
- **Detection_origin**: evaluator
- **Mistake**: When bumping CLAUDE.md Version line during a PATCH-class change (decision-log + version line + changelog), `plugins/mycelium/.claude-plugin/plugin.json#version` was NOT staged in the same commit. Three rapid bumps in the 2026-05-22 session (0.23.31 ODI, 0.23.32 Friedman, 0.23.33 Comprehension-Debt) all missed the sync. Check 30 caught the drift on push. Initial fix-up commit (sync plugin.json alone) then chained Check 26 (plugin.json is a material framework file → changing it requires its own version bump → catch-22). Resolution required a plumbing-only bump at 0.23.34 that coordinated CLAUDE.md + plugin.json + changelog in a single commit.
- **Correction**: When bumping the Version line in CLAUDE.md, ALWAYS stage these three files together in a single commit: (1) `CLAUDE.md` (Version line), (2) `plugins/mycelium/.claude-plugin/plugin.json` (version field), (3) `docs/changelog.md` (new version section). For decision-log-bearing patch bumps, also stage (4) `.claude/harness/decision-log.md`.
- **Prevention**: Rule now documented in `plugins/mycelium/engine/version-discipline.md :: Coordinated commit — files that must move together`. Pre-Ship Protocol (G-P-pre) Step 7 (Manifest impact) already lists "new directories/files in manifest.yml" but did not surface the version-sync invariant. Add a check at commit-time: when `CLAUDE.md` Version line is staged, the diff also touches `plugin.json` and `changelog.md`, or abort. Mechanism layer (Check 30 + Check 26) was already present at push-time; this graduation closes the agent-workflow gap. Future agent invocations reading corrections.md should see the rule in TL;DR before composing the first version-bump commit of any session.
- **Source**: Three observed instances 2026-05-22 in the same session (0.23.31, 0.23.32, 0.23.33 all missed the sync). Resolution at 0.23.34 (plumbing) and graduation to corrections.md + version-discipline.md at 0.23.35 (this entry). Same pattern-shape as the "documented rule diverges from enforcement" 5th-instance graduation that gave version-discipline.md itself (referenced in that doc's "Why this exists" section). Theory: Hashimoto — engineer out recurrence; Gilad — evidence-guided graduation (validators caught it three times, agent-workflow caught it zero times → close the gap).

### 2026-05-20 - "You are here" wayfinding fires only at initial entry, not at phase transitions
- **Scope**: quality
- **Category**: ux
- **Origin**: user-detected (first non-developer user, Edith-Mari Pedersen Bartnes, book project test 2026-05-20)
- **Detection_origin**: user
- **Mistake**: The wayfinding-orientation mechanism added at an earlier framework stage covered initial flow orientation, but did not fire at later phase transitions. After completing the assumption test, the user was bewildered when the deep-dive interview was mentioned — she remembered being asked about it early in the flow, but no surface showed remaining steps or "you-are-here" status after the assumption test. Recurrence of the same orientation-loss shape we'd previously addressed; new stage where the prior solution didn't reach.
- **Correction**: Extend the "You are here" surface to fire at every phase transition, not just initial entry. After completing any user-facing milestone (brief, assumption-test, etc.), the agent should surface a brief "you are here, next is X, you can also Y" indicator before opening the next phase.
- **Prevention**: When adding any user-facing flow mechanism, ask: "does this need to fire at every transition, or just initial entry?" Default to every-transition unless the cost of repetition outweighs the orientation value. For non-developer users especially, orientation value is much higher than developers because they don't carry mental models of agent flows by default. The prior assumption that initial orientation was sufficient assumed a developer-style user who tracks flow state implicitly.
- **Source**: Edith-Mari Pedersen Bartnes book-project test 2026-05-20 (`docs/receipts/cases/2026-05-20-edith-mari-book-project.md`). First non-developer user; first concrete instance of the wayfinding gap at a non-initial transition. Theory: Hoskins (scenario-driven design) — when you don't know what scenario the user is in, default to surfacing the map. Cluster candidate: this is the second observed instance of orientation-loss after a partial earlier fix; if a third surfaces, graduate to a named cluster in cluster-instances.md.

### 2026-05-16 - Check 26 scope misread: receipts under docs/receipts/cases/ ARE material
- **Scope**: process
- **Category**: version-discipline
- **Origin**: agent-self (twice in one session, surfaced by pre-push hook)
- **Detection_origin**: hook
- **Mistake**: Twice on 2026-05-16, I committed framework-tree files without bumping the version, assuming Check 26 (material framework changes require a version bump) would not count those files. First trip: `.claude/harness/decision-log.md` + `.claude/memory/patterns.md` (commit `2ab089e`, fixed in `4e796e0` with PATCH bump to 0.23.24). Second trip: `docs/receipts/cases/2026-05-16-phase0-substrate-audit.md` (commit `23a648d`, required PATCH bump to 0.23.25). Both pushes were blocked by the pre-push hook running Check 26 — the validator was correct; my mental model of "material" was wrong.
- **Correction**: Bump the version pre-emptively when adding ANY file outside obvious exclusions (build artefacts, gitignored paths, `plugins/mycelium/` is the per-harness adapter and has its own discipline). Receipts under `docs/receipts/cases/` ARE counted as material. The validator is the authority on what counts as material, not my expectation of what *should* count.
- **Prevention**: When in doubt about whether a commit needs a version bump, the cheapest discipline is to bump pre-emptively. A wasted bump is a no-op; a missing bump is a push rejection plus an amend-style follow-up commit. The cost asymmetry favours bumping. Alternative (more rigorous): read `tests/validate-template.sh` Check 26 source to understand the exact file-exclusion logic before relying on memory of what counts. Did not do this either time; should have on the second.
- **Source**: Same-session double-trip 2026-05-16, pre-push hook output. Reflexion-loop triggered both times via `PostToolUseFailure` on the failed `git push`. The recurrence in a single session is the signal that the agent's mental model was structurally wrong, not just one-off forgetfulness — exactly the kind of pattern that warrants corrections.md graduation rather than a private note.

### 2026-05-14 - Generic-framed lived-friction sources can re-identify through the graduation chain
- **Scope**: quality
- **Category**: privacy
- **Origin**: user-detected (in-session, via "are we sure data won't leak into user projects?")
- **Detection_origin**: user
- **Mistake**: A 2026-05-09 cluster correction was generic-framed at point of capture (no identifying details about the observer). When the cluster graduated to anti-patterns.md the same day (instance #5 of anti-pattern #7), the graduation entry cited its triggers by date with a named participant attribution. The named-attribution leaked into `plugins/mycelium/harness/anti-patterns.md`, which ships to every downstream plugin user. The 2026-05-13 disclosure-and-ack on consent ("generic in framework OK, named attribution needs separate sign-off") came *after* the graduation, so this was not a knowing violation — but it has been shipping ever since. Same pattern surfaced a separate Simon attribution in `plugins/mycelium/harness/theory-tensions.md` (4 references, consent state unverified). Drew also appears but with explicit public consent.
- **Correction**: Added Check 33 to `tests/validate-template.sh` with a registry at `.claude/memory/attribution-registry.yml` (registry lives outside the plugin tree so it never ships). Each known individual carries a `consent: public_ok | generic_only | unknown` field; the check scans the public-visibility scope for `generic_only` and `unknown` names and reports leaks. WARN-only initially per the framework's observability-before-enforcement discipline; graduated to FAIL at v0.23.23 once pre-disclosure leaks were cleaned.
- **Prevention**: The deeper lesson is structural: **lived-friction attribution can leak through the graduation chain even when the source is generic-framed at point of capture.** Capture-time discipline is necessary but not sufficient — every step from corrections → anti-patterns → engine docs that cites its trigger sources is itself an attribution surface. Check 33 makes the leak detectable; the principle should also inform graduation-author behavior: when graduating from a generic-framed correction, the graduation entry must itself be generic-framed, not back-fill identity by quoting the trigger's filename or date in a way that re-couples to the person.
- **Source**: In-session 2026-05-14, surfaced by the user asking *"will that file interfere with whatever the user is doing in their project?"* during a Check-32 follow-up. Theory: GDPR data-minimization principle (collect only what's needed; carry forward only what's consented) applied to internal framework documentation, not just user data. The graduation chain is itself a data-flow surface.

### 2026-04-20 - L5 market scale produces sycophantic decision log entries
- **Scope**: quality
- **Category**: bias
- **Origin**: ai-generated
- **Detection_origin**: agent_self
- **Mistake**: At L5 Market scale, the agent wrote overly optimistic phrases ("mostly positive", "strong validation", "confirms product-market fit") in the decision log. The go-to-market context primes promotional framing that bleeds into internal records.
- **Correction**: Decision log language must remain evidence-specific and hedged. "3 of 5 users mentioned X" not "strong validation from users."
- **Prevention**: Added guardrail G-M1 to `guardrails-market.md`. The `decision_log_honest` evaluator criterion catches forbidden phrases. Agent should use specific counts and hedged language at all scales, but especially L5.
- **Source**: Kahneman (optimism bias), Shotton (social proof bias). Detected by dogfood scenario `content-solo-l5-market`.

### 2026-04-20 - 27 of 44 skills had no decision log instruction
- **Scope**: process
- **Category**: process
- **Origin**: ai-generated
- **Detection_origin**: agent_self
- **Mistake**: Core guardrail G-P4 says "always log decisions" but 27 skills never mentioned the decision log. The agent sometimes skipped writing it, requiring evaluator retries (observed in 3 of 18 dogfood scenarios: `service-team-multiscale-l2-l4`, `saas-solo-l1-strategy`, `content-solo-l5-market`).
- **Correction**: Strengthened G-P4 with explicit skill list. Added `## Decision Log (MANDATORY per G-P4)` section to 10 high-impact skills (`team-shape`, `wardley-map`, `cynefin-classify`, `launch-tier`, `service-check`, `bias-check`, `privacy-check`, `security-review`, `bvssh-check`). Expanded evaluator `require_after` set from 2 to 14 skills.
- **Prevention**: When creating new skills, always include a decision log section. G-P4 now lists skills explicitly rather than relying on "significant decisions."
- **Source**: Dogfood finding F2. Argyris (double-loop learning — the instruction was present but not specific enough to change behavior).



### 2026-04-28 - upgrade.sh harness sync was hardcoded, not manifest-driven
- **Scope**: delivery
- **Category**: engineering
- **Origin**: ai-generated
- **Detection_origin**: eval_runner
- **Mistake**: upgrade.sh hardcoded 6 harness filenames for sync. When new harness files were added (guardrails-core.md, guardrails-delivery.md, guardrails-discovery.md, guardrails-index.md, guardrails-market.md, README.md), they were never added to the hardcoded list or the manifest. The `tests/` and `auto-dogfood/` directories were also in the manifest but missing from the directory loop. Result: downstream projects silently drifted from upstream over upgrades.
- **Correction**: (1) Replaced hardcoded harness list with glob + explicit preserve-list. (2) Added missing directories to framework replace loop. (3) Added README sync step for preserved directories. (4) Updated manifest.yml.
- **Prevention**: When adding new framework files, verify upgrade.sh coverage. The harness glob pattern now handles new files automatically — no list to maintain. For new preserved-directory READMEs, add to manifest `preserved_dir_readmes`.
- **Source**: DRY (hardcoded lists diverge from reality). Forsgren (change failure rate — silent drift is a deployment risk).

### 2026-04-30 - Interview ceremony too long for time-constrained users
- **Scope**: discovery
- **Category**: process
- **Origin**: ai-generated
- **Detection_origin**: external_review
- **Mistake**: The /interview skill consumed Drew Hoskins's entire first session (hit rate limit) before any delivery work started. All phases (Purpose → Users → North Star → Constraints → Classification → Canvas writing) ran sequentially. Drew left with populated YAML files but zero prototype work, despite a 48-hour deadline.
- **Correction**: /interview needs a sprint-mode detection path. When time constraint < 48h, cut to minimum viable interview (3-5 questions covering purpose, primary user, and core assumption). Defer Phase 5b/5c/6 entirely. Canvas writing should be parallelized aggressively.
- **Prevention**: Add a time-constraint question early in /interview ("How much time do you have for this project?"). If < 48h, switch to sprint interview path. If < 8h, consider skipping /interview entirely and going straight to delivery with inline discovery.
- **Source**: Hoskins friction log (2026-04-25). Horthy (instruction budget overflow — ceremony overflows the session budget). Cagan (build to learn — the interview IS the learning, but not if it prevents all building).

### 2026-04-30 - Mycelium process abandoned after /interview (process cliff)
- **Scope**: orchestration
- **Category**: process
- **Origin**: ai-generated
- **Detection_origin**: external_review
- **Mistake**: After the /interview skill completed, the agent dropped all Mycelium structure for the remaining 75% of the session (pages 7-17). No diamonds created, no canvas updated, no theory gates checked, no /diamond-progress run. The agent became a raw implementation co-pilot. The framework's value proposition disappeared after onboarding.
- **Correction**: The discovery-to-delivery transition needs a lightweight continuation mode. After /interview populates canvas, the agent should create an L3 diamond and use lightweight inline gates rather than requiring full skill invocations. The process should be present but not heavy.
- **Prevention**: After /interview completes, the agent should (1) create a diamond, (2) state the next Mycelium checkpoint plainly ("I'll check Downe's service principles before we call this done"), (3) run gates inline rather than as separate skill invocations. The goal: the user shouldn't notice the process, but the process should still run.
- **Source**: Hoskins transcript (2026-04-25). Böckeler (harness engineering — inferential guidance that's too heavy gets ignored). Smart (BVSSH Sooner — process overhead that slows delivery without adding value is waste).

### 2026-04-30 - Agent over-scoped before learning constraints
- **Scope**: delivery
- **Category**: process
- **Origin**: ai-generated
- **Detection_origin**: external_review
- **Mistake**: Agent proposed a 20-hour, full-weekend build plan before learning Drew had only 8 user-hours. Drew had to cut scope himself. The agent also recommended MCP over skill+SQL — Drew overrode with a sharper read of what the demo needed.
- **Correction**: Ask time/resource constraints before proposing scope. When the user says "let's build X," the first response should include "What's your time budget?" — not a 20-hour plan.
- **Prevention**: Add constraint discovery to the top of any delivery planning: time budget, resource constraints, demo vs. production, audience. This maps to the new G-V11 success criteria requirement — criteria include what's achievable within the constraint.
- **Source**: Hoskins transcript (2026-04-25). Goldratt (Theory of Constraints — identify the constraint before optimizing). Patton (build to learn — scope to the learning, not the vision).

### 2026-05-08 - documented-rule-diverges-from-enforcement cluster spec-graduated (8 instances, version 0.17.0)
- **Scope**: quality
- **Category**: process
- **Origin**: ai-generated
- **Detection_origin**: evaluator
- **Mistake**: The cluster reached Check 26's stated graduation criterion ("graduate at instance 6") several days ago but the graduation hadn't happened. Today's honest recount (instances 5-8 reviewed; previously instances 6 and 7 had been fixed individually without being counted toward graduation) put the cluster at 8 instances. Continuing to fix instances individually without graduating violated the framework's own stated promise — itself an instance of the cluster (a documented graduation rule diverging from how graduation actually happens). Recursive.
- **Correction**: Spec-graduated the cluster via three coordinated artifacts: (1) `engine/consistency-check-spec.md` defines what consistency-checking means, catalogs the 8 instances by subclass, articulates 5 detection-rule candidates, sets a promotion-bar (≥3 rules validated, <5% FP, 100% TP, integrated into a check); (2) `memory/cluster-instances.md` makes the cluster log canonical so instance count is mechanically auditable rather than oral history; (3) `/corrections-audit` and `/framework-health` SKILL.md updates wire the cluster log into routine audits and graduation-readiness flagging. JTBD schema gap (instance 8) closed in same commit (`validation_status_per_dimension` first-class field, backward compatible). Version bump 0.16.5 → 0.17.0 MINOR (new convention with structural impact).
- **Prevention**: With cluster-instances.md as the canonical counter and the spec's promotion bar mechanically checkable via /framework-health, future cluster instances accumulate against an explicit threshold. The recursive bug ("can't count own clusters") is closed. The choice to spec-graduate rather than ship a half-baked detection mechanism is the framework's own L3-before-L4 discipline applied to itself: don't build the mechanism without the articulated spec underneath.
- **Source**: 2026-05-08 deep-dive after L1 Discover JTBD work surfaced instance 8. Theory: Check 26 + Argyris (double-loop → triple-loop reasoning), Senge (structural lever for recurring patterns), Lopopolo (un-detected divergences = harness-context-debt). Sister findings carrying forward as candidates for the future mechanism's coverage corpus.

### 2026-05-07 - JTBD schema lacks per-dimension backing fields — 6th instance of "documented rule diverges from enforcement"
- **Scope**: quality
- **Category**: engineering
- **Origin**: ai-generated
- **Detection_origin**: evaluator
- **Mistake**: Christensen's JTBD discipline (taught in `/jtbd-map` SKILL.md, in `theory-gates.md` L1 trio guidance, and gated at L1 Discover→Define) explicitly requires functional + emotional + social dimensions to each have backing. The `jobs-to-be-done.schema.json` provenance shape only exposes aggregate `confidence` — a single 0.0-1.0 number per job. There is no schema-native way to record that one dimension is well-backed and another is hypothesis-shaped, even though the framework's own theory text says they should be assessed separately. Surfaced 2026-05-07 during L1 Discover JTBD work: writing job-002 (cautious-learner segment) honestly required a per-dimension audit (functional ~0.4 observed-reaction-backed, emotional ~0.3 inferred, social ~0.15 founder-articulated). Putting that breakdown inside provenance was rejected by schema (additionalProperties: false on provenance — same shape as the 0.16.2 source_class footgun). Worked around by placing the validation_status_per_dimension block at job-entry level (additionalProperties: true).
- **Correction**: Two paths, not yet decided:
  1. Schema-side: add `validation_status_per_dimension` (or per-dimension confidence/backing) as a first-class shape inside the `jobs` entry schema in `jobs-to-be-done.schema.json`, with optional sub-fields (status, backing, gap) per Christensen dimension. Migration: existing job-001-shaped entries continue to validate (the field is optional); job entries that DO use it get tracked staging.
  2. Theory-text side: amend `theory-gates.md` L1 trio guidance to acknowledge that current schema only tracks aggregate confidence and per-dimension assessment lives in the entry body until the schema gap closes. Less satisfying — accepts the divergence.
- **Prevention**: This is the **6th instance of the "documented rule diverges from enforcement" cluster** Check 26 graduated at instance 5. Per the 0.16.0 Check 26 commit message: "If a 6th instance of any subclass surfaces, the next graduation step is a unified 'framework changes ship with their own consistency checks' guardrail at REVIEW tier." This is that 6th instance. The graduation isn't automatic — it deserves an explicit decision because the consistency-check guardrail is a non-trivial harness change. Logging as candidate, not auto-graduated.
- **Source**: Self-detected during 2026-05-07 L1 Discover JTBD mapping. Theory: Christensen (functional/emotional/social tripartite); DRY (one canonical concept across schema layers); POLA (Saltzer & Schroeder — what the rest of the framework teaches should also be expressible in narrower scopes). Sister findings in this cluster: 0.16.2 source_class singular vs plural (instance 5 of the cluster, fixed via schema accept-both); 0.16.3 Canvas Write Read-before-Write (different cluster — tool-quirk, not schema-vs-discipline); Check 26 (instance 5 graduation, version-bump enforcement). Six-instance threshold reached — graduation decision pending.

### 2026-05-07 - `/diamond-progress` is phase-transition-shaped, treats child-spawn as side-effect
- **Scope**: orchestration
- **Category**: process
- **Origin**: ai-generated
- **Detection_origin**: agent_self
- **Mistake**: Spawning L1 Strategy from L0 Develop on 2026-05-07 surfaced that `/diamond-progress` SKILL.md is structured around phase transitions (step 1: "From [current phase] to [next phase] at [scale]"). Child-diamond spawning is mentioned only as a single-line bullet under step 9 ("If progressing: ... Identify if child diamonds should be spawned"). The full apparatus (cognitive forcing, perspective conflict check, gate evaluation, confidence threshold, counter-argument, mutation) is phase-transition-coded. When the actual operation is "L0 stays in Develop, spawn L1 in Discover," the agent has to interpret-around-the-skill-text rather than follow it. Several specifically-spawn-shaped checks aren't run because they're not specified: (a) is the parent's effective threshold met for spawn (not for parent completion)? (b) what initial confidence should the child have, and is there a rule connecting it to parent confidence? (c) which canvas files transfer/inherit vs. which are owned by the child scale? (d) how does the trio perspective coverage carry forward from parent to child? In dogfood mode the agent improvised reasonable answers; in less self-documenting projects this would create silent variance across spawn operations.
- **Correction**: Add a Step 1.5 "Identify operation type" to `/diamond-progress` SKILL.md that branches on transition (existing flow) vs. spawn (new flow) vs. pivot/park/kill (existing subcommand flow). Spawn flow needs: (1) perspective-conflict check on PARENT's canvases (already correct as-is), (2) explicit parent-readiness gate ("is parent at-or-above effective threshold AND are there strategic events warranting a child?" — see patterns.md "Spawn child diamonds at strategic-events density"), (3) child-initialization rules (which gates go pending per scale, what initial confidence to use, which canvas files to reference), (4) dual-state mutation (update parent's last_progressed + notes + spawned-child reference; create child entry with parent_id), (5) updated wayfinding showing both parent and child active.
- **Prevention**: Skills that handle multiple operation shapes should branch on operation-type at step 1, not bury alternate operations as bullets under "if progressing." Anti-pattern shape: skill text optimized for the most-common case treats the rarer-but-equally-real cases as exceptional rather than first-class. If a third instance of "skill structured around dominant case, alternate cases handled implicitly" surfaces, graduate to a meta-rule: skills with multiple operation modes must declare them explicitly at step 1.
- **Source**: Self-detected during 2026-05-07 L1 spawn in dogfood. Theory: Norman (visible affordances — alternate operations need visible structure, not implicit interpretation). Lopopolo (every interaction is a failure of the harness to provide enough context — when the agent has to infer the workflow from skill structure, the harness has under-served it).

### 2026-05-12 - Shadow-log assumption-tests had empty per-session instruments (RECURRING, 2 instances; anti-pattern #7 Level 1 — skipped step)
- **Scope**: discovery
- **Category**: process
- **Origin**: ai-generated
- **Detection_origin**: agent_self
- **Mistake**: Three assumption-tests opened 2026-05-02 (`relay-norms`) and 2026-05-04 (`xai-inline-attribution`, `xai-check-evals`) were flagged as overdue by the SessionStart counter (15/10, 11/10, 10/10 respectively) and closed today in the dogfood repo. On reading the docs, two of three had **empty per-session log tables** — the shadow-log instrument design required the agent to fill in per-session rows at session end, and the agent didn't. The third (`xai-check-evals`) wasn't even session-based — a static coverage-fixture set got counter-misclassified because its `.count.json` lacked discriminating fields. The pre-committed success criteria (≥80% relay rate over 10 sessions; ≥80% citation rate + ≥4/5 faithfulness audit) could not be evaluated against absent data. Behavioral retrospection from session memory was the closest the closure could come, and that path is exactly anti-pattern #7 (constructing claims from consistency rather than verified attribution). The relay-norms doc had explicitly named this risk in its own Codification-venue section (Lopopolo reframe, 2026-05-02): "if a norm has a clean mechanical trigger, prefer harness encoding — it survives agent-norm drift." The shadow-log pattern itself proved the point.
- **Correction**: Closed all three tests with INCONCLUSIVE / INSTRUMENT-FAILED status. Set `closed: true` in each `.count.json` to silence reminders. Did NOT fabricate result data from session memory. Two graduation candidates logged:
  1. **Relay-check hook** — companion mechanism to v0.23.8's C1 (`read-log.sh` + `verify_citations.py`). C1 captures Read-tool behavior mechanically; the relay-check equivalent would capture whether the agent's first user-facing turn references SessionStart-surfaced signals when reminders are non-empty. Stop-hook or PostToolUse-on-user-message detection. Clean mechanical trigger.
  2. **Counter-mechanism discriminator** — the SessionStart counter incremented `sessions` on `xai-check-evals.count.json` despite that file being a static coverage-fixture set, not a shadow log. Fix: require `kind: shadow_log` or `target` field on the JSON before counter-nagging, or detect that `scenarios_run`/similar already-existing-field shows the test is non-session-based.
- **Prevention**: Habit-formation tests under the assumption-test skill should either (a) ship with a mechanical capture hook (the C1 pattern is the worked example), or (b) accept qualitative-only success criteria and stop quantifying ratios that require absent instrument data. Future shadow-log designs MUST name the recording mechanism — if "the agent records at session end" is the recording mechanism, that's a design smell, not a test. Closure path for INCONCLUSIVE / INSTRUMENT-FAILED tests should be a standard outcome with its own template, not a stretched "Result" section.
- **Source**: Detected 2026-05-12 when SessionStart hooks flagged all three tests as overdue and closure surfaced the shared failure shape. Theory: Lopopolo ("every interaction with the agent is a failure of the harness to provide enough context") — the shadow-log pattern asks the agent to be the harness, and the agent isn't. Anti-pattern #7 Level 1 (skipped step) is the canonical name for the shape. Sister case to the 2026-05-09 Wardley-map false-premise instance: rule known, not self-applied.

### 2026-05-09 - Canvas-writing skills lack explicit Read-target-first step; agent loses ~14k tokens to tool-surface ceremony (RECURRING, anti-pattern #7 instance #5 in one session)
- **Scope**: orchestration
- **Category**: process
- **Origin**: ai-generated
- **Detection_origin**: evaluator
- **Mistake**: While running `/mycelium:user-needs-map`, the agent reached for `Write` on `user-needs.yml` without first using the `Read` tool on it. The agent had earlier run `head -30` on the file via Bash and treated that as having "seen" the file. Claude Code's Write tool requires a prior `Read`-tool invocation in the same session; `cat`/`head` via Bash do not satisfy this check (already documented in CLAUDE.md "Canvas writes — Read before Write"). The Write failed; the agent then issued a real `Read`, then re-issued `Write` with identical content. Net cost: ~14.6k tokens of the second pass were pure tool-surface ceremony, purchasing nothing.
- **Compounding observation**: This is the **5th instance of anti-pattern #7 (Consistency-as-Evidence) in one session, on the same day the anti-pattern shipped to documentation**. Instance #4 (the Wardley-map-undone false premise) was acknowledged in the prior corrections entry less than an hour before. The "knows the rule, doesn't self-apply" pattern is now extremely well-evidenced — TWO instances within the same hour, after the documentation graduation, after self-acknowledgment of the prior instance.
- **Correction**: Agent acknowledged the friction; user flagged it directly. No content change to user-needs.yml needed (the second Write succeeded with identical content).
- **Prevention** (three structural fixes; documentation-only graduation has now demonstrably failed twice):
  1. **F1 — Every canvas-writing SKILL.md gets an explicit "Step 0: Read the target canvas file(s) with the Read tool" step.** Mechanical edit across ~10 skills (interview, canvas-update, log-evidence, jtbd-map, wardley-map, ost-builder, gist-plan, dora-check, metrics-pull, user-needs-map). The CLAUDE.md generic rule is not enforced because the per-skill workflows don't reinforce it. Skill-level enforcement is the structural layer.
  2. **F2 — Validator Check 31 (proposed in instance-#4 corrections entry, strengthened by this instance)**: scan SKILL.md files that issue `Write` instructions against `.claude/canvas/*.yml` paths and require a `Read` instruction earlier in the same workflow. WARN-level initially.
  3. **F3 — Skill text should distinguish `head`/`grep`/`cat` via Bash from the `Read` tool explicitly** in the canvas-write context. The agent conflated them; that's evidence the skill text isn't sharp enough.
- **Cost framing**: At Pro-tier rate-limit scale (per a cohort-log thread in auto-memory — attribution to Mycelium-specific use unconfirmed), ~14k wasted tokens per canvas-writing skill is the kind of cost that turns adoption into abandonment. This is not just a discipline question; it's a token-economy question. F1 has measurable user-facing cost reduction.
- **Source**: User-flagged 2026-05-09. Theory: same as instance #4 (Argyris double-loop, Buçinca cognitive forcing, Pearl causal inference). Same-day TWO-instance recurrence is the bar for "structural enforcement is required, not optional."
- **Promotion candidate**: Combine instance #4 (Read-before-claim in diamond-assess) and instance #5 (Read-target-canvas in writing skills) into a single behavioral mechanism: **pre-claim/pre-write Read enforcement at skill-step level, validated by Check 31**. Ship as 0.22.0 (MINOR — new mechanism on existing skill surfaces).

### 2026-05-09 - Anti-pattern #7 self-application failed same day as graduation (RECURRING, instance #4)
- **Scope**: orchestration
- **Category**: bias
- **Origin**: ai-generated
- **Detection_origin**: evaluator
- **Mistake**: Hours after graduating anti-pattern #7 *Consistency-as-Evidence* and shipping it in `harness/anti-patterns.md` + `/devils-advocate` Technique 4 + `/corrections-audit` Step 6d + G-P-pre item 9, the agent ran `/mycelium:diamond-assess` on the L1 Strategy diamond and recommended "build the Wardley map now — it's a marked-known gap." The recommendation rested on reading the L1 spawn note's phrase "needs explicit evolution-stage mapping" as evidence the map was undone, *without opening landscape.yml to verify*. The map was in fact substantially complete (9 components with provenance, 4 movements, 2 gameplay options, 2 out_of_scope boundaries, populated strategic_frame block authored 2026-05-07). The same failure shape the framework had just named: rhetorical residue treated as evidence of state, no attribution check, recommendation built on the false premise. Auto-memory note (cross-session) had already flagged this on 2026-05-09 morning: "agent has constructed causal chains from consistency-only evidence ≥3 times — knows the rule, doesn't self-apply." This incident is instance #4, occurring the same day as graduation.
- **Correction**: Agent self-caught when invoked /mycelium:wardley-map and read landscape.yml prior to editing. Acknowledged as anti-pattern #7 self-application failure. Diamond-assess error documented; partial Wardley refresh kept (the edits were accurate, only the recommendation was wrong); pivoted to /mycelium:user-needs-map (the genuinely missing evidence bucket).
- **Prevention**: Documentation-only graduation does not produce behavioral self-application. The graduation closes the *teaching* loop; it does not close the *enforcement* loop. Three follow-ups needed:
  1. **Pre-recommendation hook in /mycelium:diamond-assess Step 4 (gate evaluation)**: before claiming a required-evidence bucket is missing or partial, the skill must Read the canvas file that bucket maps to. Today the skill recommends skills (e.g., "Wardley Map | Missing | /mycelium:wardley-map") on the basis of theory-gates.md + spawn-note text without checking the canvas. This is the structural enforcement that makes self-application happen at the gate, not after the recommendation has been given.
  2. **Validator Check 31 (proposed)**: scan SKILL.md content for assertion patterns ("X is missing", "Y has not been done", "Z needs to be built") that name a canvas/yaml file without a corresponding Read instruction in the same skill step. Heuristic-level WARN; full enforcement is human review.
  3. **/devils-advocate Technique 4 (attribution-vs-consistency) ambient triggering**: the technique exists (graduated this morning) but is not yet ambient-active inside /mycelium:diamond-assess. The skill should inline the attribution-vs-consistency check before publishing recommendations.
- **Source**: Detected 2026-05-09 by user reaction to wardley-map invocation revealing the existing strategic_frame block. Theory: Argyris double-loop (single-loop = "remember to apply the rule"; double-loop = "build the rule into the gate that triggers behavior"). Buçinca/Malaya/Gajos cognitive forcing (forcing the human's pre-assessment first did surface the right read — "scope is unclear" pointed at the right gap; the agent's gates missed what the human caught). Pearl causal inference (the agent treated observational consistency between spawn-note text and "the map is undone" as causal evidence; never tested by reading the file). Same theory grounding as the anti-pattern itself.
- **Recurrence flag**: This is the **4th instance on this project** of the same root cause (2026-04-30 over-scoping, 2026-05-03 sharper-framing anchoring, 2026-05-09 cohort-log verbosity-attribution gap, 2026-05-09 Wardley-map-undone false premise). Three were corrected by the user. Anti-pattern #7 graduation closed the documentation gap. The 4th instance, occurring same day, demonstrates that structural enforcement is the right next layer. Promotion candidate to behavioral mechanism (skill-level Read-before-claim hook) on next graduation cycle.

### 2026-05-09 - plugin.json version drift on 0.21.0 ship (coupled-field sync gap)
- **Scope**: orchestration
- **Category**: process
- **Origin**: ai-generated
- **Detection_origin**: evaluator
- **Mistake**: Shipped 0.21.0 (three anti-pattern graduations) by bumping CLAUDE.md's leading `*Version` line, updating receipts, and writing a full changelog entry — but forgot to bump `plugins/mycelium/.claude-plugin/plugin.json` from 0.20.11 to 0.21.0. Same coupled-field drift class as the 0.20.x dogfood B2 finding ("plugin.json version stayed pinned at 0.20.0 while CLAUDE.md moved through 10 patches"), but at MINOR-bump rather than PATCH-bump scale. Caught only when the user asked "Did you also update this repo to 0.21.0?" — the agent had reported "shipped" while the user-visible plugin manifest still said 0.20.11. Check 26 watches plugin.json as a *material path* (would have flagged a bare CLAUDE.md edit as needing a bump), but doesn't enforce *coupled-field equality* between the two files. The drift was structurally undetectable by the existing validator.
- **Correction**: Bumped plugin.json to 0.21.1 (tracks CLAUDE.md exactly). Added Validator **Check 30** (`plugin.json#version tracks CLAUDE.md Version line`) that parses the leading `*Version X.Y.Z` from CLAUDE.md and the `version` field from plugin.json, fails on drift. Shipped as 0.21.1 hygiene patch.
- **Prevention**: Coupled-field invariants need field-level enforcement, not just path-level watching. When a value must be equal across two files, the validator must compare the values — checking that "both files were edited recently" doesn't catch the case where one was bumped and the other was edited for unrelated reasons. Future coupled-field invariants (skill-count in CLAUDE.md vs auto-discovered count, gate-count claims, theory-count claims) should ship with their own field-equality checks. If a third coupled-field drift surfaces, graduate to a meta-pattern under anti-pattern #8 (Stale State Read) extension or its own anti-pattern.
- **Source**: Detected by user 2026-05-09 immediately post-0.21.0 ship. Theory: Argyris double-loop (the validator now enforces what the agent forgot). Same source-class as B2 from the 0.20.x dogfood (Bartnes self-test) — recurrence at higher version-bump scale confirms the failure mode generalizes; harness response is field-level enforcement rather than discipline.

### 2026-05-09 - Framework-guard helper went missing without canary
- **Scope**: orchestration
- **Category**: process
- **Origin**: ai-generated
- **Detection_origin**: evaluator
- **Mistake**: During the 0.21.1 ship, every Edit/Write/Bash call against the upstream Mycelium repo was denied because `mycelium-roadmap/.claude/scripts/framework_guard.py` (and `_manifest_lib.py`) were absent — likely casualties of an earlier upstream sync that dropped the legacy `.claude/scripts/` tree without replacing it. The hook's fail-closed behavior is correct (better to deny than silently allow), but neither session start nor the reflexion hook nor any validator check verified that the guard's own helpers exist. The agent only discovered the helpers were missing when the hook fired and produced the "Restore from upstream OR delete .claude/state/upstream.json" message — and then couldn't fix it from inside the agent (the Write to restore the helper goes through the same hook, which still denies). User had to run `mkdir -p .claude/scripts && cp ...` from the shell to unblock.
- **Correction**: User restored helpers manually. No code change shipped for this incident yet.
- **Prevention**: Add a session-start check (or `/framework-health` step) that, when `.claude/state/upstream.json` exists and `active: true`, verifies `.claude/scripts/framework_guard.py` and `.claude/scripts/_manifest_lib.py` exist. If missing, surface a one-line diagnostic at session start ("framework-guard active but helpers missing — restore via `bash .claude/scripts/upgrade.sh` or `cp` from upstream") so the user fixes it before the first Edit/Write attempt blocks. Alternatively, ship the framework_guard helpers as part of the plugin tree (so `${CLAUDE_PLUGIN_ROOT}/scripts/framework_guard.py` is the canonical location and the project-local copy is optional/deprecated). Plugin-tree placement also closes the legacy-cleanup loose end where dogfood instances still need framework helpers in `.claude/scripts/` even though the canonical layout has moved.
- **Source**: Detected 2026-05-09 during 0.21.1 ship. Theory: Lopopolo (every interaction is a failure of the harness to provide enough context — silent absence of guard helpers is exactly that class). Defense-in-depth: every fail-closed gate should have its own readiness canary.

### 2026-05-06 - `/diamond-progress` silently interprets re-invocation as approval
- **Scope**: orchestration
- **Category**: communication
- **Origin**: ai-generated
- **Detection_origin**: agent_self
- **Mistake**: During Juniors.dev pre-run dogfood, the user ran `/diamond-progress` to evaluate L0 Discover→Define. Gates passed-with-caveats; the agent paused and asked for explicit human approval before mutating state ("Pending your explicit approval: progress L0 Purpose from Discover → Define?"). The user re-invoked `/diamond-progress` rather than typing "yes." The agent interpreted the re-invocation as approval and advanced the diamond, rendering `"Reading your re-invocation as approval — executing the progression now."` on screen. The user expected a fresh gate evaluation. The behavior is reasonable as a UX shortcut but is documented nowhere in `skills/diamond-progress/SKILL.md` — neither in the workflow steps, the human-approval section, nor a "shortcuts and conventions" subsection. Implicit harness behavior is a footgun: in this case it cost the user one round of confused re-running before the next `/diamond-progress` invocation finally produced the Define→Develop refusal moment.
- **Correction**: At minimum, document the convention in `skills/diamond-progress/SKILL.md` step 4 (human approval): "If the user re-invokes `/diamond-progress` while a previous invocation is awaiting approval, that re-invocation is treated as approval. To force a fresh evaluation instead, the user should type `evaluate again` or modify state and then re-invoke." Better: add a line to the agent's pending-approval prompt that names the convention explicitly ("Reply 'yes' to advance, 'no' to stay, or re-invoke `/diamond-progress` to approve. Type 'evaluate again' to re-run gates."). The mechanism is fine; surfacing it removes the footgun.
- **Prevention**: Audit other skills with deferred-action patterns for similar implicit conventions. `/diamond-progress pivot|park|kill` subcommands and `/preflight` are the most likely candidates. If a second instance of "implicit interaction convention" surfaces, graduate to a meta-rule: skills with multi-turn workflows must document interaction conventions in the prompt that asks for them, not in the skill text.
- **Source**: Detected during 2026-05-07 Juniors.dev presentation pre-run. Theory: Norman (Design of Everyday Things — affordances must be visible, not implicit). Lopopolo (every interaction is a failure of the harness to provide enough context).

### 2026-05-06 - Wayfinding map: agent improvised layout against template
- **Scope**: discovery
- **Category**: communication
- **Origin**: ai-generated
- **Detection_origin**: agent_self
- **Mistake**: After `/interview` populated the canvas, the agent rendered the post-interview wayfinding map as a vertical tree (`L0 Purpose ●──┐ │ L1 Strategy ○─┤ │ ...`) with title `You Are Here — Wayfinding Map`, `●` symbol, no phase progression, and inline confidence on the active line (`← YOU ARE HERE (Discover, confidence 0.3, anecdotal)`). The wayfinding.md spec called for a different layout: title `YOUR JOURNEY`, horizontal phase progression (`Discover ✓ → Define → Develop → Deliver`), `◆` for active, plain-language description per scale, footer with confidence + next-action. Root cause: wayfinding.md described the rendering rules and showed a template, but didn't forbid alternative layouts. The agent had latitude to "interpret" and used it.
- **Correction**: Tightened `engine/wayfinding.md` with a "STRICT — reproduce the template literally" header at the top of "How to Render," plus a list of common improvisations to forbid (vertical box-drawing, `●` instead of `◆`, inline confidence, missing descriptions, alternate titles). Same template, but no room to redesign.
- **Prevention**: When a doc is descriptive but the agent has visual latitude, expect it to improvise. For rendering specs, lead with "reproduce verbatim, do not redesign" and enumerate common deviations. If this recurs (≥3 instances of layout-improvisation across other rendering docs), graduate to a meta-pattern: "rendering specs must be prescriptive, not descriptive."
- **Source**: Detected during 2026-05-07 Juniors.dev presentation pre-run dogfood. Theory: NNGroup (You-Are-Here pattern depends on consistency for orientation; improvised layouts defeat the purpose). Lopopolo (the harness must remove latitude where consistency matters).

### 2026-05-06 - Canvas Write fails on fresh projects — read-before-write tool quirk
- **Scope**: orchestration
- **Category**: process
- **Origin**: ai-generated
- **Detection_origin**: agent_self
- **Mistake**: During Juniors.dev presentation pre-run, a fresh `npx degit haabe/mycelium test-demo1` followed by `/interview` failed at the canvas-write step with "Error writing file" on `purpose.yml` and `jobs-to-be-done.yml`. Reproduced reliably on a second fresh project. Root cause: canvas files ship pre-populated as templates (header comments + placeholder fields), so every `.claude/canvas/*.yml` already exists on a fresh project. Claude Code's `Write` tool requires a prior `Read` (same tool, same session) on existing files; `cat` via Bash does NOT satisfy the check. The agent had `cat`-ed the file during interview synthesis but never invoked the Read tool, so Write was rejected. No skill in the framework instructed the agent to use Read before Write on canvas files — a structural assumption baked into Mycelium's tooling that wasn't surfaced.
- **Correction**: Added a "Canvas writes — Read before Write" paragraph to `CLAUDE.md` under "The Canvas (Source of Truth)." Spells out the tool-quirk explicitly, names the affected skills (every canvas-writing skill), and tells the agent to use the Read tool before Write. Edit also requires prior Read but is the right tool for partial updates.
- **Prevention**: Future skills that populate canvas files inherit this guidance from CLAUDE.md without per-skill duplication. If this recurs as a per-skill failure (≥3 instances), graduate to a PreToolUse hook on `.claude/canvas/*.yml` that injects "use Read tool first" into the deny reason — converts the quirk into a self-explaining harness rule.
- **Source**: Detected during 2026-05-07 Juniors.dev presentation pre-run dogfood. Theory: Lopopolo ("every interaction is a failure of the harness to provide enough context") — the agent's behavior was correct given the context it had; the harness was missing the tool-quirk context. Brooks (No Silver Bullet — accidental complexity from tool quirks should be absorbed by the harness, not pushed to the user).

### 2026-05-15 - RECURRING: named cohort attribution leaked into public framework repo working tree
- **Scope**: quality, privacy
- **Category**: process, communication
- **Origin**: ai-generated, founder-detected pre-push
- **Detection_origin**: user
- **Mistake**: During the v0.23.21 PATCH for ht-012 cohort-log driven friction fixes, the agent wrote three cohort participant names (one ht-012 participant + two ht-014/ht-015 participants) across 5 working-tree files (CLAUDE.md version line, docs/changelog.md entry, two SKILL.md discipline-note attributions, opportunities.yml opp-010 description + provenance + notes including a verbatim source-language quote). Plus an opp-009 commit pushed earlier the same day (commit 43989ae) already contained 2 named references on `origin/main`. The auto-memory rule "cohort attribution boundary" (generic-only in framework, public receipts need separate sign-off) was loaded into context at session start but lost to the cognitive-load gate — convenience-of-accurate-attribution outweighed remember-the-boundary. Founder caught pre-push (paraphrased: "did you just use the cohort participant's name against your own previous judgement?"); a second catch surfaced the additional two cohort members ("there are also more mentions" / "none have given agreement to using their names yet"). This is the **third instance** of the named-attribution-leak class in two weeks: v0.23.13 (initial leak in framework + private-companion repo + commit messages), v0.23.14 (commit-message-as-public-surface follow-on), now v0.23.21 (recurrence into multiple file classes).
- **Correction**: Regenericized all six files in the same PATCH (5 working-tree + opp-009 cleanup of already-pushed leak from 43989ae). Pattern: name → "ht-012 cohort participant" / "ht-014 cohort participant" / "ht-015 cohort participant"; verbatim source-language quote removed and replaced with English paraphrase + pointer to private roadmap canvas where the verbatim text stays. v0.23.21 changelog entry updated to call out the leak + fix in-band. 3 pre-existing leaks on origin (v0.23.13 title, v0.23.10 entry, v0.18.x entry mentioning a cohort participant) flagged as out-of-scope follow-up for a dedicated regenericize PATCH.
- **Prevention**: Cognitive-load gates fail; mechanical gates catch (Hashimoto principle, graduated as v0.23.16 framework-on-framework exemption rationale). The auto-memory rule was a cognitive gate. The mechanical gate that exists (Check 33 attribution registry) fail-opens in CI by design when `MYCELIUM_ATTRIBUTION_REGISTRY` env var is absent — and the agent doesn't set it locally for the framework repo because the registry lives outside this repo per the v0.23.13 architecture correction. **Proper way forward** is to make Check 33 fail-LOUD locally (different from CI) when the framework-repo working tree is being edited and the registry can't be resolved — this is the right shape of a mechanism that catches at edit-time, not push-time. Also: the just-shipped pre-push hook (v0.23.20) doesn't run validate-template.sh (which includes Check 33); extending it to do so would surface this class at push-time as a backstop. Deep-dive analysis tracked separately at the user's request, leading to a v0.23.22 candidate mechanism.
- **Recurrence flag**: **Instance #3** in two weeks of the same class (named-attribution-leak in framework repo). Class graduated to anti-pattern candidate in cluster-instances.md: `cohort-attribution-leak`. Three documented graduation actions to date have not prevented recurrence — registry move (v0.23.13), CI fail-open documentation (recent), auto-memory rule. Next graduation must be edit-time mechanical or the pattern will recur. See cluster-instances.md for full instance log + proposed graduation criterion.
- **Source**: Founder pre-push detection 2026-05-15 (two messages, sequential: first cohort participant mention then additional cohort members mention). Auto-memory entry on the cohort attribution boundary (cognitive gate that failed). Hashimoto's Feb 2026 principle on engineering-out-recurrence. Anti-pattern #8 (Stale State Read) kin: the agent read the attribution boundary rule but wrote based on the convenience-of-attribution.

### 2026-05-06 - provenance schema rejected the framework's own canonical evidence vocabulary
- **Scope**: quality
- **Category**: engineering
- **Origin**: ai-generated
- **Detection_origin**: evaluator
- **Mistake**: During pre-run dogfooding for the 2026-05-07 Juniors.dev presentation, `/interview` populated `jobs-to-be-done.yml` and `opportunities.yml`. Validation failed three times with `Additional properties are not allowed ('source_class' was unexpected)` and `('notes' was unexpected)` inside `provenance` blocks. Root cause: the framework convention uses singular `source_class` at the top level of every evidence entry across `purpose.yml`, `landscape.yml`, `metrics-pull` snapshots, and 5+ skills — the agent generalized that convention into provenance blocks. But the provenance sub-schema (`_common.schema.json#/$defs/provenance`) accepted only `source_classes` (plural array) and had `additionalProperties: false`. Same concept, two field names depending on schema depth — a structural footgun. The agent also reached for `notes` as a natural free-text field; schema rejected it without offering a sanctioned alternative.
- **Correction**: Schema now accepts singular `source_class` (single enum value, applying to all `evidence_sources`) AND `notes` (optional free-text string) inside provenance, alongside the existing plural `source_classes`. Strict mode preserved for everything else — typos still caught. If both `source_class` and `source_classes` are present, `source_classes` (per-source) takes precedence.
- **Prevention**: When `additionalProperties: false` is used, audit whether the rejected fields are typos or natural-feeling fields the rest of the framework conventions point toward. If the latter, accept them rather than fight the convention. Sister pattern to "documented rule diverges from enforcement" (5th-instance graduation Check 26, 2026-05-04) — same shape: schema layer disagrees with the convention the rest of the framework teaches.
- **Source**: Self-detected during dogfood pre-run for Juniors.dev presentation. Theory: DRY (one canonical name per concept across schema depths), POLA (Principle of Least Astonishment — Saltzer & Schroeder; what the rest of the framework teaches should also work in narrower scopes).

### 2026-04-30 - Eval overfitting in documentation
- **Scope**: quality
- **Category**: bias
- **Origin**: ai-generated
- **Detection_origin**: external_review
- **Mistake**: After an eval revealed a factual gap (Lenoir 1860 predating Otto 1876), the agent encoded the answer directly into the data documentation: "NOT the first commercial ICE (Lenoir 1860 predates it)." Drew caught it: "feels like over-fitting/cheating for the specific tests we're running." The agent also had a recurring pattern of negative documentation — defining what things are NOT rather than what they ARE.
- **Correction**: Data documentation should state what something IS, with citations. Test-derived knowledge should improve the data itself, not be injected as defensive annotations. If an eval reveals a gap, fix the data — don't annotate the documentation to pass the eval.
- **Prevention**: New anti-pattern "Eval Overfitting" added to anti-patterns.md. Detection rule: documentation contains "NOT" qualifiers that reference specific test scenarios or eval questions. Also: new anti-pattern "Negative Documentation" — defining things by what they are not.
- **Source**: Hoskins friction log (2026-04-25). Goodhart's Law (when a measure becomes a target, it ceases to be a good measure — when eval results become the target, documentation ceases to be good documentation).

## Situational Corrections

_Corrections specific to a particular project, team, or context._

