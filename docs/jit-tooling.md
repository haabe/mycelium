# JIT tooling

**Audience**: practitioners using Mycelium on non-default tech stacks or non-software product types.
**Time to read**: skeleton (Phase 1); 5 min once filled.
**Last updated**: 2026-05-08.

> This page is forthcoming. Phase 2 will write depth.

## What this page will cover

- The detect-and-generate philosophy: Mycelium does not pre-ship a per-stack catalog; it detects what's there and generates adapters.
- `/delivery-bootstrap` for tech stacks (`.claude/jit-tooling/detector.md`).
- `/metrics-detect` for metric sources (GitHub, analytics, payments, app stores).
- `product_type` dimension — software / content / AI tool / service.
- The "zero handover" north-star check.

## Phase 2 trigger

Content fills as part of Phase 2 docs backfill (~30 min).
