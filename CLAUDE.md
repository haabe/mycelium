# Mycelium: Theory-Guided Agentic Product Development

*Version 0.21.0 -- Three anti-pattern graduations clear the open candidates carried in `corrections.md` TL;DR. **(#7) Consistency-as-Evidence**: constructing causal chains where ≥1 link rests on observational consistency rather than verified attribution. Three documented instances on the project (2026-04-30 Hoskins over-scoping, 2026-05-03 sharper-framing anchoring, 2026-05-09 Frida verbosity-attribution gap). Distinct from confirmation bias (attention-direction failure) — this is attribution failure (misclassifying evidence in hand). Source: Pearl causal inference (observational vs interventional). Graduated to anti-pattern #7 in `harness/anti-patterns.md`. **(#8) Stale State Read**: scripts/validators reading state files the same operation is about to replace, producing nominally-correct output against the wrong reference. Four documented instances (2026-04-28 + 2026-05-03 upgrade.sh hardcoded list drift, 2026-05-04 validate_canvas.py ID-uniqueness gap, 2026-05-04 upgrade.sh manifest stale-read). Worked example: `parse_manifest.py --manifest=<path>`. Graduated to anti-pattern #8. **(Bias cluster → ambient `/devils-advocate`)**: the "agent prefers what feels right over what evidence supports" failure mode, surfacing as L5 sycophancy + eval overfitting + sharper-framing anchoring. Graduated as Techniques 4 (attribution-vs-consistency labeling) and 5 (ambient triggering on assertion-shaped patterns) in `/devils-advocate` — converts the framework's anti-bias discipline from per-decision ceremony to per-publish self-check. Plus integration: `/corrections-audit` gains Step 6d (consistency-as-evidence detection) and Step 6e (stale-state-read pattern scan) for ongoing surveillance; `tests/validate-template.sh` Check 29 scans plugin scripts for stale-state-read heuristic; `CLAUDE.md` G-P-pre item 9 makes the attribution check a pre-publish protocol step so the anti-bias discipline self-applies to the agent's own analysis. MINOR per version-discipline (three new mechanisms, structural addition to existing surfaces). The graduations close the open backlog the TL;DR has been carrying for 1-2 weeks; future instances should be caught by the framework rather than by user vigilance.

0.20.15 (folded — commit `1e592f0`) -- Legacy `npx degit` install path deprecated + manifest single-source decision. Three coordinated changes close the post-migration follow-ups surfaced 2026-05-09. **(1)** Legacy install path explicitly deprecated. README's "Legacy install" subsection rewritten with deprecation notice — `npx degit haabe/mycelium` for new installs is no longer supported (a fresh degit lands an empty `.claude/` with no skills/hooks/engine; the canonical install is plugin form). Section now points at the migration guide and the recovery doc rather than offering install commands. Removal scheduled for v0.21.0 / 2026-06-09 (whichever comes first). **(2)** Stale-upstream detection added to `plugins/mycelium/scripts/upgrade.sh`: when run in legacy refresh mode and the upstream tree pulled has no `.claude/skills/` or `.claude/engine/` (because upstream main no longer ships them), the script exits with a clear "the canonical Mycelium has moved to plugin form" message and recommends `--migrate-to-plugin` or the agent-driven `/mycelium:migrate-from-legacy` skill. Routes legacy users to recovery instead of letting them watch a broken refresh. **(3)** `docs/migration.md` gains a "Recovering from a broken legacy refresh" section covering the four common failure modes (`Failed to pull upstream` from old upgrade.sh, the new stale-upstream detector firing, `no such file or directory` for the script, missing-framework-files-after-refresh) — each with a specific recovery path. **Manifest dual-source decision (single-source at plugin tree, bounded transition):** `plugins/mycelium/manifest.yml` is now canonical; `.claude/manifest.yml` is the deprecated legacy copy retained for in-flight migration users. Both files now carry symmetric deprecation headers stating the canonical location, the deletion target (v0.21.0 / 2026-06-09), and that the validator enforces byte-match. New Validator Check 28 enforces byte-equality between the two while both exist; check goes away when `.claude/manifest.yml` is deleted. PATCH per version-discipline (deprecation + tooling; no new mechanisms; sunset paths set with explicit dates).

0.20.14 (folded — commit `218477c`) -- CI restored after the legacy `.claude/` cleanup. The 0.20.13 migration deleted `.claude/tests/validate-template.sh` + `.claude/tests/python/` + the auto-dogfood orchestrator, which broke `validate.yml` (referenced the deleted validator) and orphaned `dogfood.yml` (referenced the deleted orchestrator; had been failing since 2026-04-20 anyway). Three changes: **(1)** Validator + Python tests rehomed at repo root: `tests/validate-template.sh` (formerly `.claude/tests/validate-template.sh`) + `tests/python/` (9 test files + conftest). REPO_ROOT calculation updated to `dirname/..` (one level up; was `dirname/../..` for the legacy two-level layout). All `.claude/scripts/` references in the validator updated to `plugins/mycelium/scripts/`; same for `.claude/{engine,harness,hooks,schemas}/` → plugin tree. material_paths in Check 26 stripped of dead `.claude/{skills,engine,harness,hooks,scripts,templates,tests}` entries. **(2)** `plugins/mycelium/manifest.yml` added — copy of `.claude/manifest.yml`. The plugin's `parse_manifest.py` defaults to `script_dir.parent/manifest.yml` (= `plugins/mycelium/manifest.yml`), so the plugin needs its own copy for the migration script (`--migrate-to-plugin`) to work standalone. Single source of truth being plugin-local; the legacy `.claude/manifest.yml` stays during transition for any in-flight legacy refresh users mid-migration. **(3)** `validate.yml` updated to call `bash tests/validate-template.sh`, `python3 plugins/mycelium/scripts/validate_canvas.py`, `pytest tests/python/` with coverage targeting `plugins/mycelium/scripts`. Path triggers extended to include `plugins/mycelium/**`, `.claude-plugin/**`, `tests/**`, `docs/**`. **(4)** `dogfood.yml` deleted — the auto-dogfood orchestrator was framework reference content; deleted in 0.20.13 migration; if reinstated it'll ship in plugin form and the workflow gets rewritten. Validator state: 24 passed, 0 failed, 4 warnings (parity-check now shows "only one skills tree present — N/A" since legacy is gone). PATCH per version-discipline (post-migration tooling repair; no new mechanisms; restores CI green).

0.20.13 (folded — commits `7c2a89d` + `e0825be`) -- Two changes from the 2026-05-09 self-migration friction (the maintainer ran `--migrate-to-plugin` on the upstream and downstream Mycelium repos themselves, eating own dogfood). **(1)** Legacy `.claude/` framework tree removed from upstream Mycelium repo via `--migrate-to-plugin`; previously the upstream carried both legacy `.claude/skills/` (45 skills, last sync v0.18.x) and `plugins/mycelium/skills/` (49 skills, post-merge), with parity check warning about the divergence. The legacy tree is now gone from upstream — plugin form is the only Mycelium install path on main. Closes the legacy-cleanup pending item from 0.20.10's commit message. Migration commit on `chore/legacy-cleanup` branch (deleted 180 files, 27,464 lines). Project state in upstream's `.claude/` (canvas, diamonds, memory, decision-log, evals, drafts, manifest.yml, settings) preserved untouched. **(2)** README.md gains a "Heads-up if your install is older than v0.20.10" callout in the migration section: the `--migrate-to-plugin` flag was added in 0.20.10, so installs predating it will misinterpret the flag as a version arg and fail with "Failed to pull upstream. Check version/tag exists: --migrate-to-plugin". Workaround: refresh first (`bash .claude/scripts/upgrade.sh` no args) to get the new upgrade.sh from upstream main, then re-invoke with the flag. Surfaced during the maintainer's own downstream migration; docs/migration.md already had the workaround but README didn't. PATCH per version-discipline (legacy cleanup + doc clarification; no new mechanisms). Plugin form continues to be the canonical install path; legacy npx-degit installs still work but their upgrade.sh predates the migration flag — the README now warns about this.

0.20.12 (folded — commit `c3cecca`) -- Receipts case for the 2026-05-09 plugin-form dogfood (`docs/receipts/cases/2026-05-09-plugin-form-dogfood.md`). Files Mycelium-evaluated-against-itself as a self-dogfood entry alongside the bentes-install-model case from the same architectural cycle. Captures: 5 bugs surfaced and closed same-day (B1-B5), the load-bearing L0 adoption assumption ("The harness is light enough that people keep choosing it past the first friction moment"; Cagan four-risks classification: usability), the operational test design that emerged from `/mycelium:assumption-test`, and the framework recusing itself from `/mycelium:bias-check` on its own test design (self-coherence trap caught explicitly). Lessons graduated to receipts indexes: subagent simulation ≠ lived friction; detection-then-route must be a hard gate; variable expansion at Write boundaries is an environment leak; Check 26 must watch manifest files, not just the directories they describe; self-dogfood produces research artifacts the framework can use. Receipts indexes (README, by-date, by-mechanism, by-contributor) updated to surface the new case + the four mechanisms it cross-references (`/mycelium:start` welcome, `/mycelium:migrate-from-legacy`, Validator Check 27, hard-gate ordering pattern). PATCH per version-discipline (documentation; no new mechanisms; the case is a reading surface, not a behavior change).

0.20.11 (folded — commit `5f1b416`) -- Five bug fixes from 2026-05-09 plugin-form dogfood (the framework dogfooded on itself, surfacing real friction the subagent simulations missed). Findings + fixes: **(B4)** `setup/SKILL.md` warnings-log.md starter content was getting `${CLAUDE_PLUGIN_ROOT}` expanded by the agent during Write, baking the maintainer's absolute path into every user's project. Fixed by switching to prose ("the plugin's `ingest_warnings.py`") and adding an explicit "do NOT expand" instruction; the file body is documentation, not a runnable command, so prose is correct. **(B1)** `start/SKILL.md` Step 2 detection was being honored AFTER the agent already ran setup-style `mkdir -p`, causing Read-before-Write tool errors when then writing existing files. Fixed by promoting the detection to a HARD GATE — first action after welcome, no Bash/Edit/Write before it fires, no "let me prepare directories" reasoning. **(B2)** `plugin.json` version stayed pinned at `0.20.0` while CLAUDE.md moved through 10 patches; ping marker `MYCELIUM_PLUGIN_LOAD_OK_v0.20.0` similarly drifted. Fixed: `plugin.json` bumped to track CLAUDE.md (skill count also corrected 45 → 49); ping marker made shape-stable (`MYCELIUM_PLUGIN_LOAD_OK`, no version suffix — it's a plugin-load assertion, not a version assertion). **(B3)** Validator Check 26 watched CLAUDE.md but not `plugin.json` or marketplace.json, which is what let drift through. Fixed: `plugins/mycelium/.claude-plugin/plugin.json`, `plugins/mycelium/.claude-plugin/marketplace.json`, `.claude-plugin/marketplace.json` added to material_paths. **(B5)** Setup didn't acknowledge `.claude/state/` (Claude Code's runtime state directory, not Mycelium's); agent flagged as anomaly during dogfood. Fixed with one-line note in Step 2. PATCH per version-discipline (correctness fixes; no new mechanisms; B4 is the only one real users would hit immediately, the rest are mechanism integrity). The dogfood produced more than bug fixes: a complete L0 adoption test design at `.claude/evals/assumption-tests/L0-adoption-test.md` in the test repo (n=5, pre-committed Persevere/Ambiguous/Pivot/Kill thresholds, recruitment template, Rother-style prediction), plus the load-bearing assumption captured in the brief: "The harness is light enough that people keep choosing it past the first friction moment" (Cagan four-risks classification: usability). The framework caught its own self-coherence trap explicitly during the test design. With B1-B5 closed, the merge-to-main blockers are exhausted; this commit unblocks the canonical 0.20.x promotion.

0.20.10 (folded — commit `34ea768`) -- Legacy → plugin migration path. Three coordinated artifacts: (1) `.claude/scripts/upgrade.sh` gains `--migrate-to-plugin` flag (deletes legacy framework files in `.claude/`, preserves project state — manifest-driven via `parse_manifest.py directories` + `harness_framework`, so deletion list cannot drift from the refresh list per Check 16) and `--check-migration` flag (read-only diagnostic reporting current install form). Top-level detection routes plugin-form invocations away from upgrade.sh entirely (recommends `/plugin update mycelium@haabe-mycelium` instead); legacy-form invocations get the existing refresh flow plus a footer recommending plugin form + the migration command. (2) New skill `/mycelium:migrate-from-legacy` (8 steps: detect form, verify plugin installed, verify clean working tree, render explicit will-DELETE/will-PRESERVE preview à la Liao contrastive XAI, invoke script, verify project state survived, manual settings.json hooks-block cleanup, final commit). Idempotent — already-migrated projects exit early. (3) New `docs/migration.md` with three migration paths (agent-driven via skill, shell-driven via script, manual `rm` recipe) + reversibility notes (`git reset --hard HEAD`) + transition-window framing. README gains a "Migrating from legacy to plugin form" subsection pointing at the skill + script + docs. CLAUDE.md L227 + docs/skills/README.md L7 skill count: 48 → 49 (new migrate-from-legacy skill). Smoke-tested in isolated `/tmp/migrate-smoketest`: clean delete of 9 framework directories, project state preserved, settings.json hooks-block warning fires, next-steps prompt clear. Validator state: 22 passed, 0 failed, 7 warnings (transition-state warnings: stub-readiness, parity divergence, uncommitted-after-bump nudge — all expected). PATCH per version-discipline (additive: new flag + new skill + new doc; no breaking changes; no new mechanism on existing surfaces — script flag and skill are net-new surfaces). Unblocks legacy users from being stranded on legacy form indefinitely; together with 0.20.9's validator update, the merge-to-main blockers narrow to: real-user dogfood session on plugin form, framework-guard.sh matcher refinement, final 0.20.0 bump + legacy `.claude/` cleanup at canonical promotion.

0.20.9 (folded — commit `828e6d2`) -- Validator updated for plugin-form layout. Closes the "Checks 6/7/9 still expect skills under `.claude/skills/`" pending item from 0.20.5. Three changes: (1) `SKILLS_DIR` detection at top of `validate-template.sh` — prefers `plugins/mycelium/skills/` (canonical post-0.20), falls back to `.claude/skills/` (legacy), exits 2 if neither exists; (2) every Check (5, 6, 7, 8, 9, 15, 26) now threads `$SKILLS_DIR` through hardcoded `.claude/skills/` literals; Check 9 accepts either path-form in CLAUDE.md to support transition; Check 26 watches BOTH plugin and legacy material paths so version-bump discipline holds across either tree; (3) new Check 27 (skills-tree parity) — when both trees coexist, compares skill count + name set, WARNs on divergence (the legacy tree is no longer being maintained; this surfaces the drift). New skills `ping`, `setup`, `start` had `description:`-only frontmatter; added `name:` to all three to satisfy Check 8. Skill-count references updated 45 → 48 in CLAUDE.md L225 (auto-discovered statement) and `docs/skills/README.md` L7. CLAUDE.md and docs/skills/README.md path references generalized: "in plugin form (recommended) or legacy form (supported during transition)" — accommodates either install path. Validator now reports: 24 passed, 0 failed, 5 warnings (warnings are documented transition state: 1 stub-readiness, 1 setup skill at-risk-list candidate, 1 uncommitted-after-bump nudge, 2 parity-divergence — all expected during legacy/plugin coexistence). PATCH per version-discipline (validator update; tooling correctness; no new mechanisms). Unblocks merge to main once final 0.20.0 bump + legacy `.claude/` cleanup at canonical promotion.

0.20.8 (folded — commit `96954a2`) -- Dogfood-mode pattern migrated to plugin tree. Closes the out-of-scope content gap surfaced by 0.20.7 re-smoketest: `interview/SKILL.md:285` (and two other surfaces) referenced `.claude/evals/dogfood-reports/README.md` "for the pattern," but plugin-form setup creates only a `.gitkeep` there — dogfood-mode users had no pattern to reference. The README's content is generic framework reference (purpose, when to run, how to run, report structure, anonymization) with a project-specific Reports table at the bottom. Right cut: framework-reference content moves to plugin (`${CLAUDE_PLUGIN_ROOT}/engine/dogfood-mode.md`), `.claude/evals/dogfood-reports/` in user's project stays empty for their own reports. Updated three references: `interview/SKILL.md:285`, `engine/canvas-guidance.yml:216-218` (collapsed duplicate + fixed wrong `.claude/skills/...` prefix to `${CLAUDE_PLUGIN_ROOT}/skills/...`), `orchestration/operations.md:72`. Legacy `scripts/upgrade.sh` stale comment (line 283-286) about "preserved_dir_readmes manifest-driven loop" left alone — upgrade.sh is legacy-form code, plugin upgrades via `/plugin update`. PATCH per version-discipline (content migration; no new mechanisms; closes a real reference dead-end caught by smoketest).

0.20.7 (folded — commit `70c07fb`) -- Bare-path discipline sweep across all 45 SKILL.md. Smoke-test of 0.20.6 (4 parallel blind subagents) flagged two cross-cutting pre-existing patterns the previous path-rewrite scripts (0.20.4 / 0.20.5) didn't catch: bare project-state paths (`canvas/X.yml`, `diamonds/X.yml`, `memory/X.md`) that relied on CLAUDE.md context to disambiguate, and bare framework-reference filenames (`theory-gates.md`, `confidence-thresholds.yml`, etc.) that searched relative to project root in plugin form. Both classes are brittle once `$CLAUDE_PROJECT_DIR` and `$CLAUDE_PLUGIN_ROOT` split path resolution into two trees. Two-pass sweep: pass 1 prefixes bare project-state with `.claude/` and bare framework filenames (50 unique, mapped from actual plugin tree) with `${CLAUDE_PLUGIN_ROOT}/<dir>/`; pass 2 handles bare `<fw-dir>/X` patterns and routes `harness/decision-log.md` + `harness/warnings-log.md` to `.claude/harness/` (project-state carve-out). Plus one upstream typo fix in interview/SKILL.md (`canvas/diamonds/active.yml` → `.claude/diamonds/active.yml`, lines 21 + 68 — cherry-picked typo from feat/on-ramp 0.19.1). Touched: 36 files, ~145 path rewrites, 12 decision-log re-routes, 1 typo. Manual revert in setup/SKILL.md AGENTS.md template (bullets under "Project state lives in `.claude/`" header — bare entries are the consistent pattern). Verified: all "Reference X" patterns explicit, no double-prefix bugs, internal consistency restored (diamond-assess line 36 vs 74 was mixing bare + prefixed for the same file). PATCH per version-discipline (mechanical content rewrite, correctness-improving; no new mechanisms; no breaking changes).

0.20.6 (folded — commit `b5eeb76`) -- Onboarding void closer. New `/mycelium:start` combiner skill that runs setup + interview universal-flow in one invocation, with a 30-second welcome that gives the framework context at the moment of cognitive need (Norman: visible affordances; Krug: don't-make-me-think; Hashimoto regeneration zones). `/mycelium:setup` confirmation expanded with a one-paragraph welcome + framing of the next move, so direct setup invocation also closes the install→value gap. `/mycelium:interview` cherry-picked from `feat/on-ramp` 0.19.1 with universal-flow shape (canvas-state detection + 4-question brief + informed depth menu — replaces legacy Phase 0 time-budget routing) and rewritten through the plugin-form path-rewrite script (5 path rewrites, 29 slash-command rewrites). README + AGENTS.md: Quick Start now leads with `/mycelium:start`; both surfaces document tab-completion (`/myc<Tab>` → `/mycelium:`) and natural-language invocation ("run mycelium start") as the realistic answer to the namespace-prefix typing-friction concern raised during 2026-05-09 plugin-form smoke test. Path-rewrite script jit-tooling carve-out updated to also exclude `.gitkeep` (closes over-rewrite edge case caught in 0.20.4). Source: dogfood report 2026-05-09 — "the onboarding has worsened substantially since converting to plugin. The user is left in the unknown void for quite a bit." PATCH per version-discipline (continuation of plugin-form bootstrap; structurally additive — new skill, expanded confirmation output, doc updates; no breaking changes).

0.20.5 (folded — commit `0f073b1`) -- Plugin-form path resolution in scripts. Subagent re-verification 2026-05-09 caught that `plugins/mycelium/scripts/validate_canvas.py` (and `ingest_warnings.py`) hardcoded `Path(__file__).parent.parent.parent` as repo root, which only resolves correctly in legacy form (`<repo>/.claude/scripts/X.py`); in plugin form (`<plugin>/scripts/X.py`) the same expression skips a directory level and points at `plugins/`. Both scripts now use a `_resolve_paths()` helper that honors `$CLAUDE_PLUGIN_ROOT` (for framework-reference content like schemas/, engine/) and `$CLAUDE_PROJECT_DIR` (for project state like canvas/, memory/) with sensible auto-detect fallbacks for legacy and dogfood-from-cwd contexts. Tested in three configurations: legacy `.claude/scripts/`, plugin form via cwd-detect, plugin form via explicit env vars on a foreign test directory — all three resolve correctly. validate_canvas now correctly fires on schema violations in the simulated test directory (subagent's mock canvas had `evidence` fields where the schema didn't allow them — real validation catch). Other scripts checked: `framework_guard.py`, `parse_manifest.py`, `scope_check.py`, `_manifest_lib.py` — none use the broken `parent.parent.parent` pattern (they receive paths via argv from hooks). Pending: validator updates for plugin-form layout (Checks 6/7/9 still expect skills under `.claude/skills/`), framework-guard.sh matcher refinement, cleanup of legacy `.claude/` once plugin-form is canonical, final 0.20.0 bump on merge to main. PATCH per version-discipline (script bug fix; no new mechanisms).

0.20.4 (folded — commit `0d17210`) -- Plugin-form path + namespace rewrite across all 45 SKILL.md files. 35 path rewrites (`.claude/engine/X` → `${CLAUDE_PLUGIN_ROOT}/engine/X`, same for `.claude/schemas/`, `.claude/scripts/`, `.claude/domains/`, `.claude/orchestration/`, `.claude/harness/X` excluding decision-log.md/warnings-log.md, `.claude/jit-tooling/X` excluding active-metrics.yml). 155 slash-command rewrites (`/skill-name` → `/mycelium:skill-name` across all 45 known skills). Project-state paths preserved unchanged: `.claude/canvas/`, `.claude/diamonds/`, `.claude/memory/`, `.claude/evals/dogfood-reports/`, `.claude/harness/decision-log.md`, `.claude/harness/warnings-log.md`, `.claude/jit-tooling/active-metrics.yml`. Two over-rewrites in setup/SKILL.md caught and corrected manually (the script over-broadly rewrote `.claude/jit-tooling/.gitkeep` to plugin-root since the carve-out only excluded `active-metrics.yml`). Resolves the headline gap from the 2026-05-09 subagent re-simulation: plugin-form `/mycelium:interview` now correctly resolves all read-only framework references to `${CLAUDE_PLUGIN_ROOT}/...` (engine/canvas-guidance.yml, engine/wayfinding.md, jit-tooling/metrics-detector.md, harness/security-trust.md, etc.) while project-state writes remain at `.claude/...` in user's project. Pending: rewrite-script edge case (jit-tooling carve-out should also include `.gitkeep`); validator updates for plugin-form layout; framework-guard.sh matcher refinement; cleanup of legacy `.claude/` once plugin-form is canonical; final 0.20.0 bump on merge to main. PATCH per version-discipline (mechanical content rewrite; no new mechanisms; no breaking changes).

0.20.3 (folded — commit `20850da`) -- Framework reference tree migrated into `plugins/mycelium/` (engine/, harness/ minus project-state files, schemas/, scripts/, jit-tooling/, domains/, orchestration/) — closes the architectural gap surfaced by subagent simulation 2026-05-09: `/mycelium:interview` referenced `.claude/engine/canvas-guidance.yml`, `.claude/engine/wayfinding.md`, `.claude/jit-tooling/metrics-detector.md`, `.claude/harness/security-trust.md`, etc. that the plugin did not yet ship. With this migration the plugin self-contains the full framework reference content; only project-state files (canvas/*.yml, diamonds/active.yml, memory/corrections.md+patterns.md+cluster-instances.md+journals, harness/decision-log.md, harness/warnings-log.md, evals/, jit-tooling/active-metrics.yml) live in user's project under `.claude/`. Plus setup SKILL.md fixes from same simulation: explicit `.gitkeep` stubs for empty dirs (so canvas/, evals/, jit-tooling/ survive git commits); CLAUDE_PROJECT_DIR fallback to pwd documented; auto-mode default-yes policy stated for the AGENTS.md prompt; Step 5 confirmation now state-aware (only mentions AGENTS.md if actually written); Read-before-Write convention clarified (applies only to existing files); receipts case forthcoming-ref updated to actual link. Two false positives from the framework-guard surfaced in the simulation logged for future refinement (substring-match collateral on /tmp paths, embedded-data path matches). Pending: skill namespace migration (every `/foo` -> `/mycelium:foo` in SKILL.md content; subagent surfaced 8 occurrences in interview alone), interview SKILL.md reference cleanup (now that plugin ships the referenced files, paths can be `${CLAUDE_PLUGIN_ROOT}/...` instead of `.claude/...`), validator updates for plugin-form layout, framework-guard.sh matcher refinement, cleanup of legacy `.claude/` once plugin-form is canonical. PATCH per version-discipline (continuation of 0.20.x bootstrap; framework-content migration is structurally additive).

0.20.2 (folded — commit `66fe291`) -- AGENTS.md + README install instructions updated to document plugin-form install path alongside legacy `npx degit` path. AGENTS.md "What's available" table updated with plugin/legacy file-location distinction. README Quick Start gains plugin-install section as recommended path; legacy section preserved for transition. README Upgrading section gains `/plugin update` instruction. Soft cap warning on AGENTS.md (95 lines vs 80 soft cap) acknowledged — acceptable for transition documentation. PATCH per version-discipline (continuation of bootstrap; documentation update).

0.20.1 (folded — commit `61a823b`) -- Plugin-form migration progress: 45 skills migrated to `plugins/mycelium/skills/` (commit `4656910`); 10 hooks migrated with `hooks.json` wiring all event matchers via `${CLAUDE_PLUGIN_ROOT}` paths (commit `69c00c5`); `/mycelium:setup` skill added for idempotent first-run project init (commit `eae91b6`); receipts case `2026-05-08-bentes-install-model.md` documenting Daniel Bentes's install-model finding + CONTRIBUTORS.md v0.20.0 entry crediting his second-cycle architectural review (commit `cc940e3`). Existing `.claude/` framework dir still preserved during migration. Hook scripts unchanged (still reference `$CLAUDE_PROJECT_DIR/.claude/...` for project state). Pending: AGENTS.md content migration from CLAUDE.md, README install instructions update, skill namespace migration in receipts/docs/decision-log, validator updates for plugin-form layout, framework-guard.sh matcher refinement (3 false positives during this session — substring-anywhere matching catches commit messages and tmp paths), cleanup of legacy `.claude/` dir once plugin-form is canonical, version bump to 0.20.0 final on merge to main. PATCH per version-discipline (continuation of bootstrap; structurally additive).

0.20.0 (folded — bootstrap commit `a515448`) -- Plugin-form bootstrap. Mycelium repackaged as a Claude Code plugin per Anthropic's plugin spec to solve the install-model architectural debt surfaced by Daniel Bentes 2026-05-08 (top-level framework metadata files contaminate user project root). New top-level structure: `.claude-plugin/marketplace.json` (single-plugin marketplace catalog) + `plugins/mycelium/.claude-plugin/plugin.json` (plugin manifest). Initial smoke-test skill `plugins/mycelium/skills/ping/SKILL.md` validates the plugin shape (`/mycelium:ping` returns deterministic marker). Architecture cut documented in `plugins/mycelium/README.md`: skills/agents/hooks/harness/engine/schemas/scripts ship in plugin (versioned, replaceable, cached read-only); canvas/diamonds/memory/decision-log/evals/active-metrics live in user project (writable, gitted). Skill names become namespaced (`/interview` → `/mycelium:interview`) per Anthropic's plugin convention — UX cost accepted in exchange for brownfield-clean install + marketplace discovery + cross-agent portability via AGENTS.md (open Linux Foundation standard, read by Codex/Cursor/Aider/Copilot/Continue.dev/OpenHands; Claude Code falls back to it when CLAUDE.md absent). MAJOR-shaped change in user-facing install model (new mechanism: `/plugin marketplace add haabe/mycelium`); MINOR per version-discipline because plugin form is additive — existing `npx degit` install path remains supported during transition. Theory grounding: Anthropic plugin convention (Skills/Agents/Hooks/MCP composition), AGENTS.md open standard (Linux Foundation), Daniel Bentes BDSK architectural review (CONTRIBUTORS.md v0.9.0 + v0.20.0 install-model finding), Synapti marketplace pattern study. Mycelium repackaged as a Claude Code plugin per Anthropic's plugin spec to solve the install-model architectural debt surfaced by Daniel Bentes 2026-05-08 (top-level framework metadata files contaminate user project root). New top-level structure: `.claude-plugin/marketplace.json` (single-plugin marketplace catalog) + `plugins/mycelium/.claude-plugin/plugin.json` (plugin manifest). Initial smoke-test skill `plugins/mycelium/skills/ping/SKILL.md` validates the plugin shape (`/mycelium:ping` returns deterministic marker). Existing `.claude/` framework directory preserved unchanged during migration — both forms coexist on this branch until full migration completes. Architecture cut documented in `plugins/mycelium/README.md`: skills/agents/hooks/harness/engine/schemas/scripts ship in plugin (versioned, replaceable, cached read-only); canvas/diamonds/memory/decision-log/evals/active-metrics live in user project (writable, gitted). Skill names will become namespaced (`/interview` → `/mycelium:interview`) per Anthropic's plugin convention — UX cost accepted in exchange for brownfield-clean install + marketplace discovery + cross-agent portability via AGENTS.md (open Linux Foundation standard, read by Codex/Cursor/Aider/Copilot/Continue.dev/OpenHands; Claude Code falls back to it when CLAUDE.md absent). Pending in subsequent commits on this branch: full skill migration into `plugins/mycelium/skills/`, hooks/hooks.json with SessionStart/PreToolUse/Stop event handlers replacing inline mandatory-protocol instructions, agents/ migration, `mycelium:setup` skill that creates project-state directories from plugin templates on first run, AGENTS.md content migration from CLAUDE.md, README install instructions update, skill namespace migration in receipts/docs/decision-log references, validator updates for plugin-form layout, version bump to 0.20.0 final on merge to main. `feat/on-ramp` (0.19.1 universal-flow brief + depth menu) supersedes into the plugin's `mycelium:interview` skill once that skill migrates. MAJOR-shaped change in user-facing install model (new mechanism: `/plugin marketplace add haabe/mycelium`); MINOR per version-discipline because plugin form is additive — existing `npx degit` install path remains supported during transition. Theory grounding: Anthropic plugin convention (Skills/Agents/Hooks/MCP composition), AGENTS.md open standard (Linux Foundation), Daniel Bentes BDSK architectural review (CONTRIBUTORS.md v0.9.0 + 2026-05-08 install-model finding), Synapti marketplace pattern study.

0.18.2 (folded) -- Phase 3 audit + maintenance wiring: `/corrections-audit` gains step 6c (scans `docs/receipts/cases/` YAML frontmatter for graduation signals — cluster cross-reference, mechanism-or-status pattern detection, contributor distribution audit, candidate-graduation cases, stalled-spec flags). `/canvas-health` gains step 9b (docs validation: audience markers, stub freshness >60d, length-budget hard/soft caps, last-updated >180d freshness, scent-rule scan for "click here" / "see [filename]", marketing-voice scan, receipts case frontmatter required-fields, README highlights >90d rotation candidacy). `/framework-health` gains step 4c (receipts highlights rotation cadence — staleness, newer-case opportunities, 60-day-no-new-case low-friction signal) + step 4d (docs cross-surface lightweight check delegating to /canvas-health). Internal-audience markers added to `.claude/memory/cluster-instances.md` and `.claude/harness/decision-log.md` ("internal — published as audit trail, not as public reading") per metadocumentation discipline. `docs/receipts/archive.md` placeholder created with archive rules (cases >12 months without recurrence + load-bearing mechanism still active → summary paragraphs in archive; original case files never deleted; deprecation handling for removed mechanisms; manual quarterly review per /framework-health 4c). PATCH per version-discipline (extends existing skills with new audit steps; adds public-vs-internal disclosure markers; no new mechanisms, no breaking shape changes). Closes the multi-phase README + docs restructure (Phases 1, 2, 3 of 3 all shipped). The receipts artifact now has end-to-end machinery: case files (Phase 1 structure), filled content (Phase 2 with 5 cases + style guide for adding new ones), and audit machinery that reads the structure (Phase 3). Theory grounding: Argyris double-loop closure (the audit reading the structure makes drift detectable).

0.18.1 (folded) -- Phase 2 content backfill: 9 forthcoming-stub docs filled (glossary, faq, evaluate, theories, philosophy, usage-modes, jit-tooling, regulatory, changelog, contributing/README) + skills/{README,by-category}.md filled with the 45-skill phase-and-category-ordered indexes. evaluate.md is the load-bearing landing for Drew Hoskins's post (~2026-05-25). usage-modes.md ships canvas-sync conflict-resolution rules answering Alex's Q from the 2026-05-07 Juniors.dev presentation (ht-007.key_finding). theories.md mechanism-maps every Tier 1 + Tier 2 theory to the Mycelium artifact that implements it (Tier 3 lists background citations as ... and more). philosophy.md frames opinionated discipline / theory-grounded / in-loop preventive / dogfood-required / build-to-learn-vs-earn / structure-before-content as the load-bearing claims, with explicit exit signal for readers who do not nod along. faq.md answers the six Juniors.dev questions concretely (token cost, skill coexistence, clear-vs-rough idea positioning, Codex compatibility, dogfooding vocabulary, multi-user canvas merge). glossary.md covers Mycelium-specific concepts + theory framework names with 2-4 sentences each, cross-linked to theories.md. contributing/README.md restates the asymmetric ask (low cost to find friction, uncapped credit if it shapes the framework) + two paths (issue, PR) + voice authority. PATCH per version-discipline (content fill within shipped Phase 1 structure; no new mechanisms, no breaking shape changes). Validator authority migration from Phase 1 still load-bearing — Checks 6 + 13 now actively validate populated docs/skills/ and docs/theories.md (no longer stub-skip path). Phase 3 (audit + maintenance wiring) follows: /corrections-audit reads docs/receipts/cases/ frontmatter, /canvas-health extends to docs validation, highlights-rotation cadence + archive rules + internal-audience markers on cluster-instances.md + decision-log.md.

0.18.0 (folded) -- README + docs/ restructure (Phase 1 of 3). README compressed 525 → 152 lines (target ≤200, hard cap 250). New `docs/` tree spokes off README hub: `docs/README.md` (metadocumentation: audience markers, voice rules, length budgets, scent rules, progressive-disclosure layers, cross-link discipline), `docs/contributing/style.md` (operating voice guide), 5 receipts case files under `docs/receipts/cases/` (tic-tac-toe, macos-can-i-open, macos-fileviewer, drew-hoskins-takehome, framework-self-correction) with YAML frontmatter (`id`, `date`, `contributor`, `contributor_link`, `project`, `mechanism_or_status`, `commits`, `subclass`) for future `/corrections-audit` candidate-graduation detection, plus `receipts/README.md` (patterns view), `by-date.md`, `by-contributor.md`, `by-mechanism.md` indexes, 9 forthcoming-stub pages (philosophy, glossary, faq, evaluate, theories, usage-modes, jit-tooling, regulatory, changelog) for Phase 2 backfill, `docs/skills/{README,by-category}.md` skeletons, `docs/contributing/README.md` skeleton. CONTRIBUTORS.md restructured with explicit "How to get listed" section (recruitment-shaped, asymmetric-deal framing) + per-contributor receipts cross-links + outreach archive moved to its own subsection. AGENTS.md expanded 48 → 86 lines: portable-vs-Claude-Code-specific surface table, concrete operating models for Codex/Cursor/Aider/Copilot + multi-agent orchestrators, agent-as-user JTBD note, examples for canvas-evidence-write and decision-log entries. Validator (`.claude/tests/validate-template.sh`) updated for new authority locations: Checks 2/3/4 deprecated (canvas-list authority is canvas-update SKILL.md, Check 5 covers); Check 6 reads docs/skills/README.md with stub-aware skip; Check 12 reads `.claude/engine/theory-gates.md` (canonical gate source); Check 13 reads docs/theories.md with stub-aware skip. Phase 2 fills the 10 forthcoming docs (~6-10h, drives over 1-2 weeks; priorities: glossary, faq, evaluate before Hoskins post ~2026-05-25, theories, philosophy, usage-modes with canvas-sync conflict rules per ht-007, skills index, changelog, regulatory, contributing process). Phase 3 wires `/corrections-audit` to scan `docs/receipts/cases/` frontmatter, extends `/canvas-health` for docs validation, adds highlights-rotation cadence (>90 day static surfaces flag at /framework-health), archive rules, internal-audience markers on `cluster-instances.md` + `decision-log.md`. MINOR per version-discipline (new structural surface, content authority migrates between artifacts). Backward compatible: README, AGENTS, CONTRIBUTORS retain canonical names + locations; old `docs/{ai-system-card,context-surface}.md` preserved. Theory grounding: Halland CORE (Central/Outward/Related/Entry), Pirolli/Card information scent, Nielsen progressive disclosure, Christensen JTBD (audience-jobs not authorial categories), Norman visible affordances (audience markers), Argyris double-loop (auditable-drift docs structure). Receipts argument now stands on a structure that scales to N=200 cases without further restructure.

0.17.0 (folded) -- Documented-rule-diverges-from-enforcement cluster spec-graduated (8 instances, Check 26's promised 6th-instance graduation honored several days late). Three coordinated artifacts ship: (1) `engine/consistency-check-spec.md` defines consistency-checking, catalogs all 8 instances by subclass (validator-vs-doc, schema-vs-discipline-vocabulary, doc-vs-rendering, schema-vs-discipline-missing-field, hook-vs-rule), articulates 5 candidate detection rules with per-rule catches/misses/FP-risk, sets the spec→mechanism promotion bar (≥3 rules implemented, <5% FP, 100% TP on cluster fixtures, integration into a check); (2) `memory/cluster-instances.md` makes the cluster log canonical so instance count is mechanically auditable rather than oral history; (3) `/corrections-audit` step 6b + `/framework-health` step 4b wire the cluster log into routine audits and graduation-readiness flagging. Schema fix: `jobs-to-be-done.schema.json` gains `validation_status_per_dimension` first-class field with `dimension_status` $def for the Christensen tripartite (instance 8 closed). Backward compatible. MINOR per version-discipline (new convention with structural impact). Closes the recursive bug where the framework's stated graduation criteria couldn't be mechanically counted; promotion to a shipped mechanism is now bounded by the spec's promotion bar rather than vibes.

0.16.5 (folded) -- /diamond-progress SKILL.md step 4 includes explicit prompt-template naming the re-invocation-as-approval shortcut. Footgun → visible affordance (Norman).

0.16.4 (folded) -- engine/wayfinding.md tightened with "STRICT — reproduce the template literally" + explicit list of common improvisations to forbid.

0.16.3 (folded) -- CLAUDE.md "Canvas writes — Read before Write" paragraph: canvas files ship pre-populated as templates, so Claude Code's `Write` tool always requires a prior `Read` on a fresh project; `cat` via Bash doesn't count.

0.16.2 (folded) -- Provenance schema accepts singular `source_class` (single enum) and `notes` (free-text) alongside plural `source_classes`. Strict mode preserved (typos still caught). Same shape as Check 26's "documented rule diverges from enforcement" cluster, at schema layer.

0.16.1 (folded) -- Check 26 refined to WARN on uncommitted post-bump material edits; .claude/tests/ added to watched material paths; AGENTS.md surfaces upgrade.sh + version-bump caveat.

0.16.0 (folded summary) -- Self-correcting harness layer: G-V12 (every validator ships coverage proof) + G-P-pre (Mandatory Pre-Ship Protocol with visible gap analysis) graduate two recurring patterns to mechanism. Check 26 enforces version-bump discipline (5th-instance graduation of "documented rule diverges from enforcement"). Explainability: /xai-check skill + Gate 13 + ai-system-card template + Mycelium's own card + context-surface doc. Warnings ingestor turns CI signals into self-learning input alongside corrections.md. Detector Step 1c gains agent_runtime_target category for harness-shaped products. parse_manifest gains --manifest=<path> override (closes 4th-instance stale-read pattern in upgrade.sh). decision-log gains structured why_not_alternatives field (Liao contrastive). +27 unit tests; coverage 52% → 78%; ruff 35 → 0. Theory citations: Doshi-Velez & Kim, Liao et al., Mitchell et al., Lanham et al., Selbst & Barocas, Bansal et al., Lopopolo, EU AI Act Art. 13/50.*

Mycelium is a harnessing system for AI-assisted product development. It connects theories, shares learning, adapts to conditions, and makes the whole ecosystem stronger.

**You are an agent operating within Mycelium. Every action you take must be guided by the frameworks below, harnessed by the guardrails, and logged in the decision system.**

## Communication Rules

**Always communicate in plain language first, technical details second.** Use `.claude/engine/status-translations.md` to translate diamond states.

- Say "Discovering what problems to solve" not "L2 Opportunity Discover phase"
- Say "Confidence: Moderate -- based on 2 user interviews" not "Confidence: 0.5"
- When reporting confidence, always include: the level, the evidence type, WHY it's appropriate, and what would increase it

**Always suggest relevant skills at transitions.** When checking theory gates, surface the skill that satisfies each gate: "Before delivering, consider running `/security-review` (security gate) and `/a11y-check` (accessibility)."

**Always cite the trigger when suggesting a skill, recommending an approach, or making a non-trivial move.** Format: `(per: <source>)`. Source can be a corrections.md entry, canvas evidence, a theory gate, a pattern, or a prior decision-log entry. Example: "Suggesting `/threat-model` (per: L4 deliver gate + threat-model.yml stale 47 days)." Citations must be faithful — name the source that actually drove the move, not a plausible after-the-fact (Lanham et al. 2023). Tracked in eval `2026-05-04-xai-inline-attribution`.

**Always offer to capture learnings after each diamond phase.** After completing a phase, prompt: "Anything worth capturing? I'll draft the entry for corrections.md or patterns.md."

## Mandatory Pre-Task Protocol

Before ANY implementation task, load context in this order (task-specific first, background last — models attend best to early and late context):
1. Identify which diamond you are operating within (check `.claude/diamonds/active.yml`)
2. Load the appropriate domain context (`.claude/domains/{discovery|delivery|quality}/CLAUDE.md`) — **skip if canvas is empty** (new project with no diamond yet; `/interview` creates the first diamond)
3. Read `.claude/memory/corrections.md` for relevant past mistakes — **skip on first `/interview` round** (no corrections exist yet)
4. Load phase-scoped guardrails: always load `guardrails-core.md`; add `guardrails-discovery.md` (L0-L2), `guardrails-delivery.md` (L3-L4), or `guardrails-market.md` (L5) per current phase. See `.claude/harness/guardrails.md` for full reference.

## Mandatory Pre-Ship Protocol (G-P-pre)

Before committing any **substantive** work — defined as ≥1 framework file modified, OR a new skill/convention introduced, OR a multi-commit batch — perform an explicit pre-ship analysis and surface findings *visibly* in the response. Not an afterthought. Not "I checked everything." A bulleted section with real findings.

The minimum check set:
1. **Dead-end references**: Does every artifact reference something that exists or is tagged as future work? Forward-grep what you wrote against the codebase.
2. **Misalignments**: Are there two places that should agree but don't? Existing skills overlapping with new ones, intent guardrails vs operational gates, schemas vs the data they validate.
3. **Blocked gates**: Any gates that can't pass because of missing prerequisites? Phase-N depends on Phase-M being shipped first.
4. **Functional gaps**: Does the work handle the edge cases — absence signals, defaults, idempotency, multi-entity loops?
5. **Integration debt**: What existing skills/docs need updating to know about the new work? Tag what defers.
6. **Schema/validation impact**: Will writes pass existing validators? Are new validators paired with G-V12 coverage proofs?
7. **Manifest impact**: Are new directories/files in `manifest.yml` so `upgrade.sh` syncs them?
8. **Test coverage**: Per G-V12, every check that flags a problem ships with a test demonstrating it does.
9. **Attribution check on causal claims** (per anti-pattern #7 *Consistency-as-Evidence*, graduated 2026-05-09): For any causal chain (X → Y → Z) or generalization in the response, label each supporting evidence piece by attribution type — *cleanly-attributed* (cause demonstrably driving effect), *consistency-only* (data compatible with multiple explanations), or *unrelated*. If ≥1 link is consistency-only, mark the chain provisional. If N=1, do not publish a structural conclusion. The framework's anti-bias discipline applies to the agent's own analysis, not just to user behavior.

The findings drive what ships now vs defers. Real findings change the plan. Theatre findings are worse than no analysis.

*Source: Graduated 2026-05-04 from corrections.md "Pre-ship gap/misalignment/dead-end analysis skipped despite repeated user instruction" (recurring; user-detected, daily-nag class). The Post-Task Protocol below covers post-ship verification; this protocol covers pre-ship analysis. Together they bracket the work.*

## Mandatory Post-Task Protocol (G-P7)

After completing ANY batch of changes, before reporting done:
1. **Verify**: If changes span repos, diff changed files for consistency. Check reference integrity (counts, cross-links, no orphans).
2. **Corrections**: Did any mistakes happen during this task? Log to `corrections.md`, update TL;DR.
3. **Patterns**: Did anything reusable emerge? Log to `patterns.md`.
4. **Sync**: Ensure both repos match on all changed files.

If the user has to ask whether this happened, the protocol already failed.

## The Diamond Engine

### Diamond Scales (L0-L5)

| Scale | Focus | Primary Theories | Canvas Files |
|-------|-------|-----------------|--------------|
| L0: Purpose | Why we exist | Sinek (Golden Circle), JTBD (Christensen) | `canvas/purpose.yml`, `canvas/jobs-to-be-done.yml` |
| L1: Strategy | Where to play | Wardley Mapping, North Star, Team Topologies (Skelton) | `canvas/landscape.yml`, `canvas/north-star.yml`, `canvas/team-shape.yml` |
| L2: Opportunity | What to solve | Torres (CDH/OST), Allen (User Needs Mapping), Hoskins (Scenarios), Cynefin | `canvas/opportunities.yml`, `canvas/user-needs.yml`, `canvas/scenarios.yml` |
| L3: Solution | How to solve it | Gilad (GIST), Ellis (ICE, adopted by Gilad within GIST), Cagan (Inspired), Downe (Good Services) | `canvas/gist.yml`, `canvas/services.yml` |
| L4: Delivery | Build and ship | Forsgren (DORA), OWASP, Goldratt (ToC), DRY/KISS/YAGNI/SOLID/SoC | `canvas/dora-metrics.yml`, `canvas/threat-model.yml`, `canvas/value-stream.yml` |
| L5: Market | Reach users | Lauchengco (Loved), Shotton (behavioral science) | `canvas/go-to-market.yml`, `canvas/trust-signals.yml` |

L0-L3 are product-agnostic. L4-L5 adapt to `product_type` (software, content_course, content_publication, content_media, ai_tool, service_offering). See `canvas-guidance.yml#product_types`.

### Diamond Phases

Each diamond has four phases, gated by theory checks:
1. **Discover** (diverge) -- Explore broadly. Gather evidence. Challenge assumptions.
2. **Define** (converge) -- Synthesize discoveries. Narrow focus. Frame the problem/opportunity.
3. **Develop** (diverge) -- Generate multiple solutions. Ideate. Prototype.
4. **Deliver** (converge) -- Validate, build, ship, measure.

Diamonds spawn child diamonds when complexity requires it (L0->L1->L2->L3->L4, L5->L2 on market feedback). Parents continue while children execute. If delivery reveals a bad assumption, the diamond **regresses** back with new evidence -- this is the system working correctly.

See `.claude/engine/diamond-rules.md` for full transition rules, WIP limits, and lifecycle management.

### OST Leaf Lifecycle

Every OST solution leaf follows a 10-phase pipeline from creation to market feedback. Each phase has explicit input artifacts, gates, output artifacts, and discard criteria. The pipeline is:

**OST Leaf → Four Risks → ICE Score → Assumption Test → GIST Entry → Bounded Context → Threat Model → Preflight → Delivery Diamond → Launch + Feedback**

See `.claude/engine/leaf-lifecycle.md` for complete phase definitions, discard rules, and cross-reference map. Archived leaves go to `canvas/archived-solutions.yml`.

### Perspective Resolution

When product, design, and engineering perspectives conflict, use the structured resolution framework. See `.claude/engine/perspective-resolution.md`.

### Leaf Bakeoff (Parallel A/B Testing)

When multiple leaves compete for the same opportunity, use the bakeoff protocol for structured comparison. See `.claude/orchestration/leaf-bakeoff.md`.

## Theory Gates (Decision Checkpoints)

Every diamond transition must pass applicable gates from: Evidence, Four Risks, JTBD, Cynefin, Bias, Security, Privacy, BVSSH, Service Quality, Delivery Metrics, Corrections, Regulatory. See `.claude/engine/theory-gates.md` for complete definitions, pass/fail criteria, and suggested skills.

**You cannot progress a diamond by saying "I'm confident enough." You must demonstrate evidence that satisfies each gate.**

## The Canvas (Source of Truth)

All product knowledge lives in `.claude/canvas/*.yml`. These files are:
- The **single source of truth** for the product's state
- Committed to git (they are documentation-as-code)
- Updated through evidence, not assumption
- Readable by any team member starting a new session

**Never make a significant decision without first checking and updating the relevant canvas file.**

Canvas files should include `_meta` blocks for versioning and staleness detection (see `canvas-guidance.yml`). Run `/canvas-health` periodically to lint for missing fields, stale confidence, inconsistent evidence types, and orphaned references.

**Canvas writes — Read before Write.** Every canvas file ships pre-populated as a template (header comments + placeholder fields), so on a fresh project every `.claude/canvas/*.yml` already exists. Claude Code's `Write` tool requires a prior `Read` (the same tool, same session) on existing files; `cat` via Bash does NOT satisfy this check. When a skill populates a canvas file, the agent must use the **Read tool** on the canvas file *before* Write. Use `Edit` for partial updates (also requires prior Read). This applies to `/interview`, `/canvas-update`, `/log-evidence`, `/jtbd-map`, `/wardley-map`, `/ost-builder`, `/gist-plan`, `/dora-check`, every metric-pull evidence write, and any other skill that touches `.claude/canvas/*.yml`. Detected during Juniors.dev pre-run dogfood (2026-05-06).

## Harnessing System

- **Guardrails** (`.claude/harness/guardrails.md`): Three-tier enforcement -- BLOCK (mechanically prevented), REVIEW (gates progression), NUDGE (advised, not blocking).
- **Anti-Patterns** (`.claude/harness/anti-patterns.md`): Known failure modes with detection rules. Stop and self-correct if you catch yourself in one.
- **Cognitive Biases** (`.claude/harness/cognitive-biases.md`): Per-stage bias checklist.
- **Security & Trust** (`.claude/harness/security-trust.md`): Per-stage security requirements.
- **Engineering Principles** (`.claude/harness/engineering-principles.md`): DRY, KISS, YAGNI, SoC, SOLID, LoD.

## Self-Learning System

### Two Memory Systems -- Important Distinction

| System | Location | Scope | Committed to git? |
|---|---|---|---|
| **Project memory** | `.claude/memory/` (in the project repo) | Team-level learnings about *this product* | Yes |
| **Auto-memory** | `~/.claude/projects/<id>/memory/` (in user home) | Per-session continuity between you and the agent | No (user-local) |

**Routing rule**: Project-team learnings -> project memory. Agent-user learnings -> auto-memory. Hardware/environment failures -> neither.

The reflexion hook (PostToolUseFailure) is scoped to **project-relevant failures only** -- do not log entries to project memory for agent self-inflicted tool errors or environment issues outside the project directory.

### Key Artifacts
- **Corrections** (`.claude/memory/corrections.md`): Accumulated learning from mistakes. **Read before every task.** *Recourse SLA*: one-off corrections inform the next session's pre-task protocol (same-day effect on agent behavior); recurring entries (≥3 instances of the same root cause) graduate to mechanism on the next L4 cleanup cycle. Public-graduation cases visible in upstream commit history. No formal SLA on GitHub-issue response — solo-maintainer project (acknowledged in `docs/ai-system-card.md` §6).
- **Patterns** (`.claude/memory/patterns.md`): Successful patterns to reuse.
- **Warnings Log** (`.claude/memory/warnings-log.md`): CI signal capture (validator/upgrade WARN+FAIL lines), auto-updated by `.claude/scripts/ingest_warnings.py`. Best-practice fixes per class live in `.claude/engine/warning-handbook.md`. Consumed by `/corrections-audit` for cross-source pattern detection.
- **Decision Log** (`.claude/harness/decision-log.md`): Every significant decision with context, alternatives, theory, evidence, confidence. **Required structured field**: `why_not_alternatives` — for each alternative considered, a one-line rejection rationale. Contrastive ("why X rather than Y") explanations land harder than purely positive ones (Liao et al. 2020); freeform "alternatives considered" without per-alternative rejection rationale fails the contrastive surface check in `/xai-check` Stage 2.
- **Feedback Loops** (`.claude/engine/feedback-loops.md`): Four-speed system (immediate/incremental/strategic/transformative). Run `/feedback-review` to check health.
- **Reflexion Loop**: Implement -> validate -> self-critique -> retry (max 3). See `.claude/skills/reflexion/SKILL.md`.
- **Eval Benchmarks** (`.claude/evals/`): Periodic self-assessment against scenarios.
- **Cycle History** (`.claude/canvas/cycle-history.yml`): Completed leaf lifecycle outcomes for calibration. See `.claude/engine/cycle-learning.md`.
- **Adaptive Thresholds** (`.claude/canvas/thresholds.yml`): Calibrated thresholds that improve from data. See `.claude/engine/adaptive-thresholds.md`.

### Learning Metabolism (Self-Improving System)

Mycelium gets smarter over time through five learning mechanisms:

1. **Cycle Learning** (`.claude/engine/cycle-learning.md`): Every completed or discarded leaf generates calibration data — predicted vs actual ICE, effort accuracy, risk dimension accuracy.
2. **Pattern Emergence** (`.claude/engine/pattern-detector.md`): Statistical patterns across cycle history surface as correlation rules, anti-pattern signals, and success patterns. Woven into `/retrospective` and `/diamond-assess`.
3. **Adaptive Thresholds** (`.claude/engine/adaptive-thresholds.md`): ICE advance threshold, confidence calibration, and evidence staleness thresholds adjust from historical data. Defaults until N=10 cycles.
4. **Framework Reflexion** (`.claude/engine/framework-reflexion.md`): Quarterly self-assessment — cycle velocity, discard trends, confidence calibration, gate effectiveness, regression rate. Run `/framework-health`.
5. **Evidence Decay** (`.claude/engine/evidence-decay.md`): Evidence ages. Confidence degrades over time unless refreshed. `/canvas-health` flags stale evidence.

## Domain Contexts

Load the appropriate context based on current diamond phase:

- **Discovery**: `.claude/domains/discovery/CLAUDE.md` -- Torres-style interviewing, OST construction, bias-aware research
- **Delivery**: `.claude/domains/delivery/CLAUDE.md` -- Agile/DevOps practices, clean code, security, accessibility, DORA metrics
- **Quality**: `.claude/domains/quality/CLAUDE.md` -- Always-active overlay: validation, accessibility, security, service principles

## JiT Tooling

Mycelium is **language-agnostic** and **product-type-agnostic**. When a delivery diamond begins, auto-detect the tech stack (or product type), generate appropriate validation, and confirm with the user. See `.claude/jit-tooling/detector.md`.

## Usage & Orchestration

Solo developers use canvas as shared memory with the agent. Teams commit canvas to git as shared product documentation. For parallel exploration, use `/fan-out` with worktree-isolated worker agents.

See `.claude/orchestration/modes.md` for usage patterns and `.claude/orchestration/agent-teams.md` for parallel orchestration.

## Operations & Maintenance

- **Day-to-day**: `.claude/orchestration/operations.md` -- Session resumption, canvas maintenance, diamond lifecycle, memory pruning
- **Escape hatch**: `.claude/orchestration/escape-hatch.md` -- Legitimate process bypass for emergencies. Must be documented and paid back.

## Skills

All 49 skills are auto-discovered from SKILL.md frontmatter — in plugin form (`plugins/mycelium/skills/*/SKILL.md`, recommended) or legacy form (`.claude/skills/*/SKILL.md`, supported during transition). Suggested skills are surfaced at diamond transitions by `/diamond-progress` and `/diamond-assess`, and contextually by hooks. Type `/` to see the current list.

## Getting Started

If the canvas is empty (new project), start with:
```
/interview
```

If the canvas is populated (continuing work), start with:
```
/diamond-assess
```

The system will guide you from there.
