# Mycelium: Theory-Guided Agentic Product Development

*Version 0.197.1 -- **The approved text of a message is not an unlogged send** (PATCH). check_contact_recorded_as_prose fired on `message_approved_2026_09_04` (the wording of eleven cold DMs, dated by approval) the moment its task reopened; second classified false-positive class, skipped only when `message` is the sole contact word and the name says approved/draft/template/wording. Prior: Version 0.197.0 -- **A pre-registered read that falls due is said out loud, a fired closing-path proposal has a slot for its ruling, and a prose rolls_up_to is named** (MINOR). Dogfood 2026-09-10/11: two 48-hour reads registered in `read_dates` were skipped by five sessions because only `horizon` was watched; `scripts/check_reads_due.py` and the closing path now print READ DUE, session start relays it, the advisory ledger and next-item know it. The first CLOSING PATH FIRED was ruled "does not choose a path" with nowhere to write it: `ruling:` and `ruled_at:` under a fired entry now survive re-derivation. L1 read "0 of 0 live leaves" for two days because its `rolls_up_to` was prose that keyed to nothing; the summary now names that and the fix. Prior: Version 0.196.3 -- **The changelog claims only what shipped, third time** (PATCH). 0.196.0 and 0.196.1 never released; folded into 0.196.2. Docs only. Prior: Version 0.196.2 -- **The autonomous guard's new paths are covered in-process** (PATCH). 0.196.1 never released: the CI-only per-file coverage floor read autonomous_evidence_guard.py at 69%, because its YAML walk, post-edit scan, Bash branch and un-declare guard were exercised only by the bash suites the floor does not count. Ten in-process tests take it to 95%. No behaviour change. Prior: Version 0.196.1 -- **The sourced hook library finds its helper by its own location** (PATCH). 0.196.0 never released: the shell gates' library resolved `_hook_input.py` only through CLAUDE_PLUGIN_ROOT, which a Claude Code session exports into every local shell and CI never sets, so every gate suite was green here and red there (15 of 19 in the brownfield suite). The library now resolves its sibling first, and scripts/gates.sh unsets the variable so local runs match CI. No behaviour change in a real session. Prior: Version 0.196.0 -- **Six blocking hooks, one reading of the tool call** (MINOR). A blind adversarial pass (fresh agent, no session context, white box plus a sandbox; dogfood evals/security/2026-09-11) got past all six blocking hooks with the same moves: `..`, symlinks and case on a case-insensitive disk; `file`/`path`/`notebook_path` keys nobody read; MultiEdit edits nobody scanned; Bash with `./`, `$PWD`, absolute paths, newlines, `cd`, and writers the regex did not know; a list where a string belonged, crashing the helper into exit 1, which is an allow; every off-switch file writable by the agent; YAML in nine spellings against a line-anchored regex. Now: `scripts/_hook_input.py` is the one reading (paths resolved and case-folded, all keys, edits included, Bash scanned, bad input refused), guard-state writes return `ask` so the person decides, helpers fail closed, the autonomous guard parses YAML and scans the file as it would be after the edit, the framework guard denies when active with no or an empty manifest, all six hooks are on Bash, NotebookEdit and the filesystem MCP in all three manifests, and `tests/bash/test_hooks_adversarial.sh` replays 92 of the pass's cases. Prior: Version 0.195.0 -- **The different-builder test is a contract rule with a check** (MINOR). Founder rulings 2026-09-11 on the cluster generalising-from-the-dogfood-instance, whose home opp-065 was discarded 09-09: the home is now a rule in engine/agent-operating-contract.md (write the scenario for a different builder on a different product, not a label), and check_upstream_candidates.py refuses under --strict a candidate surfaced on or after 2026-09-11 without a `different_builder:` sentence; earlier candidates are not backfilled. Prior: Version 0.194.2 -- **The changelog claims only what shipped, again** (PATCH). The 0.194.1 release job failed its documented-version backstop on the never-released 0.194.0; folded into 0.194.1, as 0.192.0 was into 0.192.1. Docs only. Prior: Version 0.194.1 -- **The two new gates resolve like the old ones** (PATCH). 0.194.0 never released: on CI, which has no uv, `scripts/gates.sh` reported uvx MISSING and the gates-wrapper suite failed, and the validator's 200-line diagnostic cap showed thirteen green suites and not the red one. The auditors now resolve pip-installed binary first, uvx second, MISSING third, and only when their inputs exist; the validator prints the failing suite's context before the capped dump; a shellcheck directive the newer pinned shellcheck rejects is rewritten. Prior: Version 0.194.0 -- **Supply chain and logs: the LOW findings from DL-1262, fixed to the standard the review named** (MINOR). Actions pinned to commit SHAs with the tag beside them; the validate job runs with `contents: read`; checkouts drop credential persistence except the release job, which pushes tags and says so in a scoped zizmor ignore; requirements-ci.txt pinned exactly, with pip-audit and zizmor pinned in it so the auditors are audited; both run in CI and in scripts/gates.sh; the discovery-trigger log keeps a digest and length of the matching sentence and no longer any of the prompt; the system card states that marketplace distribution is unsigned and what stands in for signing. Prior: Version 0.193.0 -- **The plugin reviewed as the product: three fixes from DL-1262** (MINOR). The first security review run on the framework itself found two MEDIUMs on this week's code and one thin margin: canvas prose reached the agent unwrapped through the NEXT ITEM line and the research guard's excerpts (LLM01); the framework guard allowed on a malformed state file, which sits inside the agent's own write set (LLM06); the six blocking PreToolUse hooks carried the same 5-second timeout that cancelled SessionStart 32 times this month, at a measured 1.3x margin. Now: fired proposals and research excerpts are wrapped and escaped like task objectives, the systemMessage form is plain and bounded (`render_human`, `text_human` in state), a state file that exists and cannot be parsed DENIES with a reason while absent and `active: false` still allow, and the blocking hooks run at 30 s in all three manifests. Prior: Version 0.192.2 -- **The changelog claims only what shipped** (PATCH). The 0.192.1 release job failed its own backstop because the changelog documented a v0.192.0 that never released; the never-released section is folded into 0.192.1 rather than released by manual override of a red validator. Docs only. Prior: Version 0.192.1 -- **The mutation runner meets the coverage floor it exists beside** (PATCH). 0.192.0 was blocked at release by the CI-only per-file coverage floor: the runner's own test drove the mutation core in-process and left main, the pytest runner and the tokenizer fallback at exactly 70%. Four end-to-end tests on a throwaway module take it to 95%. No behaviour change. Prior: Version 0.192.0 -- **A test that names a mechanism, and something that makes the naming true** (MINOR). The founder asked what share of the tests are slop; a structural classifier said 3% and a mutation pass said 24% of sampled code mutants survive. `scripts/mutation_sample.py` is the counter-metric `engine/feedback-loops.md` named for test coverage and never computed: code-token mutations only, bytecode caching disabled (a same-size mutant restored within one second ran stale bytecode and read as a survivor, 52% to 76% on the same sample once fixed), survivors listed by line. Eleven killing tests across ten check suites: exit codes on clean paths, OR-predicates branch by branch, `<` versus `<=` at the boundary, junk marker entries. framework-health 4h runs it. Prior: Version 0.191.0 -- **The channel ledger gets a reader before L5** (MINOR). The founder read go-to-market.yml on a stale-stamp flag and named the cause: \"another gap in routing and wiring.\" Evidence held: the schema declared 2 of 14 sections, the only reader of the file was /launch-tier at L5, /handoff named channels without opening the ledger (2026-08-25: a full acquisition session never retrieved 18 of 24 channels already ruled on), and competitor evidence had two homes. Three surface-registry rows (channel-evidence read by /handoff and /launch-tier; positioning-candidate; competitor-evidence authoritative in landscape.yml, go-to-market a pointer), /handoff step 1b reads the channel map and states what worked before naming a channel, the schema declares every section the skills write. /diamond-assess step 6c reads the market canvas at L1 (reachability) and L2 (channel outcomes), so the L5 -> L2 loop runs on every assessment and not once per launch; the README chart carries both arrows. check_surface_registry enforces the reader claims on every push. Prior: Version 0.190.1 -- **Theory doc: three wording fixes from the 2026-09-10 fidelity audit** (PATCH). Team Topologies row says what the schema does (the `type` key is required, null permitted); serial position gets its Tier 3 line because the Stop hook cited it and the doc did not; Nielsen's heuristics dated as 1994 with definitions refined 2020. No mechanism changed. Prior: Version 0.190.0 -- **Attribution label: a-judgement-the-check-can-read-2026-09-10** (MINOR). The first advisory block the dogfood agent received through the harness (DL-1253) carried four lines it ruled false positives on a read, and none of the four checks had a way to record that: a bug description flagged as a bare \"today\", a person flagged as not a person because his quote said \"subagent\", eleven provenance blocks whose classes are listed as a set, and an inbound that asked nothing. Each would fire at every start until its check learned a marker, and the advisory ledger (0.184.0) cannot see a judged-and-left case because it never clears. `engine/canvas-guidance.yml#reviewed_markers` now names one convention, content in the value and never in the key name, every marker dated with a reason: `stale_prose_reviewed` (check_stale_prose), `handles_checked` (check_source_authenticity), `alignment_reviewed` and `label_reviewed` (check_source_class_fidelity, where a reviewed unequal block is reported as coverage and a repeating reason is called out as a missing rule), and `reply_not_owed` or `owed: false` on a touch (check_reply_owed, dated so a later inbound still fires). One test per marker. Prior: Version 0.189.0 -- **Attribution label: did-it-run-is-the-first-sli-2026-09-10** (MINOR). The dogfood /dora-check of 2026-09-10 read reliability as \"2 of 3 SLOs at 100%\" through the month in which the plugin's SessionStart hook was cancelled on 32 of 33 starts (DL-1247, DL-1254): the SLOs measured the framework's checks running on its own tree, and none measured whether the contract reached the agent. `/dora-check` Part 3 now requires a delivery SLI for plugin-form products, `rel-delivery`: contract-delivering hook success rate on the consumer's own transcripts, >= 99% over 30 days, measured by `check_hook_delivery.py` (0.185.0), read with DORA's bands (one cancelled run in the window is low, because that session ran with no rules and the harness discards a cancelled hook's output), `unmeasured` rendered distinctly from elite, and, when low, the DORA bottleneck regardless of the four delivery metrics. Skill text only; the dogfood canvas carries the first instance as rel-004. Prior: Version 0.188.1 -- **Attribution label: two-things-the-first-real-prompt-said-2026-09-10** (PATCH). Read off the first background block preflight delivered in the real harness, minutes after 0.186.0's split went live. (1) The async tier obeyed the 25 s in-hook budget and skipped a check (\"28s against 25s; skipped external-evidence-ratio\") although nobody was waiting and async hooks carry no manifest timeout; it now runs to a 600 s ceiling unless MYCELIUM_SESSION_START_BUDGET is set. (2) The advisory ledger keyed settles on session_id, and a resumed session keeps the id of the start it resumes, so every resume read \"same session seen twice; not settled\"; the unit the ledger settles is a START, and the key is now session_id plus the start's epoch, in the hook and in preflight's delivery. One bash assertion pair added. Prior: Version 0.188.0 -- **Attribution label: one-item-per-boundary-with-its-verb-attached-2026-09-10** (MINOR). opp-006 sol-006f, the third of the three leaves the dogfood founder ruled on 2026-09-10. The session-start hook emitted every advisory in one block to the agent; the human saw nothing, and the founder's cause statement was that a novice cannot ask for a step he does not know exists. `scripts/next_item.py` picks ONE item from the assembled block: a parked CLOSING PATH FIRED proposal first, then a muted advisory awaiting a ruling, then the oldest-firing advisory that has a mapped command (27 advisory ids mapped to the skill that does the thing), skipping anything ruled drop or snoozed. It renders one line carrying the exact command and the fixed verb set (run / rule / snooze-until DATE / drop), prepended to the agent block on every start and, on resume and fork, also sent as a `systemMessage`, the channel a human sees, which is the re-entry moment the record names. `hooks/next-item-repeat.sh` on Stop repeats it ONCE if nothing was ruled since, then stays quiet: one repeat, not one per turn. The ledger gains a `snooze` ruling with `--until`. Silent when nothing qualifies. Six Python tests, one bash test (startup, resume systemMessage, Stop once then silent, ruling silences the repeat, override, nothing qualifying). Prior: Version 0.187.0 -- **Attribution label: the-closing-path-is-stored-where-it-fires-2026-09-10** (MINOR). opp-006 sol-006e, the second of the three leaves the dogfood founder ruled on 2026-09-10. 0.183.0 derived what would close a diamond's phase and printed it; nothing stored it, and the founder asked the same evening \"did you log what would close define so that when it happens it actually does so?\" It had not been. `derive_closing_path.py --write` now stores the derivation on the diamond as `closes_on` (gates, watched inputs, what fired, stored_at), stamps `reader` on the assumptions and `on_close` on the tasks it names where absent, and preserves hand-written sub-keys (`routes_on_record`, `then_do`, `caveat`) across re-derivation; `--all --write` runs it for every diamond at session start (heavy tier) so a stored condition is never older than the last start. What it ACTS on is deliberately narrow: it stores, it stamps, and it detects that a named input has landed (an assumption got a verdict, a task closed), which it reports once as a `CLOSING PATH FIRED` proposal naming the next command; it never flips a gate, moves confidence or chooses a path. Hand-named routes in a sibling `*closes_on` field are watched too, which is how an outcome-rooted tree the script cannot link still fires. Text-level writes that touch no other line; scalars JSON-quoted after a bare `-` broke the first dry run. Nine in-process tests. Prior: Version 0.186.0 -- **Attribution label: the-contract-now-the-heavy-checks-at-your-next-prompt-2026-09-10** (MINOR). Founder, on 0.185.0: \"waiting for 1 minute is a long time\", and then: every hook must be evaluated for whether it can or should run async. Measured first: the 18 s were 39 Python steps re-parsing a 5 MB canvas, not interpreter start-up (1.2 s). Claude Code's `async: true` runs a command hook without blocking or timeout but its output is not documented as delivered, so the rule for the whole table is: anything that blocks or injects context stays synchronous (the six PreToolUse gates, the four PreToolUse guards, Stop's next-action check, everything with additionalContext, all already 65 to 430 ms); pure side effects go async (the three PostToolUse loggers, on every Read, Bash and Edit). SessionStart is split: `session-start.sh --fast` delivers the operating contract and the cheap checks now; `--async` runs the eleven heavy checks in the background into `.claude/state/session-checks.json` keyed by a canvas fingerprint; `preflight.sh` (UserPromptSubmit, whose stdout is documented as added to context) delivers that block once at the next prompt and settles the advisory ledger then, never on the partial block; and an unchanged canvas within the cache TTL (12 h) is served inline at the next start. No flag means the full run, which is what the Codex and Cursor manifests, the tests and a hand run get. Measured on the dogfood canvas: fast tier 3 s without a cache and 3 s with one, async tier 18 s in the background, preflight delivery 0.1 s. One bash test walks the whole cycle (fast, async, delivered once, served from cache, invalidated by a canvas edit, full mode). Prior: Version 0.185.0 -- **Attribution label: the-hook-that-delivers-the-contract-was-being-cancelled-2026-09-10** (MINOR). The worst finding in the project's record, read off the harness's own transcripts the morning after 0.184.0 shipped a ledger that could not record because the hook writing it never finished. `hooks/hooks.json` had carried `\"timeout\": 5` on the SessionStart hook since the 2026-05-09 plugin migration, while the hook grew to some twenty checks and ~18 s on the dogfood canvas; Claude Code's own default for a command hook is 600 s. 32 of 33 session starts between 2026-08-09 and 2026-09-10 were `hook_cancelled` at 5.0 s, and a cancelled hook's stdout is discarded: no operating contract, no advisory, reached any agent on that repo for a month, while every test of the hook stayed green, because tests run the script and not the harness. Three defences. (1) Manifest timeout 60 s on all three runtime manifests. (2) An in-hook deadline: after MYCELIUM_SESSION_START_BUDGET seconds (default 25) the hook stops starting the ten optional script-backed checks, names what it skipped as having run NOWHERE this session, and still emits; the contract file is read last and costs nothing, so it is never the thing that gets cut; a budget line appears whenever a run passes half the manifest timeout. (3) `scripts/check_hook_delivery.py`, run at session start and in canvas-health 8c(h): reads the project's transcripts for hook outcome attachments and reports cancellations per hook, FAIL when the contract-delivering hook was cancelled, with the manifest timeout named. Also removes the `$schema` key Claude Code warns about in hooks.json. Five Python tests, one bash test (zero budget skips and still delivers the contract; a fixture run finishes inside the manifest timeout). Dogfood consequence, recorded there: every 'the session-start hook told me' claim since early August is suspect, and the 'advisories fired daily for 32 and 101 days' finding behind 0.184.0 was an artefact of running the hook by hand. Prior: Version 0.184.0 -- **Attribution label: every-advisory-counts-whether-anything-follows-2026-09-10** (MINOR). opp-006 sol-006d, the first of three leaves the dogfood founder ruled on 2026-09-10 after a five-sweep discovery on the cause he named (\"inexperienced builders can't be asking for tasks they don't know should be happening\"). The session-start hook computed some twenty advisories and nothing recorded whether any was ever followed; two had fired daily for 32 and 101 days, and BVSSH rated Measurement amber for the third consecutive assessment on exactly that. `scripts/advisory_ledger.py` now sits between the assembled reminders and the emit: it records which advisories are present (a signature registry with a count where the text carries one), settles the previous session's as cleared or still_firing (the condition cleared by the next start, or did not; the k8s reading of \"did the remedy happen\", needing no per-check detector), and replaces any advisory that has fired on seven distinct days with one line asking for a ruling (keep, fix, drop). Muting is a request for a ruling, not a deletion; the report keeps counting. `canvas-health` 8c(g) prints the per-advisory table so Measurement can cite a number. Fail-open by design and said so: on any failure the script echoes its input plus one spoken line, so the hook never goes quiet because its ledger did; a corrupt ledger line is named and skipped, never discarded. Honest about its two blind spots: a \"read it and leave it\" remedy never clears (the check must honour a reviewed marker), and an awareness-only line is not counted. Person override `MYCELIUM_ADVISORY_LEDGER=off`. Surface-registry row `advisory-response`. Fifteen in-process Python tests, one hook-level bash test on happy, off and corrupt paths. Prior: Version 0.183.1 -- **Attribution label: the-closing-path-reads-an-outcome-rooted-tree-and-the-hook-stops-shouting-2026-09-09** (PATCH). Two defects found by running 0.183.0 on the repo that asked for it, the same evening it shipped. (1) `derive_closing_path.py` matched opportunities to a diamond only by diamond id inside link keys; on a tree rooted on outcomes (every opportunity `rolls_up_to: adoption`, per the dogfood one-root ruling) it read 0 open opportunities and 0 of 0 leaves for every diamond, so the leaf half of the closing path was empty by construction. It now also matches an opportunity whose `rolls_up_to` names an outcome the diamond itself rolls up to (top level or under `definition_of_done`), and when nothing links by id or by outcome it says so in one line, so an empty leaf table is distinguishable from a tree it cannot read. A diamond that rolls up to another diamond, not an outcome, still reads empty, and the line now names that. (2) `hooks/session-start.sh` CHECK 5 embeds Python in a double-quoted `-c` string and four comment lines carried markdown backticks, which bash command-substitutes; every SessionStart wrote five `command not found` lines to stderr, harmless to the injected context and invisible in Claude Code. Backticks removed; `tests/bash/test_session_start_stderr_clean.sh` asserts the whole stderr is empty on a populated and an empty project, so the next backtick fails a test. Three new Python tests, two bash. Prior: Version 0.183.0 -- **Attribution label: the-framework-says-what-is-runnable-and-what-nothing-reads-2026-09-09** (MINOR). Three shapes of one gap, named by the dogfood founder in one day and filed with probes reading absent: the framework recorded and reported after the fact, and the human had to know what to ask. (1) `derive_closing_path.py`, printed by `/diamond-assess` under a Closing path heading: for each gate not at pass, what would flip it, who owns that and the date if the record holds one, read off `theory_gates_status`, the leaves under the opportunities that cite the diamond (assumptions with no verdict, and whether a test is named), the open tasks with their horizons, a `four_risks` status stale against reviewed blocks, and rulings already asked. (2) `check_idle_opportunities.py`, canvas-health 8c(f) and session-start: an open node with no open task naming it, no leaf carrying a live assumption with a named test, and no `what_would_move_it` is IDLE, WARN tier, oldest first; measured 50 of 66 the day it was written, and four of seven discards had to be withdrawn because silence was routing. (3) `runs_on` on the assumption-test header (disk, network, human) and a RUNNABLE NOW, NEVER RUN advisory in `check_instrument_contract.py`, repeated at session start: a test an agent can run from disk is run in the session that designs it or dated within 14 days; measured, a zero-build retrodiction designed 2026-06-12 ran 89 days later in an hour once someone asked why wait. Nothing here scores, flips or moves confidence; the reading stays pre-registered and only the surfacing is automatic. 22 in-process tests; wiring and fail-open clean. Prior: Version 0.182.1 -- **Attribution label: the-tests-ran-the-script-where-coverage-could-not-see-it-2026-09-09** (PATCH). Corrects 0.182.0, which never released: CI's per-file coverage floor read `check_evidence_landing.py` at 0% because its ten tests ran the script as a subprocess, and coverage only counts what the test process imports. The tests now import the module and call `main()` in-process; the file reads 94%. Nothing else changes. The local validator waives the coverage floor for its 192-second cost, which is why 55 of 55 passed here and CI did not; the gate-set parity check compares script names, not waivers, and this is the second release in two days to reach origin red on a gate that runs only in CI. Prior: Version 0.182.0 -- **Attribution label: every-source-either-lands-or-is-an-orphan-2026-09-09** (MINOR). `check_evidence_landed.py` covered one quadrant: a closed task whose declared destination its findings never reached. `check_evidence_landing.py` covers every evidence-bearing source, tasks with inbound touches or findings, assumption-test and report files, user needs: either a reader canvas cites it or it is an ORPHAN, with CLAIMED_NOT_LANDED as the sharpest class (a write that reported success) and REVIEWED for sources a human judged not citable, with a reason, in a ledger. It fails only on regression against a baseline the project commits with `--write-baseline`, so a brownfield canvas is told its backlog and gated on not adding to it rather than failed on day one. Measured where it was born, the dogfood canvas: a three-reader sweep on 2026-09-06 had found 49 of 98 human tasks cited nowhere; the check adopted there the same day read 63 orphans, and 0 three days later, 233 sources, zero regressions. Shipped in the gate set, so pre-push, gates.sh and CI run it alike. N/A speaks: no canvas prints so and exits 0. Eight fixture tests. Also: the post-write nudge on go-to-market.yml and landscape.yml now says that a source written there has NOT been scanned for opportunities until the OST builder is run over it; the visible unscanned state per source is the half not shipped, named in the changelog. Prior: Version 0.181.0 -- **Attribution label: a-framework-turn-ends-on-one-next-action-2026-09-08** (MINOR). Downe P10, "have no dead ends", was rated fail on the dogfood canvas from 2026-05-31, when a cohort tester named the stall moment: the framework goes quiet and nothing says what to run next. v0.31.1 fixed one such moment. This is the general case, with teeth: operating-contract rule 12 says any turn in which a Mycelium skill ran ends with exactly one line beginning `Next:`, and `hooks/next-action-check.sh` blocks the turn end on Stop until that line exists, reading `last_assistant_message` because the transcript can lag the current turn. Silent on turns with no framework skill run; honours `stop_hook_active`; says so in a systemMessage when it cannot read its input or the transcript instead of failing silently; person override `MYCELIUM_NEXT_ACTION_CHECK=off`. Check 55 verifies the rule, the blocking hook and all three runtime manifests agree, so no part can lapse alone. Gate blocks are not yet in scope and are named as the next change. Dogfood side: opp-006, sol-006b built with sol-006c as the control, and P10 stays fail until a stall moment is observed to end in a next action. Prior: Version 0.180.6 -- **Attribution label: the-system-card-was-left-behind-by-the-last-bump-2026-09-08** (PATCH). Corrects 0.180.5, which never released: its validator run failed Check 40 because `docs/ai-system-card.md` still carried 0.180.4. The sync script lives at `plugins/mycelium/scripts/sync_derived.py`, not at the path the card names, and it was run after the bump commit instead of before it. This release carries the synced card and no other change; the README subtitle from 0.180.5 ships with it. Prior: Version 0.180.5 -- **Attribution label: the-h1-says-what-category-this-is-2026-09-08** (PATCH). Eight GitHub repositories are named mycelium and none of them is in this category, and the README's H1 was the bare name with a motto beneath it. One subtitle line now names the category and the mechanism in the words the About line and the founder's own post already use: a Claude Code harness that asks who this is for before the agent writes code. Testable by Test B's existing findability queries on the dogfood side. README only. Prior: Version 0.180.4 -- **Attribution label: a-page-for-the-builder-who-is-in-one-of-those-posts-2026-09-06** (PATCH). Four queries a burned builder would type reached no Mycelium page on 2026-09-06 (Brave, top 20). `docs/nobody-used-it.md` is one page in that builder's own grammar, one scene, the 46-post count tied to what it cannot say, and a plain statement of what the harness does not do. Linked from the README's pie caption and the docs index. Pre-registered on the dogfood side: a Mycelium page in the top 20 for at least 2 of the 4 queries at +30 days, or the findability line closes. Prior: 0.180.3 -- **Attribution label: two-sentences-the-judges-caught-2026-09-06** (PATCH). Six blind README judges, three per arm, found the same defect: the pie's six problem-holder posts sat under "I couldn't say that anyone asked their users up front". The sentence now says what the read supports: whether any of the nine asked before they built, the posts don't say, because timing was not coded. Two judges also read "four questions" against a video that runs past them; the video sentence now says so. README only. Prior: 0.180.2 -- **Attribution label: the-first-hundred-seconds-inline-2026-09-06** (PATCH). The README's demo video had zero views in 28 days against 18 unique README visitors in 14, so the click-out link was doing no work for anyone. A GIF of the first hundred seconds now sits inline above the link, cut from the founder's own recording with frames only where the terminal changed and each frame held for as long as it was on screen; 93 frames, 1.25 MB. It links to the full video so the click stays observable. Pre-registered on the dogfood side as Test C: video views from github.com rise from 0 in the next 14-day window, or the show-it-running line closes. Prior: 0.180.1 -- **Attribution label: the-name-in-the-founders-words-2026-09-06** (PATCH). README gains a `## The name` section: mycelium connects separate trees through one system and turns what died into what feeds the next lifeform, and the framework does both. Two claims were graded against the tree before landing: thirty-odd theories holds for the 8 tier-1 and 23 tier-2 entries in `docs/theories.md` (the 16 tier-3 are citation-only), and "every failure becomes a check" was softened to "the mistakes it catches go back into the system", which the session-start corrections read supports and the selective-graduation record does not exceed. Founder-authored after three blind-discriminator rounds rejected the agent drafts. README only. Prior: 0.180.0 -- **Attribution label: surfaces-without-these-three-rules-is-a-waiver-machine-2026-09-04** (MINOR). Two blind adversarial reads on the `surfaces` block shipped in 0.179.0 found it structurally under-defended: **declaring a surface costs work, declaring its absence costs a word**, and accessibility has a long history of being waived on exactly such grounds. Three constraints added: surfaces are DETECTED FROM ARTIFACTS WITH THE EVIDENCE RECORDED, never self-asserted; `n/a` must render distinctly from `pass` in every aggregate, so waiving can never improve a score; and there is a FLOOR — every shipped product has at least one human surface, its docs at minimum, so **a fully-n/a accessibility result is a detector bug, not a clean bill.** The `terminal` profile was also too thin, granting more relief than a terminal earns: it now binds NO_COLOR, structure-not-by-box-drawing, read-aloud-parseable output and plain-text-survivable docs, keeping only contrast and font size as genuinely inherited. And the boundary is written down: **surfaces scope the SUB-CHECKS, never the principle** — a terminal product may be n/a on keyboard navigation and still fail "be usable by everyone" on jargon — and surfaces do NOT scope security, which needs its own exposure axis because a headless library has no human surface and a large attack surface. Prior: 0.179.0 -- **Attribution label: an-instrument-aimed-at-a-surface-the-product-lacks-2026-09-04** (MINOR). `a11y-check` applied a web checklist to every product and referenced `product_type` zero times, so a delivery bar could fail on an instrument that could not bind. Measured on the framework itself: Downe P11 was rated **fail** for a terminal product, with the entry stating *"WCAG 2.1 AA is largely the wrong instrument for a terminal product."* `product_type` alone cannot fix it — `software` covers "web app, mobile app, API, CLI, library, desktop app" and lists WCAG for all of them, while an API has no human interface, a CLI inherits from the terminal and a web app is fully bound. **So the unit is the SURFACE, detected from contents, not the type.** Seven profiles in `canvas-guidance.yml#surfaces`, and one rule that decides every rating: **surface absent -> `n/a`; surface present and unmeasured -> `not-assessed`, a real gap.** Marking a criterion `fail` because its surface does not exist manufactures a defect out of a product's shape and builds a done-bar no work can satisfy. Prior: 0.178.1 -- **Attribution label: the-worst-case-was-the-quietest-one-2026-09-04** (PATCH). `check_confidence_managed.py` reported "no `confidence_derivation` block" at INFO regardless of whether evidence existed — so a diamond with a number nobody can re-derive AND scored instruments standing against it was the LEAST loudly flagged state in the tool. Found within the hour by running the new check across the rest of the dogfood board: three diamonds in exactly that state, one of them **107 days unchanged with five scored instruments naming it**. Severity now depends on whether evidence exists — WARN when it does, INFO when it does not, so a project that has gathered nothing yet is told without being failed and the adoption path stays open. Prior: 0.178.0 -- **Attribution label: evidence-landed-and-nobody-asked-what-it-did-to-the-number-2026-09-04** (MINOR). Five skills instruct an agent to update confidence when evidence lands. **No check ever verified it**, and the only "stale confidence" string in the framework was an example inside a report template. Measured on the dogfood project: a diamond whose confidence had moved ONCE in the project history, then twenty days in which 21 instruments naming it were scored, a pre-registered test returned SUPPORTS on the first attribution evidence the bet had ever had, and a crossing condition written in advance was met three times over. The number never moved and nothing noticed. `check_confidence_managed.py` closes it, and the design constraint matters more than the check: **it never demands that a number MOVE.** Considered-and-unchanged, recorded with a date, is a pass; unexamined is the defect. A guard that pushed confidence upward would be an inflation engine aimed at the one value a project must not inflate. Two failures are recorded in the code because both will recur: the derivation had been stored in a YAML COMMENT and a later rewrite deleted it, so it is a FIELD now and comments are not data; and **the first version of this guard did not bite** -- a 30-day staleness threshold against a 20-day defect, passing the exact case it was written for, so the trigger is evidence rather than the calendar. Prior: 0.177.0 -- **Attribution label: a-version-documented-twice-was-invisible-to-every-reader-2026-09-04** (MINOR). Every reader of the changelog goes through `parse_changelog_versions`, which returns a SET, so a version carrying two `## vX.Y.Z` sections was indistinguishable from one carrying a single section -- in the gap checks, in the counts, everywhere. It is not cosmetic: `notes_for` builds a published Release body from the FIRST matching section, so the others never reach a consumer. v0.108.0 carried two unrelated sections from 2026-08-08 until someone counted headings by hand. Diagnosed rather than guessed -- PR #64 moved plugin.json 0.107.1 to 0.108.1, two versions against three sections, and the published Release names the first -- so the surplus one is demoted to a subsection of the release it actually shipped in. `duplicate_changelog_versions` now blocks above the 0.49.0 floor inside `--check`. Running it without a floor immediately surfaced two more, v0.26.1 and v0.39.5, each carrying two genuinely different bodies under one number; those are REPORTED and not rewritten, because reconstructing which body was which release three months on would be inventing a record, and a floor that hid them would make them permanently unfindable. Prior: 0.176.0 -- **Attribution label: the-release-pipeline-learns-the-other-direction-2026-09-04** (MINOR). Release automation could only ever ask one of two questions: is a documented version missing a Release? Nothing asked whether a Release was missing its documentation, and that direction has the worse failure -- a Release nobody meant to cut is a public claim that a version shipped. Three mechanisms, deliberately not one: `--require-documented` withholds any version that exists only in an intermediate commit and warns rather than failing, because a red main on a push whose work landed trains dismissal; the Latest pointer refuses to promote a version with no changelog section, since a stray Release is a dead tag but a stray Latest is a false statement; and `--audit` runs weekly in its own workflow rather than at release time, because it can only ever fire after the artifact is already public. Also closes a shipped field that nothing read: `purpose_stance` overrides carry `decision: DL-NNNN`, the docs have shown it since the mechanism shipped, and the check only ever validated `human:` -- so a contradiction could be cleared by citing a decision that never existed. Now resolved against the decision log, opt-in by presence. This version number was cut as a phantom Release on 2026-09-03 and deleted the same day; it is reused deliberately rather than skipped, because a silent gap in the chain is the habit this release is about. Prior: 0.175.2 -- **Attribution label: readme-pie-palette-meets-wcag-on-both-surfaces-2026-09-03** (PATCH). The README pie shipped in near-black fills that all but vanished on GitHub's dark canvas. Re-picked from the validated categorical palette and checked computationally rather than by eye: `#3987e5` / `#d95926` / `#199e70` clear WCAG 1.4.11 (3:1 non-text) against **both** `#ffffff` and `#0d1117`, carry black section labels at 5.4-6.2:1 (clearing 1.4.3's 4.5:1), and pass the dataviz validator's CVD gate in both modes (worst all-pairs deutan Delta E 9.4, normal-vision 20.9). Slice borders are white at 2px, which reads as a separator gap on the light surface and a crisp ring on the dark one, plus a `#8b949e` outer ring that clears 3:1 on both. Title and legend colors are deliberately NOT pinned, so GitHub's per-mode theme still supplies them. Prior: 0.175.1 -- **Attribution label: readme-backs-discovery-claim-with-a-count-2026-09-03** (PATCH). The README's claim that nobody thinks discovery is optional rested on one person's word for it. Added a hand-read count instead: 46 shipped-to-no-traction posts across r/microsaas, r/SideProject and r/vibecoding, 37 of 46 never mentioning contact with anyone who had the problem. New receipts case `docs/receipts/cases/2026-08-16-l1-population-read.md` carries the method and the limits (floor not a rate, timing not coded). A craft audit found and fixed the three weakest sections of the README in the same pass, judged by voice against the rest of the file rather than by keyword.*

*Full version history: [`docs/changelog.md`](docs/changelog.md).*

Mycelium is a harnessing system for AI-assisted product development. It connects theories, shares learning, adapts to conditions, and makes the whole ecosystem stronger.

**You are an agent operating within Mycelium. Every action you take must be guided by the frameworks below, harnessed by the guardrails, and logged in the decision system.**

## Agent Operating Contract (always-on rules)

The always-on behavioral rules — the **Communication Rules** (plain-language-first; suggest-skills-at-transitions; route-through-skills with the freeform escape valve; cite-the-trigger; offer-to-capture-learnings; name-the-verification-surface incl. `Ran: <cmd> → <result>` for runnable artifacts, `G-V13`; name-the-gate; read-canvas-before-narrating; verify-after-write; BLUF-layering, `G-C1`) and the **Mandatory Pre-Task / Pre-Ship (G-P-pre) / Post-Task (G-P7) Protocols** — are defined once, canonically, in **[`plugins/mycelium/engine/agent-operating-contract.md`](plugins/mycelium/engine/agent-operating-contract.md)**.

That file is the single source of truth, and the SessionStart hook (`plugins/mycelium/hooks/session-start.sh`) injects it into every session — so the contract binds in **plugin form** too, where no operating-manual `CLAUDE.md` is templated into the consuming project. It is the plugin-form replacement for the removed legacy `degit`-templating path (the migration kept the computational half of the harness — hooks — but until v0.58.0 silently dropped this always-on inferential half for plugin-form consumers; the framework repo never noticed because its own sessions load this very file as the repo's `CLAUDE.md`). Read the contract; those active rules win over any restatement. **CI Check 47** keeps the wiring intact: contract present + packaged in `manifest.yml` + injected by the hook + referenced here + free of legacy `.claude/` framework paths.

Canonical detail behind individual rules still lives in the harness: `plugins/mycelium/harness/communication-rules.md` (rationale + graduation history + X/Twitter extraction sequence), `guardrails-core.md` (`G-C1` BLUF-layering, `G-V13` runtime-proof, `G-P7` Post-Task, `G-P-pre` Pre-Ship), `engine/canvas-guidance.yml#action_flags` (gate flags). Pre-Ship graduated 2026-05-04; write-narration-verification 2026-06-05 (#18/#19).

## The Diamond Engine

### Diamond Scales (L0-L5)

| Scale | Focus | Primary Theories | Canvas Files |
|-------|-------|-----------------|--------------|
| L0: Purpose | Why we exist | Sinek (Golden Circle), JTBD (Christensen) | `canvas/purpose.yml`, `canvas/jobs-to-be-done.yml` |
| L1: Strategy | Where to play | Wardley Mapping, North Star, Team Topologies (Skelton) | `canvas/landscape.yml`, `canvas/north-star.yml`, `canvas/team-shape.yml` |
| L2: Opportunity | What to solve | Torres (CDH/OST), Allen (User Needs Mapping), Hoskins (Scenarios), Cynefin | `canvas/opportunities.yml`, `canvas/user-needs.yml`, `canvas/scenarios.yml` |
| L3: Solution | How to solve it | Gilad (GIST), Ellis (ICE, adopted by Gilad within GIST), Cagan (Inspired), Downe (Good Services) | `canvas/gist.yml`, `canvas/services.yml` |
| L4: Delivery | Build and ship | Forsgren (DORA), OWASP, Goldratt (ToC), DRY/KISS/YAGNI/SOLID/SoC | `canvas/dora-metrics.yml`, `canvas/threat-model.yml`, `canvas/value-stream.yml` |
| L5: Market | Reach users | Lauchengco (Loved), Shotton (behavioral science) | `canvas/go-to-market.yml`, `canvas/trust-signals.yml` |

L0-L3 are product-agnostic. L4-L5 adapt to `product_type` (software, content_course, content_publication, content_media, ai_tool, service_offering). See `canvas-guidance.yml#product_types`.

### Diamond Phases

Four phases per diamond, gated by theory checks: **Discover** (diverge — explore, gather evidence), **Define** (converge — synthesize, frame the problem), **Develop** (diverge — ideate, prototype), **Deliver** (converge — validate, build, ship, measure). Diamonds spawn children (L0→L1→L2→L3→L4, L5→L2 on market feedback); a bad assumption found in delivery **regresses** the diamond back with new evidence — the system working correctly. Full transition rules, WIP limits, lifecycle: `plugins/mycelium/engine/diamond-rules.md`.

### OST Leaf Lifecycle

Every solution leaf runs a 10-phase pipeline, each phase with input artifacts, gates, outputs, and discard criteria: **OST Leaf → Four Risks → ICE Score → Assumption Test → GIST Entry → Bounded Context → Threat Model → Preflight → Delivery Diamond → Launch + Feedback**. Definitions and discard rules: `plugins/mycelium/engine/leaf-lifecycle.md`; archived leaves → `canvas/archived-solutions.yml`.

### Perspective Resolution & Leaf Bakeoff

Conflicting product/design/engineering perspectives → structured resolution in `plugins/mycelium/engine/perspective-resolution.md`. Multiple leaves competing for one opportunity → structured A/B comparison in `plugins/mycelium/orchestration/leaf-bakeoff.md`.

## Theory Gates (Decision Checkpoints)

Every diamond transition must pass applicable gates from: Evidence, Four Risks, JTBD, Cynefin, Bias, Security, Privacy, BVSSH, Service Quality, Delivery Metrics, Corrections, Regulatory, Explainability. See `plugins/mycelium/engine/theory-gates.md` for complete definitions, pass/fail criteria, and suggested skills.

**You cannot progress a diamond by saying "I'm confident enough." You must demonstrate evidence that satisfies each gate.**

## The Canvas (Source of Truth)

All product knowledge lives in `.claude/canvas/*.yml`. These files are:
- The **single source of truth** for the product's state
- Committed to git (they are documentation-as-code)
- Updated through evidence, not assumption
- Readable by any team member starting a new session

**Never make a significant decision without first checking and updating the relevant canvas file.**

Canvas files should include `_meta` blocks for versioning and staleness detection (see `canvas-guidance.yml`). Run `/canvas-health` periodically to lint for missing fields, stale confidence, inconsistent evidence types, and orphaned references.

**Canvas writes — Read before Write (HARD RULE).** Canvas files ship pre-populated, so every `.claude/canvas/*.yml` exists on a fresh project. `Write`/`Edit` require a prior **`Read` tool** call (same session); **`cat`/`head`/`grep` via Bash do NOT satisfy it** (different tool surfaces). **`Edit`**: `Read limit:1` suffices (~50 tokens; state is per-file — reuse across edits; use for large files like `purpose.yml`). **`Write`**: full Read first (it obliterates the file). **ID-bearing entries**: run `grep "^  - id: <prefix>-" .claude/canvas/<file>.yml | sort -u` before assigning and pick the next free integer (`validate_canvas.py` catches dupes on CI but a working-tree dupe can persist for days; kin to anti-pattern #8 Stale State Read). Validator Check 31 enforces the Preflight block; cost-discipline + ID-scan graduation history in `docs/changelog.md`.

## Harnessing System

- **Behavioral Contract** (`plugins/mycelium/harness/behavioral-contract.md`): Consolidated must / must-never index for the agent itself. Pointer-only (copies nothing; the cited source always wins) so the agent's own behavioral contract is grep-able in one place — the self-governance analogue of the `ai_tool` product contract in `canvas/ai-tool-metrics.yml`.
- **Guardrails** (`plugins/mycelium/harness/guardrails.md`): Three-tier enforcement -- BLOCK (mechanically prevented), REVIEW (gates progression), NUDGE (advised, not blocking).
- **Anti-Patterns** (`plugins/mycelium/harness/anti-patterns.md`) & **Cognitive Biases** (`plugins/mycelium/harness/cognitive-biases.md`): Known failure modes with detection rules (stop and self-correct if you catch yourself in one); per-stage bias checklist.
- **Security & Trust** (`plugins/mycelium/harness/security-trust.md`): Per-stage security requirements.
- **Engineering Principles** (`plugins/mycelium/harness/engineering-principles.md`): DRY, KISS, YAGNI, SoC, SOLID, LoD. Human-facing complement: **Design Principles** (`plugins/mycelium/harness/design-principles.md`) — how the framework treats its user (SDT autonomy/competence/relatedness; Theory Y; never gamify discipline).
- **Delegation Authority** (`plugins/mycelium/harness/delegation-authority.md`): who decides — agent vs human — for each *execution* decision, keyed to consequence (effective-reversibility × aggregate-blast-radius). Binds the BLOCK/REVIEW/NUDGE tiers to execution (not just epistemic discipline); names the no-standing decisions (which bet, security/privacy/ethics tradeoffs, editing the map itself) that are absolutely human; round-up-under-uncertainty; escape-hatch override. Repo anchor for behavioral-contract N9/N10.

## Self-Learning System

### Two Memory Systems -- Important Distinction

| System | Location | Scope | Committed to git? |
|---|---|---|---|
| **Project memory** | `.claude/memory/` (in the project repo) | Team-level learnings about *this product* | Yes |
| **Auto-memory** | `~/.claude/projects/<id>/memory/` (in user home) | Per-session continuity between you and the agent | No (user-local) |

**Routing rule**: Project-team learnings -> project memory. Agent-user learnings -> auto-memory. Hardware/environment failures -> neither.

The reflexion hook (PostToolUseFailure) is scoped to **project-relevant failures only** -- do not log entries to project memory for agent self-inflicted tool errors or environment issues outside the project directory.

### Key Artifacts
- **Corrections** (`.claude/memory/corrections.md`): learning from mistakes. **Read before every task.** *Recourse SLA*: one-offs inform next session; ≥3 same-root-cause instances graduate to mechanism on the next L4 cleanup cycle.
- **Patterns** (`.claude/memory/patterns.md`): successful patterns to reuse.
- **Warnings Log** (`.claude/memory/warnings-log.md`): CI WARN+FAIL capture, auto-updated; per-class fixes in `plugins/mycelium/engine/warning-handbook.md`; consumed by `/corrections-audit`.
- **Decision Log** (`plugins/mycelium/harness/decision-log.md`): every significant decision. **Required** `why_not_alternatives` field — per-alternative rejection rationale; contrastive explanations land harder (Liao et al. 2020) and feed `/xai-check` Stage 2.
- **Feedback Loops** (`plugins/mycelium/engine/feedback-loops.md`, `/feedback-review`), **Reflexion Loop** (`plugins/mycelium/skills/reflexion/SKILL.md`, max-3 retry), **Eval Benchmarks** (`.claude/evals/`), **Cycle History** (`.claude/canvas/cycle-history.yml` → `engine/cycle-learning.md`), **Adaptive Thresholds** (`.claude/canvas/thresholds.yml` → `engine/adaptive-thresholds.md`).

### Learning Metabolism (Self-Improving System)

Five mechanisms make Mycelium smarter over time (details + cadence in each sub-file): **Cycle Learning** (`plugins/mycelium/engine/cycle-learning.md` — predicted vs actual ICE), **Pattern Emergence** (`plugins/mycelium/engine/pattern-detector.md` — into `/retrospective`, `/diamond-assess`), **Adaptive Thresholds** (`plugins/mycelium/engine/adaptive-thresholds.md` — defaults until N=10), **Framework Reflexion** (`plugins/mycelium/engine/framework-reflexion.md`, `/framework-health`), **Evidence Decay** (`plugins/mycelium/engine/evidence-decay.md` — `/canvas-health` flags stale evidence).

## Domain Contexts

Load the appropriate context based on current diamond phase:

- **Discovery**: `${CLAUDE_PLUGIN_ROOT}/domains/discovery/CLAUDE.md` -- Torres-style interviewing, OST construction, bias-aware research
- **Delivery**: `${CLAUDE_PLUGIN_ROOT}/domains/delivery/CLAUDE.md` -- Agile/DevOps practices, clean code, security, accessibility, DORA metrics
- **Quality**: `${CLAUDE_PLUGIN_ROOT}/domains/quality/CLAUDE.md` -- Always-active overlay: validation, accessibility, security, service principles

## JiT Tooling

Mycelium is **language-agnostic** and **product-type-agnostic**. When a delivery diamond begins, auto-detect the tech stack (or product type), generate appropriate validation, and confirm with the user. See `plugins/mycelium/jit-tooling/detector.md`.

## Usage & Orchestration

Solo developers use canvas as shared memory with the agent. Teams commit canvas to git as shared product documentation. For parallel exploration, use `/fan-out` with worktree-isolated worker agents.

See `plugins/mycelium/orchestration/modes.md` for usage patterns and `plugins/mycelium/orchestration/agent-teams.md` for parallel orchestration.

## Operations & Maintenance

- **Day-to-day**: `plugins/mycelium/orchestration/operations.md` -- Session resumption, canvas maintenance, diamond lifecycle, memory pruning
- **Escape hatch**: `plugins/mycelium/orchestration/escape-hatch.md` -- Legitimate process bypass for emergencies. Must be documented and paid back.

## Skills

All 61 skills are auto-discovered from SKILL.md frontmatter — in plugin form (`plugins/mycelium/skills/*/SKILL.md`, recommended) or legacy form (`.claude/skills/*/SKILL.md`, supported during transition). Suggested skills are surfaced at diamond transitions by `/diamond-progress` and `/diamond-assess`, and contextually by hooks. Type `/` to see the current list.

## Getting Started

New project (empty canvas): run `/interview`. Continuing work (populated canvas): run `/diamond-assess`. The system guides you from there.
