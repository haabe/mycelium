# Surface Registry
#
# For each recurring artifact class: which file is AUTHORITATIVE, and which
# mechanism READS it. Nothing else.
#
# WHY THIS EXISTS (cluster `right-content-wrong-surface`, criterion met
# 2026-07-26, mechanism built 2026-08-06):
#
#   The artifact is produced correctly and on time. The analysis is sound, the
#   lesson is real, the assessment is honest. It is written to a surface no
#   downstream mechanism reads. Nothing is lost to the human eye; everything is
#   lost to the machinery.
#
#   The failure is invisible by construction: a reader who opens the surface
#   where the content DID land sees a complete, well-reasoned artifact and
#   concludes the loop closed.
#
# THREE INSTANCES, and the third is why this is a registry rather than a fourth
# point-check:
#   1. BVSSH assessments -> decision-log, never bvssh-health.yml, which is the
#      canvas the SessionStart hook reads to compute "overdue". Three times.
#      Point-fixed v0.59.0.
#   2. A release lesson -> changelog.md, never corrections.md, which is what
#      /corrections-audit and the cluster catalogue actually read.
#      Point-fixed v0.59.1.
#   3. Upstream candidates -> 17 items marked "UPSTREAM CANDIDATE" in a
#      chronological decision log, read by a human scrolling and by no
#      mechanism. STILL OPEN. It has no authoritative surface at all, which is
#      why the registry has to be able to express "none yet" instead of
#      quietly omitting the row.
#
# THE TEST OF THIS FILE: adding a fourth artifact class is a ROW, not a fourth
# point-check. If someone ships a fourth bespoke checker instead of adding a
# row here, this registry failed and should be reconsidered rather than kept
# for appearances.
#
# WHAT IT DOES NOT DO. It does not verify that content actually landed — that
# is per-artifact and belongs to the point-checks. It verifies the WIRING
# CLAIM: that the declared reader genuinely references the declared surface.
# A reader that does not is a loop that cannot close, and that is exactly the
# defect this cluster is about, one level up.

schema_version: 1

surfaces:
  - artifact_class: bvssh-assessment
    description: "A /bvssh-check assessment: five dimensions, CALMS, blocking findings."
    authoritative_surface: ".claude/canvas/bvssh-health.yml"
    read_by:
      - "plugins/mycelium/hooks/session-start.sh"          # computes the overdue reminder
      - "plugins/mycelium/scripts/check_bvssh_reconcile.py" # catches log-only orphans
    also_written_to: ".claude/harness/decision-log.md"
    note: >-
      The decision-log copy is provenance and is CORRECT to keep. The defect was
      writing ONLY there. Three orphans (2026-05-20, 2026-06-20, 2026-07-11)
      before the skill was given a mandatory canvas step.
    point_check: "check_bvssh_reconcile.py (v0.59.0)"

  - artifact_class: channel-evidence
    description: "What a channel did when tried: reach, reply rate, post-shape ladders, reachability of a segment, posture rulings."
    authoritative_surface: ".claude/canvas/go-to-market.yml"
    read_by:
      - "plugins/mycelium/skills/handoff/SKILL.md"       # step 1b: read the channel map before naming a channel
      - "plugins/mycelium/skills/diamond-assess/SKILL.md" # step 6c: L1 reads reachability, L2 reads channel outcomes
      - "plugins/mycelium/skills/launch-tier/SKILL.md"   # L5 go-to-market plan
    also_written_to: ".claude/canvas/human-tasks.yml"
    note: >-
      The touch logs in human-tasks.yml are the raw record and are correct to keep; the ledger of what
      a channel DID lives under go-to-market.yml#channel_intelligence. Until 0.191.0 the only declared
      reader was /launch-tier, which runs at L5, so on a project that had not spawned L5 the ledger
      had no reader at all. Dogfood 2026-08-25: a full session on acquisition proposed channels for
      hours and never retrieved a ledger that already held 18 of 24 standard channels with decisions
      attached; the gap was retrieval, not knowledge. /handoff drafts outreach and names channels
      every time it runs, so it is the reader that closes the loop before L5. Dogfood 2026-09-10: the
      file was flagged stale on a 90-day stamp while 17 dated blocks had landed in it since August.
    point_check: null

  - artifact_class: positioning-candidate
    description: "A candidate key benefit, positioning line or segment sheet that is not yet the positioning."
    authoritative_surface: ".claude/canvas/go-to-market.yml"
    read_by:
      - "plugins/mycelium/skills/launch-tier/SKILL.md"   # promotes a candidate into positioning at L5
    also_written_to: ".claude/harness/decision-log.md"
    note: >-
      Candidates sit under go-to-market.yml#positioning beside the six empty fields they may one day
      fill (target_customer, category, key_benefit, differentiators, proof_points,
      messaging_statement). /launch-tier is the only framework reader and it is L5-only; a project
      drafting public copy before L5 reads them through its own drafting skill, which is
      project-side and is declared there, not here. The row exists so the candidates are a class with
      a surface, not dated prose keys nothing lists.
    point_check: null

  - artifact_class: competitor-evidence
    description: "What a named competitor, comparator or landscape entry does, with a dated read."
    authoritative_surface: ".claude/canvas/landscape.yml"
    read_by:
      - "plugins/mycelium/skills/wardley-map/SKILL.md"
      - "plugins/mycelium/skills/launch-tier/SKILL.md"
      - "plugins/mycelium/skills/diamond-assess/SKILL.md"
    also_written_to: ".claude/canvas/go-to-market.yml"
    note: >-
      go-to-market.yml#competitive_intelligence is Lauchengco's slot for the same thing and it is
      correct to keep as a POINTER into landscape.yml (which comp id, what it means for positioning).
      It is not correct as a second home: on the dogfood canvas the one entry there was dated
      2026-05-03 and read as stale on 2026-09-10 while landscape.yml held the same competitor
      re-read four times since. Two homes for one class is this cluster one level up.
    point_check: null

  - artifact_class: release-lesson
    description: "A process lesson learned while shipping a release."
    authoritative_surface: ".claude/memory/corrections.md"
    read_by:
      - "plugins/mycelium/skills/corrections-audit/SKILL.md"
      - "plugins/mycelium/skills/framework-health/SKILL.md"  # step 4b cluster graduation
    also_written_to: "docs/changelog.md"
    note: >-
      The changelog is release COMMUNICATION and is correct to keep. corrections.md
      is what /corrections-audit, the cluster catalogue, and count-keyed graduation
      read. v0.57.4's lesson went to the changelog only and never reached the memory
      that feeds graduation.
    point_check: "Check 48, WARN tier (v0.59.1)"

  - artifact_class: correction-to-cluster-instance
    description: "A logged correction that is an instance of a known cluster."
    authoritative_surface: ".claude/memory/cluster-instances.md"
    read_by:
      - "plugins/mycelium/scripts/check_cluster_reconcile.py"
      - "plugins/mycelium/skills/framework-health/SKILL.md"
    also_written_to: ".claude/memory/corrections.md"
    note: >-
      Added 2026-08-06 (opp-034). Cluster graduation is keyed to instance COUNTS
      and nothing writes them. Measured: corrections +24 over five days, catalogue
      +1 row. The reader asserts the hop was CONSIDERED; it deliberately does not
      classify, because mis-binning corrupts a count that gates a mechanism.
    point_check: "check_cluster_reconcile.py (v0.99.0)"

  - artifact_class: metric-measurement
    description: >-
      A DORA / APEX measurement taken by /dora-check: the four metrics, their
      classification, and the window they were computed over.
    authoritative_surface: ".claude/canvas/dora-metrics.yml#measurement_history"
    read_by:
      - "plugins/mycelium/skills/dora-check/SKILL.md"   # the only declared reader that names the field
    also_written_to: ".claude/canvas/dora-metrics.yml (the per-metric `current:` fields)"
    note: >-
      ADDED 2026-08-09, from a live instance rather than from tidiness. On
      2026-08-08 the four metrics WERE re-measured and written into their
      `current:` fields — both `deployment_frequency` and `change_failure_rate`
      literally begin "2026-08-08 MEASURED". No `measurement_history` row was
      appended and the top-level `last_measured` stamp stayed at 2026-08-02. One
      file, three dates for one measurement.
      THE COST WAS NOT COSMETIC. The BVSSH assessment in that same session read
      the history, found 2026-07-17, reported DORA as 22 days stale and
      recommended "Pull DORA" — while the numbers were being refreshed into the
      fields beside it. The recommendation then survived two assessments
      unactioned, because the pull had already happened somewhere the assessment
      did not look.
      WHY `measurement_history` IS AUTHORITATIVE AND `current:` IS NOT. A
      `current:` field is MUTABLE: the next measurement overwrites it, so it holds
      at most one reading and leaves no trend. History is append-only. The
      `current:` fields are correct to keep as the at-a-glance view; writing ONLY
      there is the defect, exactly as with the decision-log copy of a BVSSH
      assessment above.
      THE READ_BY LIST IS SHORTER THAN THE FIRST DRAFT OF THIS ROW, AND THE
      CORRECTION IS THE POINT. It initially named three readers — dora-check,
      bvssh-check and canvas-health — and `check_surface_registry.py` rejected two
      of them on its first run, because those two SKILL files mention
      `dora-metrics.yml` and never the field. That is not a bookkeeping nicety: an
      agent told to read "the DORA canvas" for staleness has to pick a date field
      itself, and on 2026-08-08 it picked the one that had not moved. **A reader
      with no declared anchor is how the wrong date gets read**, and the registry
      caught the aspirational claim rather than letting it stand.
    point_check: null   # OPEN — nothing yet asserts that history advanced when a `current:` field did
    open_gap: >-
      Two consumers (bvssh-check, canvas-health) read this file for freshness
      WITHOUT naming a field. Giving each an explicit anchor is the obvious next
      change and is deliberately not bundled here: it edits two skills' semantics,
      which deserves its own pass rather than riding along with a registry row.

  - artifact_class: upstream-candidate
    description: >-
      A framework change surfaced by a dogfood project, to be authored upstream.
      Marked "UPSTREAM CANDIDATE" in prose.
    authoritative_surface: ".claude/harness/upstream-candidates.yml"
    read_by:
      - "plugins/mycelium/scripts/check_upstream_candidates.py"   # probes each candidate against the tree; --strict since 0.195.0
    also_written_to: ".claude/harness/decision-log.md"
    note: >-
      CLOSED 2026-09-14 (v0.201.0), and this row was the last to say otherwise. It read
      `null` / "17 known items sit as prose in a chronological log" from 2026-07-26 until
      today, while the dogfood project had kept `.claude/harness/upstream-candidates.yml`
      since 2026-07-27 (115 rows by 2026-09-14, 36 shipped) and
      `check_upstream_candidates.py` had read it since 0.109.0: the registry that exists
      to catch right-content-wrong-surface was itself right-content in a stale row. The
      decision-log copy stays as provenance; the defect was ever writing ONLY there.
    point_check: "check_upstream_candidates.py --strict (0.195.0: different_builder required on rows surfaced on or after 2026-09-11)"

  - artifact_class: advisory-response
    description: >-
      Whether a session-start advisory was followed by anything: the condition cleared by the
      next session, or it did not; and the mute and ruling events that follow a streak.
    authoritative_surface: ".claude/state/advisory-ledger.jsonl"
    read_by:
      - "plugins/mycelium/hooks/session-start.sh"          # settle: records, settles, mutes
      - "plugins/mycelium/skills/canvas-health/SKILL.md"   # 8c(g): report
    also_written_to: null
    note: >-
      Added 2026-09-10 (opp-006 sol-006d). Before this row, an advisory that fired daily for a
      month with no response was indistinguishable from one acted on every time; BVSSH rated
      Measurement amber three assessments running on exactly that. The ledger measures the
      condition clearing, not the human's action, and says so; the two shapes it cannot see (a
      "read it and leave it" remedy, an awareness-only line) are the check's job to mark, not
      the ledger's to guess.
    point_check: "advisory_ledger.py report (canvas-health 8c(g), v0.184.0)"
