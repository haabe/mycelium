{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mycelium.dev/schemas/diamonds/active.schema.json",
  "title": "Diamond State (active.yml)",
  "description": "The framework's most-read state file: active/parked/archived/killed diamonds per engine/diamond-rules.md \u00a7 Diamond State Tracking. Permissive by design (additionalProperties: true) \u2014 projects extend freely; the schema pins the load-bearing core: ids, scale/phase enums, confidence range, and the definition_of_done shape (v0.43.0). Added 2026-06-12 after the dogfood repo's active.yml sat committed-unparseable for \u22653 days with zero detection (corrections.md 2026-06-12).",
  "type": "object",
  "properties": {
    "schema_version": {
      "$ref": "_common.schema.json#/$defs/schema_version"
    },
    "project_type": {
      "type": "string"
    },
    "product_type": {
      "type": "string",
      "enum": [
        "software",
        "content_course",
        "content_publication",
        "content_media",
        "ai_tool",
        "service_offering"
      ]
    },
    "dogfood": {
      "type": "boolean"
    },
    "autonomous": {
      "type": "boolean",
      "description": "Declared autonomous run (engine/autonomous-mode.md). One of the three declaration surfaces the autonomous-evidence-guard reads."
    },
    "_meta": {
      "type": "object",
      "description": "File-level metadata. `last_validated` is the date a human last confirmed the content accurate; staleness horizon per engine/evidence-decay.md (strategic, 180 days). Replaced top-level `last_updated` in v0.89.0 \u2014 that field duplicated what git already records authoritatively, and a hand-maintained copy of a machine-knowable fact can only drift."
    },
    "active_diamonds": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/diamond"
      }
    },
    "parked_diamonds": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/diamond"
      }
    },
    "completed_diamonds": {
      "type": "array",
      "description": "Diamonds whose Definition of Done was MET, with evidence. Distinct from archived_diamonds, which is where work goes when it STOPS. Added v0.230.0: check_scale_occupancy.py had read this key since it shipped, while no schema defined it and no skill wrote it, so 'ever opened' could never count a finished cycle and compliance with completion was unverifiable. Written by /diamond-progress; see its Step 9b.",
      "items": {
        "$ref": "#/$defs/completed_diamond"
      }
    },
    "archived_diamonds": {
      "description": "Work that STOPPED and is no longer tracked, without its DoD being met. NOT a synonym for completed - that conflation is why completion was uncheckable before v0.230.0.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/diamond"
      }
    },
    "killed_diamonds": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/diamond"
      }
    }
  },
  "additionalProperties": true,
  "$defs": {
    "diamond": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "scale": {
          "type": "string",
          "enum": [
            "L0",
            "L1",
            "L2",
            "L3",
            "L4",
            "L5"
          ]
        },
        "phase": {
          "type": "string",
          "enum": [
            "discover",
            "define",
            "develop",
            "deliver",
            "complete"
          ]
        },
        "state": {
          "type": "string",
          "enum": [
            "active",
            "blocked",
            "parked",
            "completed",
            "archived",
            "killed"
          ],
          "description": "Lifecycle state, per engine/diamond-rules.md. `completed` added v0.234.2 and its absence was a defect in v0.231.0: that release created the `completed_diamonds` list, defined the `completed_diamond` shape, and documented `completed` as a distinct state in diamond-rules.md -- while leaving this enum on the old five, so a row correctly filed as completed failed validation on the very field naming what it is. Found 2026-09-21 by moving the first real completion into the list. Third surface of one change: the list, the rules doc, and the enum."
        },
        "confidence": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "REQUIRED as of v0.92.0. An ABSENT value is not low confidence, it is no position, and consumers were defaulting it (0.5 in one evaluator), which made 'never recorded' indistinguishable from 'recorded too high'. /interview already writes 0.15 at birth. Entry locks (v0.245.0, scripts/scale_locks.py) read `evidence_type`, not this number: the L4 lock needs its L3 at data-supported or better."
        },
        "parent": {
          "type": [
            "string",
            "null"
          ],
          "description": "The diamond this one was spawned from. CANONICAL name (v0.245.0): scripts/scale_locks.py reads it to resolve the entry lock (an L4 names the L3 it delivers, an L5 the L4 whose release it is). Written by /launch-tier and the spawn offers."
        },
        "parent_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "DEPRECATED alias of `parent` (v0.245.0); read as a fallback, never written. /diamond-render read this name while /launch-tier wrote `parent`, so no reader and writer agreed."
        },
        "object_ref": {
          "type": [
            "string",
            "null"
          ],
          "description": "What this cycle works on, by id: the opportunity (L2), the opportunity or solution (L3), the solution and increment (L4). The L3 entry lock resolves it against canvas/opportunities.yml (v0.245.0); written by the /ost-builder, /ice-score and /preflight offers since v0.217.0/0.243.0 and undeclared until now."
        },
        "launch_data": {
          "type": "object",
          "description": "L5 entry lock (v0.245.0): launch data from the shipped L4, at any n (Gilad, Evidence-Guided p123). At least one of the three must say something.",
          "properties": {
            "usage": {},
            "feedback": {},
            "metric_movement": {}
          },
          "additionalProperties": true
        },
        "learning_delivery": {
          "type": "object",
          "description": "An L3's learning delivery (v0.257.0): the L3 reaches real people only with all three filled and only until `until`, through Security, Privacy and Service Quality. `means` follows the product type (web software: infrastructure as code for an environment that can be torn down; courseware: a pilot cohort; service: by hand). Production for everyone is an L4. Once the L3 delivers, a change to the audience is recorded in `changes`; a release to everyone goes through an L4 (v0.268.0).",
          "properties": {
            "audience": {"type": "string"},
            "until": {"type": "string"},
            "means": {"type": "string"},
            "ended": {"type": "object", "description": "How the learning delivery ended, written when the L3 completes (v0.268.0): how (withdrawn | handed_to_l4), on (the date, not in the future), l4 (the L4's id when handed on), note (free text, any language, never read by a check).", "properties": {"how": {"type": "string", "enum": ["withdrawn", "handed_to_l4"]}, "on": {"type": "string"}, "l4": {"type": "string"}, "note": {"type": "string"}}, "additionalProperties": true},
            "changes": {"type": "array", "description": "Each change to the audience once the L3 delivers (v0.268.0): on, audience_was, kind (narrowed | reworded | widened | everyone), why; `reassessed` names the gates re-run for a widened audience. `everyone` is the L4's.", "items": {"type": "object"}}
          },
          "additionalProperties": true
        },
        "children": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "product_type": {
          "type": "string"
        },
        "evidence_type": {
          "$ref": "_common.schema.json#/$defs/evidence_type"
        },
        "created": {
          "$ref": "_common.schema.json#/$defs/iso_timestamp"
        },
        "created_at": {
          "$ref": "_common.schema.json#/$defs/iso_timestamp"
        },
        "last_updated": {
          "$ref": "_common.schema.json#/$defs/iso_timestamp"
        },
        "last_progressed": {
          "$ref": "_common.schema.json#/$defs/iso_timestamp"
        },
        "completed_at": {
          "$ref": "_common.schema.json#/$defs/iso_timestamp",
          "description": "True ship timestamp, stamped at Deliver->Complete (v0.53.0). Times the outcome->discovery loop (/metrics-pull Step 8b + session-start CHECK 10)."
        },
        "theory_gates_status": {
          "type": "object"
        },
        "theory_gates": {
          "type": "object"
        },
        "definition_of_done": {
          "$ref": "#/$defs/definition_of_done"
        },
        "parked_reason": {
          "type": "string"
        },
        "parked_at": {
          "$ref": "_common.schema.json#/$defs/iso_timestamp"
        },
        "resume_conditions": {
          "type": [
            "array",
            "string"
          ],
          "items": {
            "type": "string"
          }
        },
        "killed_at": {
          "$ref": "_common.schema.json#/$defs/iso_timestamp"
        },
        "killed_reason": {
          "type": "string"
        },
        "learnings": {
          "type": [
            "array",
            "string"
          ]
        },
        "pivot_history": {
          "type": "array"
        },
        "notes": {
          "type": "string"
        },
        "progression_ruling": {
          "type": "string",
          "enum": [
            "progressed",
            "blocked",
            "needs-evidence"
          ],
          "description": "Written by /mycelium:diamond-progress on EVERY assessment, v0.92.0+. Records that a progression judgement happened and which way it went. Absent means never assessed \u2014 not passed. Enumerated rather than free text because the only prior consumer grepped decision-log prose for 'block', which any paragraph explaining the gates satisfies without a gate having fired."
        },
        "progression_ruled_at": {
          "type": "string",
          "format": "date",
          "description": "ISO-8601 date the progression_ruling was written. Lets a consumer tell a fresh ruling from a stale one."
        },
        "progression_blockers": {
          "type": "array",
          "description": "Required when progression_ruling is blocked or needs-evidence; omitted when progressed.",
          "items": {
            "type": "object",
            "required": [
              "gate",
              "reason"
            ],
            "properties": {
              "gate": {
                "type": "string",
                "description": "Which gate or threshold did not clear."
              },
              "reason": {
                "type": "string",
                "description": "Why, in the assessing agent's own words."
              },
              "unblocked_by": {
                "type": "string",
                "description": "The skill or evidence that would clear it."
              }
            }
          }
        },
        "progression_history": {
          "type": "array",
          "description": "One entry per transition crossed, appended by /mycelium:diamond-progress (v0.248.0): `transition` (\"define -> develop\") or `from`/`to`, `date`, `ruling`, and the gate statuses relied on. The scale-lock gate refuses a forward phase move without one for each transition it crosses.",
          "items": {
            "type": "object",
            "additionalProperties": true
          }
        },
        "pmf": {
          "$ref": "#/$defs/pmf"
        },
        "spawned_by": {
          "type": "string",
          "description": "Which skill opened this diamond, when it was opened by a mechanism rather than by hand (v0.232.0). `launch-tier` for an L5 spawned by a major-launch categorisation."
        },
        "spawn_trigger": {
          "type": "string",
          "description": "The condition that fired the spawn, in words, so a later reader can tell a mechanical spawn from a judgement call."
        }
      },
      "required": [
        "id",
        "scale",
        "phase",
        "confidence"
      ],
      "additionalProperties": true
    },
    "pmf": {
      "type": "object",
      "description": "L5's entry condition, measured with Ellis's Must-Have Survey (v0.232.0). Cagan puts product/market fit as the condition L5 is not reachable before; Ellis supplies the only operational test in this corpus. Typed rather than left free-form because the failure this prevents is a band asserted without a sample behind it.",
      "required": [
        "band"
      ],
      "properties": {
        "instrument": {
          "type": "string",
          "description": "Which test produced the band. `ellis-must-have-survey` is the one this framework ships; name another if you used one, so a later reader knows what the number means."
        },
        "very_disappointed_pct": {
          "type": [
            "number",
            "null"
          ],
          "description": "Percent answering 'very disappointed'. NULL when not measured \u2014 null and 0 are different claims, and writing 0 for 'we did not ask' is the error this nullability exists to prevent.",
          "minimum": 0,
          "maximum": 100
        },
        "n": {
          "type": "integer",
          "description": "Sample size. REQUIRED reading before believing any percentage here: a percentage over four users is not a percentage.",
          "minimum": 0
        },
        "band": {
          "type": "string",
          "enum": [
            "green",
            "tweak-product-or-language",
            "wrong-audience-or-underbuilt",
            "not-yet-measurable"
          ],
          "description": "Ellis's three bands plus the honest fourth. green = >=40%, 'the green light to move full speed ahead'. tweak-product-or-language = 25-40%, and note his own wording puts LANGUAGE beside product. wrong-audience-or-underbuilt = <25%. not-yet-measurable = the sample is too small to state a percentage, which is a real answer and the expected one for a young project \u2014 an L5 that knows it has no PMF evidence is a true record."
        },
        "as_of": {
          "type": "string",
          "description": "YYYY-MM-DD the survey was run. A PMF reading with no date cannot go stale, and PMF is exactly the kind of number that does."
        }
      },
      "additionalProperties": true
    },
    "completed_diamond": {
      "description": "A diamond in completed_diamonds. Everything a diamond has, PLUS proof that the Definition of Done was met. The extra requirements are the point: 'completed' asserted without a verdict is the state this schema exists to make impossible, because an unevidenced completion is indistinguishable from work that merely stopped (v0.230.0).",
      "allOf": [
        {
          "$ref": "#/$defs/diamond"
        },
        {
          "type": "object",
          "required": [
            "completed_at",
            "dod_verdict"
          ],
          "properties": {
            "completed_at": {
              "type": "string",
              "description": "YYYY-MM-DD the DoD was judged met."
            },
            "dod_verdict": {
              "type": "object",
              "description": "How the pinned DoD was judged met. Written by /diamond-progress Step 9b.",
              "required": [
                "signal_observed",
                "verified_by"
              ],
              "properties": {
                "signal_observed": {
                  "type": "string",
                  "description": "What was actually OBSERVED, in the observer's words \u2014 not a restatement of the DoD's signal. If you can only restate the target, the signal was not observed."
                },
                "verified_by": {
                  "type": "string",
                  "description": "How it was checked: a metric snapshot, a named person, a test, a link. 'self-assessed' is permitted and is a real answer; an empty one is not."
                },
                "threshold_met": {
                  "type": [
                    "string",
                    "number",
                    "boolean"
                  ],
                  "description": "The DoD threshold's actual value at completion, where the DoD declared one."
                },
                "shortfall": {
                  "type": "string",
                  "description": "Where the outcome fell short of the DoD but the diamond was completed anyway. Recording it keeps 'completed' honest instead of forcing a binary that invites rounding up."
                }
              },
              "additionalProperties": true
            }
          }
        }
      ]
    },
    "definition_of_done": {
      "type": "object",
      "description": "Outcome-based done-criterion pinned at diamond birth (v0.43.0, /define-done). outcome + signal required; the rest optional per skills/define-done/SKILL.md.",
      "properties": {
        "outcome": {
          "type": "string",
          "description": "What changes, for WHOM \u2014 a behaviour, not a feature. Problem-first."
        },
        "signal": {
          "type": "string",
          "description": "The ONE observable thing you'd see them DO (OMTM)."
        },
        "kind": {
          "type": "string",
          "enum": [
            "leading",
            "lagging",
            "mixed"
          ]
        },
        "threshold": {
          "type": [
            "string",
            "number"
          ]
        },
        "measure": {
          "type": "object",
          "description": "Optional (v0.53.0). Makes `signal` checkable post-ship to close the outcome->discovery loop (/metrics-pull Step 8b). source: 'manual' is first-class. See skills/define-done/SKILL.md.",
          "properties": {
            "source": {
              "type": "string",
              "description": "Metric adapter key (github, plausible, ...) OR 'manual'."
            },
            "field": {
              "type": "string",
              "description": "Snapshot field path for automated sources; omit for source: manual."
            },
            "target": {
              "type": [
                "string",
                "number"
              ]
            },
            "check_after": {
              "type": "string",
              "description": "When to check \u2014 lag matched to `kind`. Per-DoD, not a global window."
            },
            "guardrail": {
              "type": "string",
              "description": "Counter-metric that must not worsen while `signal` improves (Goodhart guard)."
            },
            "last_checked": {
              "type": [
                "string",
                "null"
              ],
              "description": "Stamped by /metrics-pull; null = never checked."
            }
          },
          "additionalProperties": true
        },
        "rolls_up_to": {
          "type": "string",
          "description": "Parent diamond id + which parent outcome this serves (contribution-not-summation). Child diamonds only."
        },
        "kill_criterion": {
          "type": "object",
          "properties": {
            "state": {
              "type": "string"
            },
            "date": {
              "$ref": "_common.schema.json#/$defs/iso_timestamp"
            },
            "premortem": {
              "type": "string"
            },
            "on_expiry": {
              "type": "string",
              "description": "What happens when `date` passes and neither the outcome nor the kill has fired. The default is stop, or re-pitch with a new written date; 'continues' is not an answer (v0.227.0)."
            }
          },
          "required": [
            "state",
            "date"
          ],
          "additionalProperties": true
        },
        "reader_test": {
          "type": "object",
          "description": "Step 5 of /mycelium:define-done (v0.227.0): someone who did not write the bar resolved invented result sets with it. `resolved` short of `cases` means a word in the bar failed.",
          "properties": {
            "date": {
              "type": "string"
            },
            "reader": {
              "type": "string"
            },
            "cases": {
              "type": "integer",
              "minimum": 0
            },
            "resolved": {
              "type": "integer",
              "minimum": 0
            },
            "note": {
              "type": "string"
            }
          },
          "additionalProperties": true
        },
        "log": {
          "type": "array",
          "description": "Dated record of how this Definition of Done has been scored, re-specified or ruled on. Updates go HERE, never as new dated keys beside or inside the bar (outcome, signal, threshold, kill_criterion). The bar is what the owner steers by and has to stay short enough to state from memory; the log is allowed to grow. check_dod_shape.py reports the two sizes.",
          "items": {
            "type": "object",
            "properties": {
              "date": {
                "type": "string",
                "description": "YYYY-MM-DD the update was made."
              },
              "text": {
                "type": "string"
              },
              "moved_from": {
                "type": "string",
                "description": "Retrofit only: the dated key this entry used to be."
              }
            },
            "required": [
              "date"
            ],
            "additionalProperties": true
          }
        },
        "provenance": {
          "type": "object"
        }
      },
      "required": [
        "outcome",
        "signal"
      ],
      "additionalProperties": true
    }
  }
}
