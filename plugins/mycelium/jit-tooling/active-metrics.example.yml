# Example active-metrics.yml (auto-generated by /metrics-detect)
# This file is gitignored by default -- it declares the metric sources
# being tracked for this project. Targets are not secrets, but snapshots
# may reveal business state. Opt in to committing by un-gitignoring
# active-metrics.yml and .claude/evals/metrics/ explicitly (e.g., for
# dogfood / open-source projects that want to track their own curve).
#
# Do NOT store API keys, tokens, or credentials here. Mycelium is not a
# secrets manager; all auth goes through env vars and vendor CLIs.

detected_at: "2026-04-16T10:00:00Z"
confirmed_by_user: false

sources:

  # Reference adapter, ships with Mycelium.
  github:
    status: "active"           # active | deferred | disabled
    target: "owner/repo"       # required
    window_days: 14            # GitHub traffic API window (max 14)
    adapter_version: 1
    last_pulled_at: null
    # known_referrers: [...]   # optional override of defaults in github.md

  # Generated on demand via metrics-adapters/GENERATING.md
  plausible:
    status: "active"
    target: "example.com"      # your Plausible site
    window_days: 30
    adapter_version: 1
    last_pulled_at: null

  stripe:
    status: "deferred"         # user said "later" during /metrics-detect
    target: "prod"             # environment label, not a secret
    adapter_version: 1
    last_pulled_at: null

  app_store_connect:
    status: "disabled"         # adapter generation failed or source not applicable
    disabled_reason: "No app on App Store yet. Re-enable when launching."
    target: ""
    adapter_version: 0
