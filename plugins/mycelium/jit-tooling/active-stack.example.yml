# Example active-stack.yml (auto-generated by /delivery-bootstrap)
# This file is gitignored -- it's generated per project.
# Copy this to active-stack.yml and customize, or let the skill auto-detect.

detected_at: "2026-04-07T10:00:00Z"
confirmed_by_user: false  # Set to true after user confirms

languages:
  - name: "typescript"
    runtime: "node"
    version: ""

shape: "single-app"  # single-app | monorepo | multi-service
complexity_tier: "solo"  # solo | team | enterprise

commands:
  test: "npm test"
  typecheck: "npx tsc --noEmit"
  lint: "npx eslint ."
  lint_fix: "npx eslint . --fix"
  format: ""
  build: "npm run build"
  security_scan: "npm audit"
  a11y_test: ""  # Filled if frontend detected (e.g., "npx axe http://localhost:3000")

validation_suite: "npm test && npx tsc --noEmit && npx eslint ."

existing_ci: false
existing_tests: false
existing_linting: false
existing_security_scanning: false
