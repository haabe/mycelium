# THE FRAMEWORK-DEV GATE SET — one source of truth for CI and the pre-push hook.
#
# WHY THIS FILE EXISTS
# -------------------
# Until v0.110.0 the gate list lived in two hand-maintained places: the steps in
# `.github/workflows/validate.yml` and a hard-coded run of four scripts inside
# `scripts/git-pre-push-example.sh`. Measured 2026-08-09 they had drifted to
# **11 in CI against 4 in the hook** — seven gates that could only ever go red
# after a push. That is the precise failure `corrections.md` recorded on
# 2026-06-18 ("Local validation != CI gates"), whose own prevention rule was
# "when adding a CI-only gate, add it to the pre-push hook OR document the
# run-locally step in the same commit". The rule was followed for the gate that
# motivated it and for nothing after: seven later gates went to CI alone.
#
# A hand-maintained list in two files diverges. It is not a discipline problem;
# it is two lists. So there is one list, here, and `check_gate_parity.py`
# asserts that every check invoked in CI appears in it.
#
# FORMAT
#   <script-name>[ <args>]      one gate per line; `#` starts a comment
#   !waived <script-name> <reason>   a CI gate deliberately NOT run locally
#
# A waiver needs a reason on the same line. The alternative to supporting
# waivers is that the first genuinely CI-only gate gets "fixed" by deleting the
# parity check, and then nothing watches the list at all.
#
# SCOPE: framework development only. The hook runs this set solely when
# `tests/python/` is present; a downstream project consuming the plugin never
# reaches it.

!waived check_coverage_floor.py needs a coverage.json from a --cov test run, which costs 192s
#   against 78s without instrumentation. That duration caused two failures on 2026-08-31: GitHub
#   closed the idle SSH connection mid-hook so every push died with SIGPIPE after passing, and an
#   agent reached for --no-verify and put a red commit on main. CI enforces this gate on every
#   push (validate.yml), so nothing is unenforced — it is just not enforced twice.
check_legacy_paths.py --root .
check_doc_references.py --root .
check_theory_fidelity.py --root .

# Added v0.110.0 — the seven that were CI-only. All seven run in well under a
# second on this tree, so the reason they were not here was never cost.
check_wiring.py --root .
check_wiring_contract.py --root .
check_negative_control.py --root .
check_test_authenticity.py --root .
check_empty_input_honesty.py --root .
check_source_class_fidelity.py --root .
check_surface_registry.py

check_purpose_stance.py

# The parity guard gates itself. If CI gains a check and this file does not, the
# hook fails before the push rather than CI failing after it.
# Added v0.122.0. The gate-parity guard caught this one being CI-only in the same
# run that filed P6 — a registry row about a BLOCK guardrail unmet BECAUSE its
# checks are CI-only. The guard was right about its author.
check_promise_registry_swept.py --root .

check_instrument_contract.py --root .
check_gate_parity.py --root .

# touch_log[-1] must genuinely be the most recent entry. An out-of-order log
# parses cleanly and silently answers "what happened last" wrong.
check_touch_log_order.py --root .

# REPORT-ONLY until the review registry is populated. Runs without --strict, so it
# exits 0 and prints. It found 42 fail-open sites in this tree on 2026-08-26, 31 of
# them SILENT, and NO baseline was seeded: ruff.toml's own policy forbids
# re-introducing a tolerated-debt baseline, and the dogfood precedent is 11 entries
# each carrying a written judgement, not a bulk rubber-stamp. Add --strict here once
# harness/fail-open-reviewed.yml holds real verdicts.
check_fail_open.py --strict
#   --strict ADDED 2026-08-31 (v0.153.0) after CI went red twice on this gate while the
#   pre-push hook passed. CI ran `--strict`, the local set ran the bare script, and
#   check_gate_parity compares SCRIPT NAMES not flags — so the same gate was advisory
#   locally and blocking in CI, which is the 2026-08-09 drift wearing a disguise: not a
#   missing gate, a gate with different teeth in the two places.
check_field_wiring.py --strict
check_target_progress.py --canvas-dir .claude/canvas
check_confidence_managed.py
