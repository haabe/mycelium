# Test fixture registry: contains a generic_only person whose name does NOT
# appear anywhere in the fixture's tree. Check 33 must pass.
people:
  - name: FixtureTestPersonA
    consent: generic_only
  - name: FixtureTestPersonB
    consent: public_ok
