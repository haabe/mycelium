# Consistency check spec (fixture)
## Promise registry (capability claims awaiting mechanism)
No marker at all. This is NOT the same finding as a stale sweep: absent means the contract
was never adopted, stale means it was adopted and then not honoured.
