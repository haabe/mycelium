# Consistency check spec (fixture)
## Promise registry (capability claims awaiting mechanism)
**last_swept: 2026-08-01** — a fixed PAST date. The test pins `--today`, so this fixture does
not decay with the calendar; a fixture that ages into failure fails on a date nobody chose.
