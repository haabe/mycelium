# Consistency check spec (fixture)
## Promise registry (capability claims awaiting mechanism)
**last_swept: 2999-01-01** — the bypass path: a date far enough ahead that the check never
fires again. Guarded because it is the cheapest way to turn an instrument into decoration.
